/* ============================================================
   DarkPrint backend — lib/server/policy public surface
   ============================================================ */

export type { Action, Actor, Resource } from "./types";
export { can } from "./can";
export { visibleTo } from "./visible-to";
