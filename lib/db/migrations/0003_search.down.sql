-- Reverses 0003_search. The indexes and the foreign keys go with the tables that
-- carry them, so the two DROPs are the whole reversal.
DROP TABLE IF EXISTS "card_version_embedding";
DROP TABLE IF EXISTS "release_embedding";
