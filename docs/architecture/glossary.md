# 2 · Domain glossary

[← Back to the index](../ARCHITECTURE.md)

Definitions below are summarised from `docs/architecture-wip/ENTITIES.md` (Phase 2A); no
fact here was re-derived from the code independently of that inventory. The full field-level
detail for every entity is in [3 · Concept model](concept-model.md). The table at the end of
this section — the naming inconsistency table — is carried over from `ENTITIES.md` §10
**unchanged**.

| Concept | Definition | Synonyms in UI copy | Synonyms in code | Where it lives | Status |
|---|---|---|---|---|---|
| Blueprint | A graph of automations, described by a DOT topology plus the node cards its nodes pin. The headline unit of the registry, not "the dark factory" (`ENTITIES.md` §1). | "blueprint", "bundle" (as a working copy), "folder" (as a download), "release", "snapshot" | `Bundle` (raw upload), `ResolvedBlueprint` (joined), `BlueprintRecord` (query index), `Blueprint` (view model), `OwnedBundle`, `Release`, `BundleView` | `lib/core/bundle/types.ts`, `content/blueprints/<slug>/` | LIVE |
| Node card | A versioned, reusable unit of work: one agent's spec, ports, tools and prohibitions. Referenced from a DOT node by `id@version`, never embedded in the topology file. | "Cards" (nav), "Node cards" (`/nodes` h1), "Node card" (spec pages) | `NodeCard`, `CardVersionRecord`, `CardRef`, `Port` (one input/output on a card) | `lib/core/card/schema.ts:26`, `content/cards/<id>@<version>.yaml` | LIVE |
| DOT topology | The blueprint's graph: nodes and edges only, no card content. A DOT node's attributes carry a *pointer* at a card (`card="id@version"`), never the card itself. The file is `topology.dot`, renamed from `blueprint.dot` per the owner's 2026-08-25 instruction — the folder is the blueprint (a repository), this file is its topology. | "topology.dot", "the DOT file", "the topology" | `TOPOLOGY_DOT`, `ResolvedBlueprint.dot`, `DotAttrs`, `ResolvedEdge` (one topology edge, joined to its ports) | `lib/core/dot/**`, `content/blueprints/<slug>/topology.dot` | LIVE |
| Resolution | The join of a DOT node to its card, producing `ResolvedNode`. Topology outlives a missing card — an unresolved node is still counted, as a third category alongside unattended/staffed. | "resolves" / "unfinished" / "rejected" (the three states shown on `/upload`) | `ResolvedNode`, `ResolvedBlueprint`, `loadBundle` | `lib/core/bundle/types.ts:43`, `lib/core/bundle/resolve.ts` | LIVE |
| Ontology / vocabulary | The shared vocabulary a blueprint and its cards are written against: five closed kinds of term (`phase`, `node-type`, `risk-marker`, `data-type`, `tool`). A curated core merged with an optional local overlay. **There is exactly one living vocabulary** since 2026-08-30, and since 2026-09-05 it has **no version at all** (D-131): `CORE_ONTOLOGY` (**54 terms** — 5 phases, 13 node types, 9 risk markers, 15 data types, 12 tools, measured off `lib/core/ontology/core.ts` rather than derived). `Ontology` is `{ title, terms }`. The reason is on the type at `lib/core/ontology/types.ts:76-82`: the vocabulary exists to name what an Attractor node IS, Attractor fixes those shapes in its own spec and carries no vocabulary version, so a DarkPrint-only semver on top named a thing nothing else moved with. Terms are still added and retired inside the one vocabulary, through `deprecated`, which is the mechanism §6.2 needs. **The residue is `OntologyTerm.since`**, which still means *the ontology version that introduced the term* and now has no such thing to name; all 54 core terms carry `V01`, whose docblock says it is a release rather than a version. | "Ontology" (browser, per D-40), "Ontology file (YAML)" (format), "The vocabulary" (the vocabulary browser's rail label, on `/spec/ontology` since 2026-09-06 — a naming inconsistency, see below) | `Ontology`, `OntologyView`, `OntologyTerm`, `ContentKind = "ontology"` **and** `VocabularyBrowser`/`BundleVocabulary` | `lib/core/ontology/**`, `content/ontology/extensions.yaml` | LIVE |
| Shell tool | A node type whose instruction is a shell command rather than prose, carried in `params.tool_command`. Added 2026-09-04 with `broader: tool`, and it exists so an imported Attractor `parallelogram` round-trips: `tool` maps to `box`/`codergen` because §4.10's parallelogram handler FAILs with no command and all 25 of the archive's `type: tool` cards instruct with prose. **No card in `content/` is one**, deliberately. | not named in UI copy; `/nodes` prints the label "Shell tool" | `shell-tool` (term id), `TOOL_COMMAND_KEY`, `ATTRACTOR_TYPE_SHAPES["shell-tool"]` | `lib/core/ontology/core.ts`, `lib/core/card/schema.ts:315`, `lib/core/attractor/emit.ts:198` | LIVE |
| Condition (edge guard) | The expression on an Attractor edge that decides whether §3.3 may take it. DarkPrint **parses and validates its syntax and evaluates none of it**: `parseCondition` answers clauses and problems, and no caller can ask whether an edge is taken. The rule it feeds, `attractor/condition-syntax`, is named after Attractor's own `condition_syntax` (§7.2) so a reader can match the two by eye, and is graded WARNING here where Attractor grades it ERROR — `gate.ts` rule 4 forbids a DarkPrint inference from refusing anybody's bundle. The grammar followed is §10.2's, not §10.5's evaluator, and each refusal states what a §10.5 runner would do instead. | "guard" in the Attractor compatibility panel | `ResolvedEdge.condition`, `parseCondition`, `ConditionClause`, `attractor/condition-syntax` | `lib/core/attractor/condition.ts`, `lib/core/attractor/lint.ts` | LIVE |
| Term | One entry in the ontology: a phase, node type, risk marker, data type or tool. Namespaced (e.g. `lupo/pii-handling`) when local; bare when core. | "term" | `OntologyTerm`, `TermDeprecation` (a term's redirect-on-deprecation record) | `lib/core/ontology/types.ts:27` | LIVE |
| Autonomy | A static graph reading of how much of a blueprint runs unattended, expressed only as a class name (`assisted` / `supervised` / `conditional` / `closed-loop`) — never as the underlying 1-4 ordinal. **Two readings since 2026-08-30**: `staffingFraction` is the headcount (unattended nodes over all nodes) and `control.fraction` is the same share over control points alone. `fraction`, the number every surface prints and the band reads, is the weaker of the two, or the headcount alone when the graph declares fewer control points than `DARKPRINT_CONFIG.autonomy.minControlPoints` (2). | "Autonomy" | `AutonomyResult`, `AutonomyClass`, `AutonomyControl`, `computeAutonomy` | `lib/core/analysis/autonomy.ts` | LIVE |
| Control point | A node whose type decides whether and how other nodes run, as distinct from a node that does the work. Membership is the inherited `OntologyTerm.governsFlow` flag, carried by `evaluative`, `orchestration` and `human-gate` and inherited down `broader`. Computed in one place and never re-derived. | not named in UI copy; the rationale sentence counts them ("2 of 4 control points run unattended") | `governsFlow`, `controlCitation`, `isControlPoint`, `AutonomyControl`, `AutonomyContribution.controlTerm` | `lib/core/ontology/resolve.ts`, `lib/core/analysis/autonomy.ts` | LIVE |
| Dark factory | A *computed property* of one graph shape — every node unattended **and** all five phases covered — never the site's category or its goal (D-02, D-28). | "dark factory" | `isDarkFactory` (`AutonomyResult` field) | `lib/core/analysis/autonomy.ts:317` | LIVE |
| Static risk exposure | The engine's structural risk reading over a blueprint's declared and inferred risk markers. Never represented as a security audit. | "Static risk exposure" | `SecurityResult`, `MetricKey = "security"`, `computeSecurity` | `lib/core/analysis/security.ts` | LIVE |
| Phase coverage | Which of the five fixed lifecycle phases (`planning`, `implementation`, `testing`, `debugging`, `deployment`) a blueprint's nodes touch. A description of scope, never a score or a to-do list. | "phase coverage" | `PhaseCoverage`, `computePhaseCoverage` | `lib/core/analysis/phase-coverage.ts` | LIVE |
| Metric | One of six scored axes on a blueprint's scorecard (autonomy, efficacy, reliability, transparency, cost/time, security), each carrying a `source` that decides who may write it. **Still computed, still stored, and rendered on no blueprint page since 2026-09-04**: the owner asked the whole scoring reading off `/blueprints/{owner}/{slug}`, so the only surfaces left that draw a metric are `components/build/ScorePanel.tsx` on `/build` and `components/upload/ValidationReport.tsx` on `/upload`, both over a graph the reader is holding rather than one the registry published. **Three of the six now have no producer at all**: efficacy, reliability and transparency were `source: "community"` and were to be filled by a ballot, and the ballot was deleted on 2026-09-05 (§11.0 Q14), so `metricsFor`'s community arm is unreachable and the three come out of `EMPTY_COMMUNITY` as zeros. `/upload` says so in the open, in both the exported report and the publish-button block: they are filled in by nothing. | "scorecard", the six axis labels | `Metric`, `MetricSource`, `metricsFor` | `lib/types.ts:41`, `lib/content/view.ts:150-211` | LIVE (autonomy, security) / MOCK (efficacy, reliability, transparency, cost) |
| Digest | The content identity of a card or bundle: a sha256 hash over canonical JSON (card) or over the DOT plus sorted card digests (bundle), excluding authorship metadata. | "digest", "version sha256:…" | `cardDigest`, `bundleDigest` | `lib/core/hash/digest.ts` | LIVE |
| Prohibition | What a card refuses to receive, in two fields that are not the same promise. `cannot` holds `data-type` term ids ONLY and the resolver checks every edge against it (`bundle/prohibition-violated`, error). `willNot` holds the author's own prose undertakings and is read by nothing. Both are required and both default to `[]`. | "What it refuses" (rail), "cannot receive", "will not" | `NodeCard.cannot`, `NodeCard.willNot`, `ProhibitionView`, `bundle/prohibition-violated`, `card/prohibition-misfiled` | `lib/core/card/schema.ts`, `lib/core/bundle/resolve.ts` | LIVE |
| Storable versus approved | Two different questions about one diagnostic list. **Storable** asks whether an artefact may be held at all — it parses, it is addressable, it is attributable — and is answered by a named list of twelve `DiagnosticCode`s rather than by severity. **Approved** is a score, and a score is allowed to be bad. Nothing DarkPrint *infers* may block storage; a `bundle/prohibition-violated` may, because the card's own author asked for it. | not surfaced in UI copy | `GateClass`, `DIAGNOSTIC_GATE`, `STORAGE_BLOCKING_CODES`, `INFERRED_CODES`, `isStorable`, `blocksStorage` | `lib/core/gate.ts` | LIVE |
| Draft bundle | What `darkprint import` synthesises from an Attractor pipeline: a `topology.dot`, one card per node at version `0.1.0`, and a stub manifest. Every card carries `author: <handle>` (who is answerable) and `provenance: derived:attractor <origin>` (where the prose came from). A draft is never a release. | "DRAFT" (the CLI's own stderr notice) | `importAttractorDot`, `DRAFT_CARD_VERSION`, `DERIVED_PROVENANCE_PREFIX`, `importPipeline` | `lib/core/attractor/import.ts`, `packages/cli/src/import.ts` | LIVE |
| The private / runtime-read line | Which attributes of an emitted Attractor DOT a runtime actually reads, declared as data and checked in both directions. `ATTRACTOR_EMITTED_ATTRIBUTES` names every attribute the emitter writes with Attractor's own meaning and must be a subset of the reserved sets; `DARKPRINT_EMITTED_ATTRIBUTES` names every one it writes that Attractor ignores and must be disjoint from them; `ATTRACTOR_UNEXPRESSED_ATTRIBUTES` is derived as the remainder and is printed into the header of every emitted file. | the disclosure header at the top of an exported `.dot` | `ATTRACTOR_EMITTED_ATTRIBUTES`, `DARKPRINT_EMITTED_ATTRIBUTES`, `ATTRACTOR_UNEXPRESSED_ATTRIBUTES`, `ATTRACTOR_SPEC_PIN` | `lib/core/attractor/emit.ts`, `lib/core/attractor/reserved.ts` | LIVE |
| Registry / Archive / Index | Three separate stores the UI keeps apart: the **archive** (`content/`, immutable, read by `lib/content`), the **index** (`lib/data/**`, everything that moves — votes, downloads, ownership), and `Registry` (`lib/core/archive/registry.ts`, the in-memory query layer rebuilt from the archive each process). | "the registry" (whole product), "the archive", "the index", "the library" | `Registry`, `lib/content`, `ContentStore` (unused) | `lib/core/archive/registry.ts`, `lib/content/**`, `lib/data/**` | LIVE (archive, Registry) / MOCK (index) / PLANNED (`ContentStore`) |
| Author | The person credited on a card, blueprint or comment. A string field on the content itself, unchecked against any account. | "author", "handle", "@username", "owner", "builder" | `Author.username` | `lib/types.ts:167`, `lib/data/users.ts` | MOCK |
| Account | The one signed-in identity the fixture can represent — singular, because there is no session. | "you", "your account" | `Account` | `lib/data/account.ts:45` | MOCK |
| Profile | The public page at one handle: pinned items, counted archive facts, seeded preview signals (downloads, stars, watchers, community support, validated). | "profile" | `Profile`, `PinnedRef` (a pinned blueprint or card, at most two) | `lib/data/profiles.ts:33` | MOCK (seeded fields) / LIVE (counted fields) |
| `similar:purpose` | The one evidence marker of the semantic channel (T300): a hit carrying it was retrieved by cosine proximity of the query to the release's embedded purpose text, not by a token match, and such hits only ever form a contiguous tail behind every lexical hit. Value pinned by T300's suite; constants and calibration live beside `SIMILAR_MIN` in `lib/server/search/embed.ts`. | search results, MCP `search` | `Hit.evidence` | `lib/server/search/embed.ts` | LIVE |
| Validated | How many OTHER accounts' blueprints this handle downloaded, ran, and submitted a run report for (SEAM-84) that made it onto that blueprint's own run evidence. Seeded per handle. It used to be paired with a claim that it never moved any blueprint's `EvidenceLayers`, which always read "no verified runs"; that panel was deleted on 2026-09-04, so the figure now stands beside nothing at all rather than beside a panel that disagreed with it. | "validated" (account panel) | `Profile.validated` | `lib/data/profiles.ts:78` | PLANNED (the run-report pipeline it presupposes, SEAM-84, is undeclared UI) |
| Owned card | An account's node card, public (a document in `content/cards/`) or private (a fixture in `lib/data/cards.ts`), the card-side counterpart to `OwnedBundle`. | "your cards", "Private" pill | `PrivateCard`, `NodeSummary.visibility` | `lib/data/cards.ts` | MOCK (private rows) / LIVE (public rows) |
| Owned bundle | An account's working copy of a blueprint — published or still a draft, public or private, with an optional fork lineage. No separate `Fork` type exists. | "your blueprints", "bundle" | `OwnedBundle`, `Draft`, `Lineage`, `Drift` (vs. the upstream), `BundleFile` (a listed file), `UpstreamMoved` (a pinned card's upstream repin) | `lib/data/bundles.ts:155` | MOCK |
| Save | A private bookmark, and **no longer its own gesture** since 2026-09-05 (D-132): the owner folded Save into Star, so on a surface that has read `getSignals` there is one control and starring a CARD writes the `save` row beside the counter. A card save is an account row (`POST`/`DELETE /api/account/saves`) and the Saved tab reads it through `listSaves`; a blueprint save is still browser-local and reaches no shelf, because `save.target_id` holds a bundle id and the star control holds a slug (D-262-04). A tile with no signals behind it still draws the plain bookmark. | "Save", "Saved" (tab), "Star" (the control that now writes it) | `Save`, `SaveRecord`, `listSaves`, `FavoriteStar` (`StarControl`, `SeededStar`, `Bookmark`), `localStorage["darkprint:favorites"]` | `lib/server/saves/**`, `components/ui/FavoriteStar.tsx` | LIVE (card saves, the Saved tab) / MOCK (blueprint saves, browser-local) |
| Community signals | The mutable, per-blueprint index row: downloads, votes/support, three community-voted metrics, comments. **"Community-voted" is now a name for how the three were MEANT to be filled and never a description of how they are** — the ballot that would have voted them was deleted on 2026-09-05 (§11.0 Q14), so efficacy, reliability and transparency are assembled from `EMPTY_COMMUNITY`'s zeros, rendered nowhere, and have no producer anywhere in the tree. | "community stars", "support", "N public" | `CommunitySignals` | `lib/data/community.ts:42` | MOCK |
| Comment | A note attached to a blueprint or a card. `id` is local to its parent row today, so the real key is `(slug, id)`. | "Community notes" | `Comment` | `lib/types.ts:178` | MOCK |
| Reported cost | The one metric whoever ran a blueprint could someday self-report; the platform never observes a run. Nothing produces one yet. | "Cost / time", "Reported by runners" | `ReportedCost`, `MetricSource = "reported"` | `lib/data/community.ts:30` | PLANNED |
| MCP server | The proposed read-only interface an agent would use to search and fetch from the registry. No server exists; the page is a design proposal. | "MCP" | `lib/mcp.ts` | `app/mcp/page.tsx`, `lib/mcp.ts` | PLANNED |
| Key scope | What an API key is allowed to do, chosen once when the key is minted and never afterwards. Two values, `read` and `write`. **Every key minted before 2026-09-05 is read-scoped for life** — migration `0010_key_scope` adds the column `NOT NULL` with default `read`, so the backfill grandfathers each existing key into the promise it was actually issued under rather than into a wider one. A read key is what `/settings` §06 has always described: it raises the MCP read-rate ceiling, carries no identity and authorizes no write. A write key acts as its account, and **reaches no route yet** — `writeActorFor` is the only published key-to-`Actor` path and nothing in `app/api/**` calls it. The check is a RE-READ at the moment of the write, requiring `scope='write' AND revoked_at IS NULL`, because the `ResolvedKey` brand proves where a record came from and can never prove it is still live. | "Read" / "Write" on the mint form; the per-scope sentence under it | `KeyScope`, `KEY_SCOPES`, `isKeyScope`, `writeActorFor`, `ApiKeyRecord.scope` | `lib/server/limits/`, `lib/db/migrations/0010_key_scope.up.sql`, `components/settings/ApiKeys.tsx` | LIVE (the column, the type, the mint form, `writeActorFor`) + PLANNED (no route accepts a key for a write) |
| Authoring skill | The `skills/darkprint/` Claude Code skill that interviews an author into a blueprint folder. Installs and runs; does not publish or push. | "the skill", "Assisted Design" | `skills/darkprint/**` | `app/skill/page.tsx`, `skills/darkprint/` | LIVE (install) / PLANNED (push to registry) |
| Stored vocabulary | The shape `release.local_vocabulary` holds: `{ text, terms }` — `text` the author's `ontology/extensions.yaml` **byte for byte**, `terms` its parse. `null` is the other legal value. **Not a bare `OntologyTerm[]`**, which both merged readers refuse: T090 serves `text` verbatim at export rather than re-emitting it from the parse (D-90-03), so a release storing terms alone loses the author's comments, key order and formatting. Published as `StoredVocabulary` from `@/lib/server/archive` since T133 and refused at the WRITE, so a shape the readers would reject can no longer be created (D-133-01) | "Ontology extensions", "the vocabulary" | `StoredVocabulary`, `localVocabulary` (column), `vocabulary` (input field) | `{ text, terms }` in T090/T010/T130 fixtures | Backend, LIVE |

## Naming inconsistency table

Carried over unchanged from `ENTITIES.md` §10.

| Concept | Name in UI copy | Name in code | Name in fixtures | Why it matters |
|---|---|---|---|---|
| The reusable unit | "Cards" (header), "Node cards" (`/nodes` h1), "Node card" (spec pages), "node card" (`Save.kind`) | `NodeCard`, `CardVersionRecord`, `CardRef`, `ContentKind = "node"`, route `/nodes/[...id]`, `nodeHref` | directory `content/cards/`, `BUNDLE_CARDS_DIR = "cards"`, `CARD_LIBRARY_DIR = "cards"`, `public/cards/` | Four spellings for one table: `node`, `nodes`, `card`, `cards`. The route says `nodes` and the directory says `cards` |
| Vocabulary | "Ontology" — **which was the SPEC PAGE's own `h1` for a few hours on 2026-09-06 and was the browser's name before that**; the page that carried it, `/spec/ontology`, was itself folded into `/spec/card` later the same day (D-156), so the word is a band heading on the card layer page now and has been carried by three surfaces in one day without leaving the site. "Ontology file (YAML)" was the Learn menu row and the footer Specification column, and **that row is gone with the route**, so the chrome names the vocabulary nowhere and reaches it through `Node card (YAML)` — the entry that names the file whose fields the vocabulary supplies values for, ~~"Ontology terms" (profile tab)~~ (that tab and its route were deleted by the owner on 2026-09-06, so this spelling no longer ships anywhere; the vocabulary itself is untouched), "The vocabulary" (the browser band's rail label, which moved from `app/ontology/page.tsx` to `app/spec/ontology/page.tsx` on 2026-09-06 when the owner folded the two routes into one), "the curated core vocabulary" (`/upload`) | `Ontology`, `OntologyTerm`, `OntologyView`, `ContentKind = "ontology"` **and** `VocabularyBrowser`, `BundleVocabulary`, `bundleVocabulary()`, `BUNDLE_VOCABULARY`, `localTermsUsed`, `contentVocabulary()` | `content/ontology/extensions.yaml` | The code carries both words for one entity. D-40 (`CONFIRMED`) settled the *UI* on "Ontology" and left the identifiers split. **D-40 assigned its two UI words to two ROUTES, there is one route since 2026-09-06, and the collision was resolved by deleting a row rather than by renaming one.** "Ontology" was the browser's word and the browser has no route of its own now; repointing that Browse row at `/spec/ontology` would have put it and "Ontology file (YAML)" on one destination on one screen, which is what `components/site/nav.test.ts` refuses. So **the Browse row is gone from the header and the footer**, Browse is two rows rather than three, and the surviving word is the file form — the Learn row could not take the other one, because it is stop 03 of `SPEC_SEQUENCE` between "Topology file (DOT)" and "Node card (YAML)" and `spec-routes.test.ts` pins that shape. **What a reader loses is the word "Ontology" in the bar**; what carries them there instead is the Learn menu, the footer's Specification column, and the `/ontology` 308. Measured against `SiteHeader.tsx` and `SiteFooter.tsx` on 2026-09-06 |
| Ontology version | was "Scores computed under" | was `AutonomyResult.ontologyVersion`, `SecurityResult.ontologyVersion`, `BlueprintAnalysis.ontologyVersion` | none — no file in a bundle ever declared one | **CLOSED 2026-09-05: the concept left the vocabulary entirely (D-131), so there is no spelling to reconcile.** 2026-08-30 had removed it from the card and the manifest and left it on the score; the three copies on the analysis went in one edit, with the `BundlePanel` row that drew them and the disclaimer that two versions are not comparable. `ontology_version`/`ontologyVersion` on a card stays a retired key (`card/retired-field`, warning, `lib/core/card/validate.ts:160,164`), which is the one place the word still appears in shipped behaviour |
| Card wire keys generally | field names shown as `risk_markers`, `will_not`, `phase` | `riskMarkers`, `willNot`, `phases` | snake_case in every `content/cards/*.yaml`, with the camelCase spelling accepted as an alias | Mapped by `validate.ts`. A JSON API has to pick one and the two file types in a bundle disagree. `requires_human` left this row on 2026-08-30: it is retired, not renamed |
| Phase, singular/plural | "phase ·" chip per declared phase, "Phase coverage" | `NodeCard.phases: string[]` | wire key is singular `phase`, accepting a scalar or a sequence | `phase: planning` in the file, `phases: ["planning"]` in the model |
| The graph file | "topology.dot", "the DOT file", "the topology", "the blueprint file (DOT)" | `TOPOLOGY_DOT = "topology.dot"`, `Blueprint.graph.dot`, `ResolvedBlueprint.dot` | `content/blueprints/<slug>/topology.dot`; the only DOT file, in `content/` and in every exported bundle | Renamed from `blueprint.dot` to `topology.dot` per the owner's 2026-08-25 instruction. Until that same date, `exportBundle` also compiled a second, runnable DOT — `FACTORY_DOT = "factory.dot"`, never in `content/`, only in the export — which the owner then instructed out of every bundle entirely; a reader's own harness compiles the topology and cards into a runnable pipeline now. `FACTORY_DOT` was **deleted** from `lib/content/bundle-export.ts` on 2026-08-30: its only remaining consumers were test pins, and both suites now spell the name as a local literal, so the negative assertions still stand and the constant no longer implies a file. The Attractor compile itself did not go anywhere — it is an opt-in command now (`darkprint export <dir> --attractor`), never a file in a published folder |
| Security metric | "Static risk exposure" everywhere | `MetricKey = "security"`, `SecurityResult`, `computeSecurity`, `analysis.security`, `securityRaw` | no fixture row (computed) | The public label and the identifier share no word. Scales differ too: `SecurityResult.level` is 1-4 and `Metric.value` is 0-100 |
| Cost metric | "Cost / time", "Reported by runners" | `MetricKey = "cost"`, `MetricSource = "reported"` | `CommunitySignals.cost` (seeded number) **and** `CommunitySignals.reported` (`ReportedCost`) | One axis, two fixture fields with a precedence rule (`view.ts:196`) |
| Autonomy reading | the class name only: "Closed-loop", "Conditional", "Supervised", "Assisted" | `AutonomyClass` (kebab: `"closed-loop"`), `AutonomyResult.level` (1-4, never rendered), `AutonomyLevel` (`lib/types.ts:61`), `AUTONOMY_CLASS_LABELS` (`autonomy.ts:203`) **duplicated** as `AUTONOMY_LABELS` (`lib/format.ts:98`) keyed on the band | `Draft.autonomy` is a free-text title-case string (`"Closed-loop"`, `"Conditional"`, `"Assisted"`) | Three representations of one value: kebab enum, 1-4 ordinal, title-case string. The fixture stores the display string |
| Support / votes / stars | "Save", "community stars", "support", "N public", "stars" (account panel) | `Blueprint.votes`, `CommunitySignals.votes`, `Profile.support`, `starsFor()`, `FavoriteStar`, prop named `support` | `COMMUNITY[slug].votes`, `PROFILES[handle].support`, `NODE_DOWNLOADS` (support derived from it) | **The word "vote" now means only a star or a note upvote.** The BALLOT sense — a 0-100 score on efficacy, reliability or transparency — left the vocabulary on 2026-09-05 with the route and the write path (§11.0 Q14), so nothing on the site collects a vote in that sense and `Account.validatorWeight` multiplies nothing. One idea under four names, stored three different ways: per-blueprint counter, per-profile counter, per-card derivation. `ProfileHeader`'s `stars` is a fifth spelling and a different KIND of figure besides — a sum of the other four (`blueprints.votes` plus `cards.support`) computed in `ProfileShell`, not a sixth stored value |
| Bookmark | "Save", "Saved" (tab) — and "Star", since the two controls became one | `Save` type, `listSaves`, `FavoriteStar`, `localStorage["darkprint:favorites"]` | the `SAVES` fixture is unused; the shelf reads the `save` table | Two save sets became one UI gesture on 2026-09-05 without becoming one store: a card star writes the account row AND the counter, a blueprint star writes only the counter, and `Remove` on the Saved tab deletes the save while leaving the star standing (`components/profile/SavedList.tsx:117-126` says so in the footer) |
| Follow | "Watch" (button), "N watchers" | `Profile.watchers` | seeded integer | No verb/noun pair in code; the button verb has no field |
| Person identity | "handle", "@username", "owner", "author", "builder" | `Author.username`, `OwnedBundle.owner`, `NodeCard.author`, `BundleManifest.author`, `HistoryEntry.author`, `Comment.author` (an object, not a string) | `AUTHORS` is keyed by a **short key** (`mara`, `kwame`, `sol`, `orin`, `lupo`, `hachi`) that is **not** the `username` (`mara-veil`, `k0bra`, `sol-antczak`, `orin`, `lupo`, `hachi`) | Three of six fixture keys differ from the handle. `Comment.author` is an embedded object where every other author reference is a string |
| The container | "blueprint", "bundle", "folder", "release", "snapshot" | `Bundle` (raw), `ResolvedBlueprint`, `BlueprintRecord`, `Blueprint` (view model), `OwnedBundle`, `Release`, `BundleView` | `content/blueprints/<slug>/`, `public/bundles/<slug>/` | Five code types for one thing at different lifecycle stages, and the two directories disagree on singular/plural |
| The store | "the registry", "the archive", "the index", "the library" | `Registry` (in-memory query index), `lib/content` (the archive reader), `ContentStore`/`memoryContentStore` (unused), `COMMUNITY` (called "that database" in its own header) | `content/` | Four words, three of them for different layers. "Registry" in UI copy means the whole product; `Registry` in code means one query object |
| Bundle version | "version sha256:ab12cd…", "1 version", "v1.4.0-hardened" | `Release.version`, `HistoryEntry.version`, `BundleView.version`, `Blueprint.digest` | published: `version` **is** `shortDigest(digest)`; draft: a hand-typed semver | The same column holds a hash on one row and a semver on another |
| Notes | "Community notes" | `Comment`, `comments`, `Comments` | `COMMUNITY[slug].comments`, `NODE_COMMENTS` | The UI never says "comment" and the code never says "note" |
| Engine findings | "Validator checks", "notes", "problems", "diagnostics", "findings" | `Diagnostic`, `DiagnosticCode`, `SecurityFinding`, `explainedNotes`/`otherNotes` (`app/blueprints/[slug]/page.tsx:246`) | diagnostic codes in `lib/core/diagnostics.ts:11` | `finding` means a (marker, node) row in the security result; `note` means a non-error diagnostic. Both read as the same word in UI copy |
| Node role | "type ·" chip, node "kind" in the schematic | `NodeCard.type` (an ontology `node-type` term id) **vs** `AgentNodeKind` (`lib/types.ts:102`, a 12-member presentation union) **vs** `AttractorNodeKind` | cards write `type: agent`; graph seeds carry `kind` | Three different vocabularies for one node's role, and `shape` belongs with `type` rather than with `rankdir`: spec §2.8 makes it the **handler selector**, not Graphviz layout, so the emitted shape is what decides which Attractor handler runs a node. **The site filed it as decoration until 2026-09-04.** The three vocabularies are **13 ontology node types** (10 concrete plus 3 abstract categories, since `shell-tool` landed 2026-09-04 beside the `orchestration` branch of 2026-08-30), 12 drawing kinds, and Attractor's own shapes. The three vocabularies no longer line up: `lib/graph-seed.ts`'s `KIND_BY_TYPE` has no row for `parallel`, `parallel.fan-in` or `manager-loop`, so a card declaring one draws as `executor` (§11). **That gap does NOT widen for `shell-tool`**: `agentNodeKind` (`lib/graph-seed.ts:135-149`) tries the direct row, then walks `ontology.ancestors`, so a `shell-tool` card resolves through its `broader: "tool"` and draws as the tool kind with no row of its own |
| Content kind | "Blueprint", "Node", "Ontology" (upload selector); "Blueprints", "Cards", "Ontology" (nav) | `ContentKind = "blueprint" \| "node" \| "ontology"`, `KIND_NOUN` maps `node` → `"node card"` | `Save.kind` uses `"blueprint" \| "node card" \| "vocabulary term"` | Two enums for one dimension, with three of six members spelled differently |
| Prohibition | "What it refuses" (rail), with two headed groups: "cannot receive · the resolver checks every edge against these" and "will not · the card's own undertaking, which no check reads" | `NodeCard.cannot` **and** `NodeCard.willNot`, `ProhibitionView` (whose `term` is now required), `bundle/prohibition-violated`, `card/prohibition-misfiled` | `cannot:` holds `data-type` term ids only; `will_not:` holds the author's prose | **RESOLVED 2026-08-30.** One array carrying two semantics became two fields carrying one each, which is what lets a reader tell a checked promise from an unchecked one without running anything. `enforcedCount` no longer exists: it existed only because of the conflation |
| Human involvement | "A person acts here", "human gate", "human-input" | `requiresHuman(ontology, type)` (`lib/core/ontology/resolve.ts`), `HumanReason` (now a one-member union), ontology `human-in-the-loop` category, `AgentNodeKind` members `gate` and `human-input`, `AutonomyContribution.requiresHuman` (computed) | no wire key — the card's `type` is the whole of it | **RESOLVED 2026-08-30.** There is exactly one route now: the card's `type`. `NodeCard.requiresHuman` and the wire key `requires_human` are gone, so `type: agent` with a person claimed at the node is inexpressible in TypeScript as well as in YAML. Every reader calls the same function (`card/validate.ts`, `analysis/autonomy.ts`, `app/nodes/page.tsx`, `app/nodes/[...id]/page.tsx`, `components/profile/load.ts`, `lib/server/search/cards.ts`) |
| Download counter | "↓ N downloads" on both blueprint and card pages | `Blueprint.downloads`, `downloadsFor(cardId)` | `COMMUNITY[slug].downloads` (per slug), `NODE_DOWNLOADS[id]` (per card **id**, not per ref) | One label, two grains: per bundle vs per card id across all versions |
