# DECISIONS — the decision ledger

> A decision that is not `OWNER-STATED` carries no authority. No agent may act on a
> `PENDING-OWNER-REVIEW` row, cite it as established, or use it to justify a change.

Built from `docs/audit/CONTEXT_SURFACE.md` and `docs/audit/CLAIMS_RAW.md`, with every
`Verifiable in code` value checked against the working tree at `27de537`, not inferred from
the documents. Conflicts found while building it are in `docs/audit/CONTEXT_CONFLICTS.md`.

## The provenance bar used here, stated so it can be argued with

A row is `OWNER-STATED` when one of three things exists:

1. **A quoting construction.** The source prints the owner's own words — `The author,
   2026-08-04:` followed by a quotation, `The author's note: "…"`, or a quoted instruction
   recorded in a code comment. An agent restating such a decision in its own words is still
   `OWNER-STATED`, because the original is traceable.
2. **A commit that records the change as the owner's request.** `Author-requested batch of
   six changes` (`feaa9bc`), `The author's instruction, 2026-08-11` (`62fee59`).
3. **An approval marker in a `docs/superpowers/` spec** — `Status: approved` in the header,
   or `Approved` against a numbered item. **The owner confirmed on 2026-08-12 that these are
   his approvals** (answer Q1 below), with one caveat recorded verbatim: *"I used the plugin
   superpowers but I used at the very beginning of the project. It could have been diverged
   a little wrt the current state of the project."* So an approval marker establishes that
   the decision was made; it does not establish that it still holds. Four rows moved to
   `OWNER-STATED` on this answer — D-28, D-32, D-35, D-52 — and two of them (D-32, D-35) are
   `CONTRADICTED` by the code, which is exactly the divergence the caveat predicts.

Everything else that *reports* owner approval without reproducing it stays `UNATTRIBUTED`:

- **`the author asked X` with no quotation.** Dozens of code comments and several documents
  use it. It may be entirely accurate; nothing in the repository lets a reader check it.
  Q1's answer raises the credibility of this class without establishing any single instance.
- **`The following interpretation was checked with the author during the review`
  (`sol_feedback.md:35`).** The only witness that the author confirmed the review's
  premises is the agent that wrote the review. Every `Factual (owner-confirmed)` row in
  `CLAIMS_RAW.md` rests on that one sentence, and none of them reproduces an owner word.

## The `files/` documents carry no authority, and 649 code comments cite them

`files/darkprint-design.md`, `files/darkprint-onboarding-positioning.md` and
`files/darkprint-ontology-v0.1.md` — "doc 1", "doc 2", "doc 3" — are named as *the
specifications* by `architecture/README.md:24-26` and `PROJECT.md:316-318`, and are cited by
section from **649 comments across 138 files** under `app/`, `components/`, `lib/`,
`scripts/` and `skills/`.

The owner answered on 2026-08-12 that **an agent wrote them**, that he did not write them
himself (Q2), and that he does not hold them as decisions he adopted (Q3, *"not really"*).
`CONTEXT_SURFACE.md` had read their Italian as a signal he wrote them; that reading is
falsified. Every rule tracing to them is therefore `AGENT-PROPOSED` — twenty rows below,
including D-12, the rule that nothing may be described as working that is not built, which
holds up the site's entire disclosure architecture.

So the chain is: an agent wrote three documents, later agents cited them 649 times as
"the specifications", and `architecture/README.md:24-26` and `PROJECT.md:316-318` told every
subsequent reader that is what they are. That is the mechanism this audit was asked to find,
in its clearest instance.

This does not make those rules wrong. It makes them unowned, and it means the phrase "doc 2
§0.4" in a code comment is not a citation of anything the owner decided.

## The skill exists on purpose; its contents are unread

The owner confirmed he created and shipped `skills/darkprint/` deliberately (Q4), adding
*"But I just left there as a test. I need to make it concrete useful"*, and that he has not
reviewed its contents (Q5). So D-86 is `OWNER-STATED` and D-60, D-61, D-62 and D-64 —
everything the skill instructs — are `AGENT-PROPOSED` and unread.

## The 2026-08-30 wave, and how to check its attribution

D-91 to D-104 record one wave of work the owner instructed on **2026-08-30** with the words
*"use ultracode mode to implement all the things discussed"*, given after he reviewed and
agreed a decision register covering the items. That quotation and its date sit in every one
of those rows' Status cells, which is what makes the authority auditable rather than
asserted.

**One thing about the chain, stated so nobody has to reconstruct it.** The session that wrote
these rows did not hear the instruction directly; it received it from the session that
orchestrated the wave, quoted and dated. Under this document's own provenance bar that is a
quoting construction and therefore `OWNER-STATED` — "an agent restating such a decision in its
own words is still `OWNER-STATED`, because the original is traceable" — but the trace runs
through one relay, and a reader checking these rows should check them against the
conversation, not against this file. Where a *judgement* was made by an implementing session
rather than settled in the register, it is filed separately and marked
`PENDING-OWNER-REVIEW`: D-105 (the ontology version was not bumped), D-106 (`report` ships
against a withdrawal record) and D-107 (a fifth MCP tool against a closed set of four).

**Two id shapes, and they are different things.** A bare two-segment id — `D-101` — belongs to
this ledger. A three-segment id — `D-100-04`, `D-260-24`, `D-270-01` — is a backend task's
ruling and lives in `backend.md`. The numbering passed 100 in this wave, so the distinction
now matters when reading prose.

---

## 1 · What the product is

| ID | Decision in one sentence | Source path and line | Provenance | Verifiable in code | Status |
|---|---|---|---|---|---|
| D-01 | DarkPrint is a registry of blueprints — graphs of automations you hand to an agent, drop into existing code, or compose with other blueprints — and blueprints, not the dark factory, are the headline. | `docs/superpowers/specs/2026-08-04-ia-redesign.md:12-15` (owner quoted); `PROJECT.md:38-43`; `README.md:3,6-7`; `IMPLEMENTATION_PLAN.md:11-12,43`; `sol_feedback.md:37` | OWNER-STATED | IMPLEMENTED — `app/layout.tsx:38-46`; `components/site/SiteHeader.tsx:52-56` | CONFIRMED |
| D-02 | "Dark factory" is a computed property of one graph shape, never the site's category, its summit, or a goal. | `docs/superpowers/specs/2026-08-04-ia-redesign.md:19-20`; `PROJECT.md:31-35`; `README.md:29`; `sol_feedback.md:152-154`; `files/darkprint-ontology-v0.1.md:35` | OWNER-STATED (covered by the same 2026-08-04 quotation) | IMPLEMENTED — `lib/core/analysis/autonomy.ts:317`; the essay is Learn stop 06, `components/spec/sequence.ts:415-420` | CONFIRMED |
| D-03 | DarkPrint runs nothing: it hands out files and reads them back, and both composing and running happen on the user's own machine with their own tool. | `docs/superpowers/specs/2026-08-04-ia-redesign.md:269-271` (owner quoted); `PROJECT.md:23-24`; `README.md:9-10,23-24`; `files/darkprint-design.md:29`; `sol_feedback.md:38-39` | OWNER-STATED | IMPLEMENTED — no runner anywhere in `lib/`; `/upload` resolves in the browser tab | CONFIRMED |
| D-04 | A blueprint pins the route a harness takes, so two runs differ only where the blueprint changed; a prompt hands the harness a goal and lets it invent the route. | `docs/superpowers/specs/2026-08-10-landing-reproducibility-beat-design.md:12-17,21-22` (owner quoted); `docs/superpowers/plans/2026-08-10-landing-reproducibility-beat.md:115-117` | OWNER-STATED | IMPLEMENTED — `components/home/SectionSameRun.tsx`, landing beat 2 | CONFIRMED |
| D-05 | The registry will be reachable from an agent through an MCP server that RAG-searches it for the best-fitting blueprint. | `docs/superpowers/specs/2026-08-06-build-restructure-design.md:16-20` (owner quoted) | OWNER-STATED | IMPLEMENTED server-side — T220/T300 shipped `lib/server/mcp` + the four `/api/mcp/**` routes with hybrid lexical/semantic search, and T280 flipped `/mcp`'s copy to the live contract; the npm transport (`npx -y darkprint mcp`) remains unpublished, and the page says so | CONFIRMED |
| D-06 | The three primitives are the blueprint (a graph), the node (a versioned card) and the ontology (the vocabulary both are written against), and those three are the whole model. | `PROJECT.md:27-29`; `README.md:14-17`; `files/darkprint-design.md:39`; `architecture/README.md:3-4` | UNATTRIBUTED | IMPLEMENTED in the MODEL, and its navigational witness expired on 2026-09-06 — `lib/core/dot`, `lib/core/card`, `lib/core/ontology` are unchanged and are the three primitives, but `components/site/SiteHeader.tsx` carries **two** Browse rows now rather than three. D-153 folded the vocabulary browser into `/spec/ontology` and the Browse row went with the route. **The primitive did not stop being a primitive; it stopped having a browse shelf of its own**, which is a claim about the site's shape and not about the model, and the two should not be read off one another again. **AND ON 2026-09-06 IT STOPPED HAVING A SPEC PAGE OF ITS OWN TOO** (D-156): `/spec/ontology` is deleted and the vocabulary is documented on `/spec/card`, beside the fields that consume it. The model is again unmoved — `lib/core/dot`, `lib/core/card` and `lib/core/ontology` are the three primitives and none of them was touched — and the ruling's own evidence is why: only 13 of the 54 core terms overlap Attractor at all, so the vocabulary is a layer of its own even with no page of its own | PENDING-OWNER-REVIEW |
| D-07 | The central product claim is that the useful reusable artefact is the *shape of the work*, not a prompt and not a model. | `PROJECT.md:16-18`; `sol_feedback.md:48-51`; `files/darkprint-onboarding-positioning.md:150` | UNATTRIBUTED (`sol_feedback.md:35`'s confirmation marker is the reviewing agent's own report) | UNVERIFIABLE (a thesis, not a behaviour) | PENDING-OWNER-REVIEW |
| D-08 | DarkPrint promises a reproducible, checkable specification and never a deterministic LLM result. | `sol_feedback.md:71-77`; `IMPLEMENTATION_PLAN.md:13-14`; `sol_feedback.md:420-421` | UNATTRIBUTED (same marker as D-07; the owner's own quotation at D-04 argues reproducibility, not determinism) | IMPLEMENTED — `app/layout.tsx:42` says "statically checks"; the word `deterministic` appears on no user-facing surface (only in code comments) | PENDING-OWNER-REVIEW |
| D-09 | The site's headline claim is "Autonomy you can read as a graph." | commit `feaa9bc` ("Author-requested batch of six changes"); `README.md:4` | OWNER-STATED | CONTRADICTED — retired; `app/layout.tsx:38-46` now leads with "reusable blueprints for agent workflows", and the phrase survives only at `README.md:4` | CONFIRMED (as a decision; superseded in code — see D-10) |
| D-10 | The primary promise is about reusable, inspectable agent workflows, not autonomy. | `sol_feedback.md:179-185`; `IMPLEMENTATION_PLAN.md:11-13` | AGENT-PROPOSED (`sol_feedback.md:182-185` disclaims its own wording as "Possible direction, not final copy") | IMPLEMENTED — `app/layout.tsx:52-54` carries that non-final wording almost verbatim | PENDING-OWNER-REVIEW |
| D-11 | The validated marketing claim is "Specifications go in. Software comes out." | `files/darkprint-onboarding-positioning.md:37` ("già validato nelle note") | AGENT-PROPOSED (Q2/Q3: doc 2, neither owner-written nor owner-adopted; the "già validato" marker has no note behind it) | CONTRADICTED — removed by commit `feaa9bc`, which records the owner replacing it; the string appears nowhere under `app/` or `components/` | PENDING-OWNER-REVIEW |

## 2 · The honesty rules

| ID | Decision in one sentence | Source path and line | Provenance | Verifiable in code | Status |
|---|---|---|---|---|---|
| D-12 | Nothing may be described as working that is not built, and the limitation is stated exactly where the capability is first suggested. | doc 2 §0.4, cited at `docs/superpowers/plans/2026-07-29-content-cli.md:20`, `docs/superpowers/specs/2026-07-29-content-cli-design.md:31-35,82-84`, `docs/superpowers/plans/2026-08-06-build-workspace.md:18`; `PROJECT.md:131-132` | AGENT-PROPOSED (Q2/Q3: doc 2 §0.4). The site's most load-bearing rule originates in a document the owner did not write and does not hold as adopted | IMPLEMENTED — `components/site/honesty.test.ts` (19 pinned claims), `components/ui/ComingSoonBadge.tsx`, `app/settings/page.tsx:33-46` | PENDING-OWNER-REVIEW |
| D-13 | Community figures are seeded, and every one carries a `◐ seeded` marker in glyph *and* word wherever it renders. | `PROJECT.md:132-134`; `README.md:46`; `lib/data/account.ts:11-14` | UNATTRIBUTED | IMPLEMENTED — `lib/data/community.ts`, `lib/data/account.ts`, `components/ui/community-support.test.ts:57`, `components/ui/autonomy-surfaces.test.ts:217-229` | PENDING-OWNER-REVIEW |
| D-14 | A claim tagged `open` in the honesty ledger fails the build if it moves behind a disclosure; cutting for pace is how limit statements disappear. | `PROJECT.md:197-207`; `docs/superpowers/specs/2026-07-31-content-reorg-design.md:60-64` | UNATTRIBUTED | IMPLEMENTED — `components/site/honesty.test.ts`; `components/ui/visible-text.ts:38,48` (`plainText` / `openText`) | PENDING-OWNER-REVIEW |
| D-15 | ~~Amber has exactly two reserved jobs — "not built yet" and "this box leaves the page" — and no third thing may take it.~~ **SUPERSEDED IN ITS THIRD CLAUSE BY D-151, 2026-09-06.** The two reserved jobs are UNREPEALED and still mean what they meant; what falls is the "no third thing" half, and it falls to the owner rather than to an agent. | `docs/superpowers/plans/2026-08-06-build-workspace.md:18,252`; `app/globals.css:74-103` | UNATTRIBUTED | **WAS IMPLEMENTED, and the shape of its implementation is what D-151 overturns.** `app/globals.css` reserved amber and added `--color-copper-line` rather than reuse it, and that token existed for no other reason. On 2026-09-06 the owner made the card register amber, so the two moved copper tokens are deprecated in place and the amber block states three jobs: `ComingSoonBadge` ("not built yet"), `.route-box`/`.route-label` ("this box leaves the page"), and the card register ("you are on a card"). **This row was spent ONCE before it fell**, on 2026-09-05, to overrule the same owner asking for the same thing, and D-151 carries that history — read the two together, in that order | PENDING-OWNER-REVIEW as to the original. **The supersession is CONFIRMED, 2026-09-06 (D-151)** |
| D-151 | **AMBER IS THE CARD REGISTER, cyan is the blueprint register, and the pair is a NAVIGATIONAL signal rather than decoration.** The owner's words, verbatim: *"the amber should be the dominant color on the cards sections. So that an user in a glance can know wheter they are on a blueprint or in a card"*. This is a THIRD sanctioned job for the token, added alongside its two claim jobs rather than replacing them: `ComingSoonBadge` still means "not built yet" and `.route-box`/`.route-label` still mean "this box leaves the page". **It reverses D-15's "no third thing may take it" clause and it reverses a ruling this ledger's own file records winning.** | the owner's instruction, 2026-09-06 (given in conversation, quoted above in full); `app/globals.css`'s copper pole and accent block, which carry both sides of the argument beside the tokens; `docs/ARCHITECTURE.md` §9.1 and §9.4 rule 5 | **OWNER-STATED, 2026-09-06.** The instruction was given in this conversation, in the owner's own words, after the same question had been put to them with its costs stated, which is why this row is `CONFIRMED` rather than `PENDING-OWNER-REVIEW` | **IMPLEMENTED.** `--color-copper-line` and `--color-copper-ink` WERE the card register and are deprecated in place; `--color-copper` and `--color-copper-deep` stay, because they are the card sheet's PAPER (oklch L 0.26 and L 0.35) and a ground is not something a reader reads a claim off. **The two moved tokens are still DECLARED on purpose**: Tailwind emits a `@theme` colour's utilities only while it can see one used, so deleting either today would silently stop about a dozen files' classes from existing, with no error, and drop those words to whatever they inherit. The pass that converts the last consumer takes the tokens with it. **Contrast was RE-MEASURED across the hue change and not carried**: on the card sheet (`SHEET_REGISTER.copper`, #24140c) and the listing's ground (`bg-surface-2/40` on the void, #090b14), `--color-amber` reads 9.7:1 and 10.8:1, `--color-amber-bright` 11.8:1 and 13.1:1, `--color-amber-ink` 13.4:1 and 14.8:1; at reduced opacity amber's TEXT floor is 70% and 60% is a non-text boundary, where copper's text floor was 80%. Different opacities on the same grounds, which is why inheriting the old numbers would have been wrong | **CONFIRMED, 2026-09-06.** **READ THIS WITH D-15, WHICH IS THE DECISION IT REVERSES, AND THEY POINT AT EACH OTHER.** The history matters and is the reason this row is long. `app/globals.css` reserved amber for exactly two claims and argued at length that the node card must never take it — "the most literally-built thing on the site", "the one lie this figure cannot afford". **That reservation was SPENT ONCE, on 2026-09-05, against this same owner asking for this same thing**: they asked for an amber accent on the card's download control, an implementer overruled them citing the token contract, and copper shipped. `components/bundle/bundle-header-actions.test.ts` records the mismatch in its own words, that the copper was the integrator's call rather than the owner's literal word. **The owner has now ruled the other way, deliberately, on a ground the first pass never weighed**: every sentence of the old argument is about what amber CLAIMS of the thing it touches, and the ruling is about what a reader needs BEFORE they have read anything at all. A reader who has not started reading cannot yet be misled by a claim, and no amount of "the card is the most literally-built thing on the site" tells them which kind of page they are standing on. **The tree was already on the ruling's side on one route**, which is what makes this a resolution rather than a whim: `components/nodes/NodeCardSummary.tsx` and `components/nodes/VersionHistory.tsx` had been painting the card register amber on `/nodes` for some time while the card DETAIL page refused it, so the site disagreed with itself. **The argument that lost is PRESERVED, in `app/globals.css` verbatim and in `docs/ARCHITECTURE.md` §9.1 in summary, and it does not get to win again.** The cost is priced rather than footnoted: amber now carries one PLACE meaning and two CLAIM meanings, they co-occur on a card page, hue no longer separates them and form does — §11.0 Q52 |
| D-16 | The homepage's beat-2 qualifier "illustrative: DarkPrint does not run your graph" comes off the page, together with the two guards that pinned it. | commit `62fee59` ("The author's instruction, 2026-08-11, given after being shown the two tests that pinned the line"); `components/home/SectionSameRun.tsx:138`; `components/site/honesty.test.ts:405-430` | OWNER-STATED | IMPLEMENTED — the string is gone from all three files | CONFIRMED |
| D-17 | When an ask is phrased in account or hosting terms this site does not have, say so and ship the honest version of the same interaction rather than building the account system or quietly shrinking the ask. | `PROJECT.md:326-338`; `architecture/website.md:83-85` | UNATTRIBUTED | IMPLEMENTED — `components/blueprint/ForkAction.tsx`; `app/settings/page.tsx:33-52` (every control `disabled`, the reason stated above the first panel) | PENDING-OWNER-REVIEW |
| D-18 | Reported values are never called measurements; the interface says *reported*, not *measured*, until DarkPrint can verify the run. | `files/darkprint-design.md:268-274`; `sol_feedback.md:422-423` | UNATTRIBUTED | IMPLEMENTED — the separation was `components/blueprint/EvidenceLayers.tsx:10-45`, deleted 2026-09-04 with the blueprint page's scoring reading; the rule survives in `lib/content/view.ts`'s `MetricSource` and in the copy `honesty.test.ts` pins, which is where it is now checkable; `components/site/honesty.test.ts` pins "nothing on this site measures a run" as `open` | PENDING-OWNER-REVIEW |
| D-136 | **The header's "a snapshot over HTTP, not a clone" sentence comes off the blueprint band**, together with the control it qualified. | the owner's instruction, 2026-09-05; `components/bundle/BundleHeader.tsx`; `components/bundle/CodeMenu.tsx:108`; `components/blueprint/CloneMenu.tsx:197` | OWNER-STATED, 2026-09-05 | IMPLEMENTED, and the claim is not lost, which is the only ground on which a disclaimer may be deleted. `Get the folder` left the header with the trio (D-132), so the sentence was qualifying a control that is no longer in the band; the download is the file list's `Code` dropdown, and `CodeMenu.tsx:108` states the snapshot limit there, beside the command it is about. `CloneMenu.tsx:197` states it on the card page's own menu. `components/site/honesty.test.ts:507-512` pins *"a snapshot, not a clone"* `where: "present"` over `CloneMenu`'s own markup rather than over the header, which is why the removal reddened nothing — and is also the shape §11.0 Q29 is about | CONFIRMED (the deletion). **PENDING-OWNER-REVIEW as to the band's `note` fallback**, which went in the same edit: *"nothing to fetch: this bundle is not published"* existed to explain the disabled `Get the folder` beside it, and dropping it is the agent's reading that a question the header no longer raises needs no answer |

## 3 · The engine and the file format

| ID | Decision in one sentence | Source path and line | Provenance | Verifiable in code | Status |
|---|---|---|---|---|---|
| D-19 | `lib/core/**` is isomorphic — no `node:*`, no `Buffer`, no `Date.now()`, no `Math.random()` — so the same engine runs in the browser on `/upload`. | `architecture/engine.md:5-7`; `architecture/README.md:51-54`; `PROJECT.md:111`; `README.md:51-52` | UNATTRIBUTED | IMPLEMENTED — 30 modules under `lib/core`; `components/upload/UploadFlow.tsx` runs `loadBundle` client-side | PENDING-OWNER-REVIEW |
| D-20 | A published card is never edited in place: content changed means a new version, the old file archived beside it, content-addressed by a `sha256:` digest. | `files/darkprint-design.md:132,153,159`; `skills/darkprint/references/card-schema.md:129`; `architecture/engine.md:161-163`; `architecture/node-card.md:136-138` | AGENT-PROPOSED (Q2/Q3: doc 1 §4) | IMPLEMENTED — `lib/core/version/bump.ts`, `lib/core/hash/`, `bundle/digest-mismatch` | PENDING-OWNER-REVIEW |
| D-21 | Every threshold and weight lives in one configuration file and nowhere else. | `files/darkprint-design.md:385`; `files/darkprint-ontology-v0.1.md:67`; `architecture/engine.md:169-171`; `README.md:64-65` | AGENT-PROPOSED (Q2/Q3: doc 1 §11) | IMPLEMENTED — `lib/core/config.ts:123` `DARKPRINT_CONFIG`, deep-frozen | PENDING-OWNER-REVIEW |
| D-22 | Isolation is a property of the graph and is checked two independent ways: a card's declared `cannot` (an error that refuses the bundle) and the topological/similarity criteria-leak walk. **NARROWED 2026-08-30 by D-91, not reversed**: `cannot` is typed now — `data-type` term ids only — so the check no longer has to decide per entry whether an entry is enforceable at all, and the prose half it used to carry moved to `will_not`, which no check reads. | `architecture/engine.md:92-129`; `architecture/node-card.md:120-126`; `PROJECT.md:74-80`; `skills/darkprint/references/ontology.md:109-134`; `files/darkprint-onboarding-positioning.md:143,221` | AGENT-PROPOSED (Q2/Q3: doc 2 §3, doc 3 §4.1) | IMPLEMENTED — `lib/core/bundle/resolve.ts` (`bundle/prohibition-violated`), `lib/core/analysis/` criteria-leak | PENDING-OWNER-REVIEW |
| D-23 | A generator's `spec` paraphrasing the criteria producer's is a leak, warned above 0.35 3-gram Jaccard. | `architecture/engine.md:100-102`; `skills/darkprint/references/writing-cards.md:41`; `skills/darkprint/references/ontology.md:122` | UNATTRIBUTED | IMPLEMENTED — `lib/core/config.ts:151-155` `similarityThreshold: 0.35` | PENDING-OWNER-REVIEW |
| D-24 | The five phases are a closed list, never namespaced, and phase coverage is descriptive — nothing scores off it and no surface may draw an empty list as a defect. | `files/darkprint-ontology-v0.1.md:25,35,140`; `architecture/ontology.md:31-33`; `skills/darkprint/references/card-schema.md:186`; `architecture/engine.md:136-137` | AGENT-PROPOSED (Q2/Q3: doc 3 §2) — but see D-25, where the owner did rule on what the five phases mean | IMPLEMENTED — `lib/core/ontology/core.ts`; `card/namespaced-phase` | PENDING-OWNER-REVIEW |
| D-25 | The five phases the card field draws from are "the expected high level phases a dark factory should have, but do not necessarily have to stick to nodes". | `skills/darkprint/references/card-schema.md:180` (quoted in a JSDoc block as "the author's ruling") | OWNER-STATED | IMPLEMENTED — `phases` is optional and repeatable on `NodeCard` | CONFIRMED |
| D-26 | A local ontology term must be namespaced and must declare `broader`, or the analysis ignores it silently, which is the worst outcome available. | `files/darkprint-ontology-v0.1.md:142-143`; `architecture/ontology.md:107-109`; `skills/darkprint/references/ontology.md:219-223` | AGENT-PROPOSED (Q2/Q3: doc 3 §7) | IMPLEMENTED — `card/unknown-term`; `partitionTerms()` in `lib/core/ontology/resolve.ts` | PENDING-OWNER-REVIEW |
| D-27 | ~~The exported `factory.dot` is runnable by Attractor as it stands~~, **RESTATED 2026-08-30**: the emitted DOT is accepted by Attractor's grammar and by its lint rules, and it sets none of the reserved attributes `ATTRACTOR_UNEXPRESSED_ATTRIBUTES` names, so every one of those takes the runner's own default. The claim is about the **format**, never about the pipeline, and the artefact says so in its own header. DarkPrint's schema must stay readable by existing Attractor tooling. **The second half of the restatement became checkable on 2026-09-04 and is now false in one named case.** `attractor/condition-syntax` is the first rule that can test *accepted by its lint rules*, and `emit.ts` copies an edge's `condition` verbatim into the emitted file, so a blueprint carrying a guard §10.2 refuses exports a DOT Attractor's own lint rejects. The archive is clean today — all 24 of its guards parse — and the limit is a property of the claim rather than of the archive. | `PROJECT.md:85-88`; `files/darkprint-design.md:25`; `architecture/blueprint.md:98-103`; `skills/darkprint/references/dot-and-attractor.md` | AGENT-PROPOSED (Q2/Q3: doc 1 §0 constraint 1) as to the original wording; the restatement is OWNER-STATED, 2026-08-30 — *"use ultracode mode to implement all the things discussed"*, given after the owner reviewed and agreed a decision register covering these items | IMPLEMENTED **and now tested**, which it was not before — `lib/core/attractor/emit.ts`, `tests/attractor-round-trip.test.ts` (D-102), `lib/server/export/build.ts:276-281`. **Its old evidence was stale**: the row cited `public/bundles/*/factory.dot`, a file no published folder has carried since 2026-08-25 | CONFIRMED as restated (see D-99, D-100, D-102). The over-claim "as it stands" is what 2026-08-30 corrected, in the code and in every surface that repeated it |
| D-28 | A blueprint is a dark factory only when every node is unattended **and** all five phases are covered. | `docs/superpowers/specs/2026-08-04-ia-redesign.md:432-439` ("Approved 2026-08-04", no attribution) | OWNER-STATED (Q1: a bare `Approved` in a superpowers spec is the owner's approval) | IMPLEMENTED — `lib/core/analysis/autonomy.ts:317` `autonomousNodes === totalNodes && bp.phaseCoverage.missing.length === 0` | CONFIRMED |
| D-29 | The security weights are provisional starting values to be calibrated against real data; four of nine blueprints flooring at 1 is an open item, not a result. | `PROJECT.md:244-251`; `architecture/engine.md:84-86`; `architecture/ontology.md:144-146`; `files/darkprint-ontology-v0.1.md:67` | UNATTRIBUTED | IMPLEMENTED as an unresolved state — `lib/core/config.ts:134-149` still holds the doc 3 starting weights | PENDING-OWNER-REVIEW |
| D-30 | Changing any weight is a PATCH of the ontology version because it re-scores every blueprint that already exists. | `files/darkprint-ontology-v0.1.md:158`; `PROJECT.md:251`; `architecture/README.md:48-50`; `architecture/ontology.md:128-130` | AGENT-PROPOSED (Q2/Q3: doc 3 §8) | UNVERIFIABLE **and, since D-93, UNMECHANISED**: there is no version-publish path at all, so the rule is a discipline on whoever edits `DARKPRINT_CONFIG.security.weights` and nothing can enforce or record it. `DARKPRINT_CONFIG.security.weights` was never versioned in the first place, which is why a resolvable vocabulary version could not have made a score reproducible even in principle. `/spec`'s `ScoringModel` was strengthened rather than left to imply otherwise: it now says in the open that two scores carrying the same version were not necessarily computed under the same numbers, and that the site has no way to tell you which. **UNSTATEABLE since 2026-09-05 (D-131)**: there is no ontology version for a weight change to be a PATCH *of*, so the rule has lost its subject as well as its mechanism. What it was reaching for survives as `lib/core/config.ts:117`'s own note — the weights are the calibration and nothing versions them | PENDING-OWNER-REVIEW |
| D-91 | A card refuses in **two** fields, not one: `cannot` holds `data-type` term ids the resolver checks every edge against, and `will_not` holds the author's own prose undertakings, which no check reads. | `lib/core/card/schema.ts` (a ~30-line block arguing the names); `lib/core/card/validate.ts`; `lib/core/bundle/resolve.ts` | OWNER-STATED, 2026-08-30 — *"use ultracode mode to implement all the things discussed"*, after the owner reviewed and agreed a decision register | IMPLEMENTED — `NodeCard.cannot` / `NodeCard.willNot`, both required, both defaulting to `[]`, deliberately symmetric so the unchecked half is not modelled as a lesser citizen. A sentence under `cannot` is now `card/unknown-term` (error); a `will_not` entry that resolves to a `data-type` is `card/prohibition-misfiled` (**warning** — the entry still says what it says, and the vocabulary can grow a term that would otherwise turn a published card unloadable). `checkProhibitions` reads `cannot` and states in its docblock that it never reads `willNot`. `enforcedCount` is **deleted**; it existed only because of the conflation. The names were argued in writing and `unenforced` / `advisory` / `soft_cannot` refused as grading the promise: `cannot` is an incapacity the engine imposes and holds you to, `will not` is an undertaking the author gives. Cost accepted deliberately: card identity moved, so every card digest, every bundle digest and every byte under `public/bundles/**` moved with it | CONFIRMED — the owner instructed this wave on **2026-08-30** with *"use ultracode mode to implement all the things discussed"*, after reviewing and agreeing a decision register that covered it |
| D-92 | A card's `type` is the **whole** answer to whether a person acts at a node; the `requires_human` field is withdrawn from the schema entirely rather than derived onto the model. | `lib/core/ontology/resolve.ts` (`requiresHuman`, `humanCitation`, the one derivation); `lib/core/card/schema.ts`; `lib/core/analysis/autonomy.ts` | OWNER-STATED, 2026-08-30 — *"use ultracode mode to implement all the things discussed"*, after the owner reviewed and agreed a decision register | IMPLEMENTED — `NodeCard.requiresHuman` is gone from the interface, so `type: agent` with a person claimed at the node is inexpressible in TypeScript as well as in YAML. **Two duplicate implementations existed and both are gone** (`validate.ts`'s `isHumanType` resolved the deprecation then asked `isA`; `autonomy.ts`'s `humanCitation` asked `isA` then followed the redirect — same boolean, different traversal, separately maintained), and a cell now asserts the boolean and its citation agree on every shipped node type. `card/human-type-inconsistent` is deleted; `card/retired-field` (warning) replaces the generic unknown-key advice a withdrawn key would otherwise get, and quotes the card's own `type` back with different advice per answer. **The reachable defect was always the other direction**: `type: tool` + `requires_human: true` was legal, nothing validated it, and it was live in fixtures and mock data. **One behaviour change falls out and is recorded rather than hidden**: `type: "human-in-the-loop"` used to be refused *by accident* (the rule asked `isA(type, category)`, subsumption is reflexive, and the category answered its own question) and now loads clean, as `evaluative` always did | CONFIRMED — the owner instructed this wave on **2026-08-30** with *"use ultracode mode to implement all the things discussed"*, after reviewing and agreeing a decision register that covered it |
| D-110 | The capability doc 3 §3 allowed and D-92 removed — a person staffing a node whose `type` says nothing about people — is **not** restored; if somebody acts, the graph must show them. | `lib/core/analysis/autonomy.ts`; `architecture/ontology.md:48`; `architecture/node-card.md:117`; `files/darkprint-ontology-v0.1.md:61` | OWNER-STATED, 2026-08-30 — the owner was walked through every open item one at a time and ruled on each, on this row specifically | IMPLEMENTED — nothing was added back. **The cost was priced before the ruling, not after**: doc 3 §3 deliberately permitted `requires_human: true` on a type that says nothing about people, `autonomy.ts` carried an arm for it, and fixtures exercised it (`tests/server/t200/world.ts`). Losing it means a `tool` node somebody has to press the button on can no longer say so, and the autonomy figure for such a blueprint reads higher than the truth. The owner's ground for accepting that: a person recorded in a card field is invisible on the schematic, which is the same defect as a criteria leak carried in prose, and this repository already holds that isolation is a property of the topology. Staffing a node therefore means putting a human node in the graph, where the reader and the analyzer both see it. **No card in `content/cards/` ever used the removed direction** — all 57 were checked, two carry a `human-gate` type and both declared `true` — so the archive needed no migration and no blueprint's score moved. **Three source design documents still state the opposite rule** and are deliberately NOT edited: they are the record of what was specified, and rewriting a spec to agree with a later ruling destroys the evidence that the ruling changed something. The divergence is carried in `docs/ARCHITECTURE.md` §11, the same way `docs/audit/CLAIMS_RAW.md`'s stale rows are | CONFIRMED — owner ruling, 2026-08-30, after the lost capability and the overstated-autonomy risk were put to them alongside restoring an explicit staffing override and adding a prose-heuristic warning |
| D-93 | The vocabulary-version registry is removed on a supersession argument: a release stores its whole scorecard at publish time, so a published score is a fact written once and never recomputed against a historical vocabulary, and the version it *was* computed under is on the score. | `lib/server/ontology/view.ts`; `lib/core/ontology/core.ts`; `lib/db/schema.ts` | OWNER-STATED, 2026-08-30 — *"use ultracode mode to implement all the things discussed"*, after the owner reviewed and agreed a decision register | IMPLEMENTED — 19 files deleted (`lib/server/ontology/store, bump, errors, digest, input, validate, well-formed, vocabulary`, three colocated suites, the whole of `tests/server/t030/**`), six error classes with them, taking `tests/error-hygiene.test.ts` **53 → 47**, the first decrease it has ever recorded. `openView` is `openView(extensions?)`: synchronous, no `Db`, no version. `NodeCard.ontologyVersion` and `BundleManifest.ontologyVersion` are gone. **Two corroborating facts, placed where they are load-bearing**: `deprecated` retires a term inside the one living vocabulary and leaves its id in place, and `DARKPRINT_CONFIG.security.weights` was never versioned at all, so a resolvable version could not have made a score reproducible even in principle. **One departure from the letter of the instruction, flagged rather than absorbed**: `release.scored_ontology_version_id` is a uuid FK into the table whose only writer was deleted, so publish cannot write it and `registry/scores.ts` reads the version off `AutonomyResult.ontologyVersion` inside `release.autonomy` instead — same value, one indirection shorter, one query fewer. The column and both tables stay declared and unread; dropping them is a separate migration decision | CONFIRMED — the owner instructed this wave on **2026-08-30** with *"use ultracode mode to implement all the things discussed"*, after reviewing and agreeing a decision register that covered it, with the carrier change (`scored_ontology_version_id` → `release.autonomy`) recorded as an implementation consequence the owner has not separately ruled on. **The one clause this row left standing is retired by D-131 (2026-09-05)**: *the version it was computed under is on the score* was true while `AutonomyResult.ontologyVersion` existed, and that field is gone with the concept. The column and the two tables this row left declared-and-unread are dropped by `0009_drop_ontology_versioning`, which is the separate migration decision this row said was owed |
| D-94 | Every node-type term added after doc 3 §3's original six is **named after the Attractor handler it selects**, and the three added on 2026-08-30 sit under a new abstract category `orchestration`. | `lib/core/ontology/core.ts` (a 30-line docblock); `lib/core/attractor/emit.ts` (`ATTRACTOR_TRANSLATED_TYPES`) | OWNER-STATED, 2026-08-30 — *"use ultracode mode to implement all the things discussed"*, after the owner reviewed and agreed a decision register | IMPLEMENTED — `parallel` (handler `parallel`, shape `component`), `parallel.fan-in` (`parallel.fan_in`, `tripleoctagon`), `manager-loop` (`stack.manager_loop`, `house`). `orchestration` is unparented alongside `human-in-the-loop` and `evaluative`, argued in place: `evaluative` judges **content** while these three shape the **run**, and a root over all three would assert that "who does the work", "who judges it" and "who shapes the run" are three kinds of one thing, which no document claims. **`parallel.fan-in` is a SIBLING of `parallel`, never a child** — a join is the counterpart of a split, and `isA(fan-in, parallel)` would make every rule about fan-out silently catch the join. The six original types are grandfathered as `ATTRACTOR_TRANSLATED_TYPES` and a cell holds every later row to the identity rule, so a tenth non-identity row reds. A core term id may now contain a `.`, and it is a character and never a separator: `splitTermId` reads `/` alone, and `lib/href.test.ts` round-trips every shipped term id from `termHref` back to `view.get` | CONFIRMED — the owner instructed this wave on **2026-08-30** with *"use ultracode mode to implement all the things discussed"*, after reviewing and agreeing a decision register that covered it |
| D-97 | **A conditional edge counts exactly as much as an unconditional one, in every risk analysis, always. A leak that can happen is a leak.** | `lib/core/bundle/types.ts` (a 25-line docblock on `ResolvedEdge.condition`); `lib/core/bundle/resolve.ts` (restated at the `buildGraph` call, the one line the rule reduces to) | OWNER-STATED, 2026-08-30 — *"use ultracode mode to implement all the things discussed"*, after the owner reviewed and agreed a decision register | IMPLEMENTED, and **narrowed on 2026-09-04 without being weakened**. `ResolvedEdge` carries `condition` and `weight`, both optional; the rule that no risk reading may treat a guarded edge as maybe-not-taken is untouched, and `lib/core/analysis/analyze.test.ts`'s `scores identically` cell still holds security, findings, autonomy and markers byte-identical between a guarded bundle and its unguarded twin. What changed is that the VALUE and the SYNTAX are now separate questions: `lib/core/attractor/condition.ts` implements §10.2's grammar so a guard Attractor's own lint would refuse can be reported, and it exports no `evaluate`, deliberately, because one would put `condition="false"` a single call away from deleting an edge out of a security walk. The original wording — *both opaque, DarkPrint implements no part of Attractor's expression grammar* — is left standing above rather than rewritten, per D-113. `weight` is kept a **string** because `0` and `0.0` are different bytes in a file somebody else's runner reads. The guard is evaluated at run time, on another machine, against data DarkPrint never sees. The docblock names the three ways somebody would "improve" this — weight a path by its guard, skip a false-looking edge, add a probably-not-taken tier — and names the exact cost: the improvement is worth precisely what an author gains by writing `condition="false"` on the edge that leaks. Held at three sites, including a suite that guards **every** edge of all three fixtures and holds the analysis, the diagnostics and the reachability to the unguarded bundle, with an instrument cell first so a regex that matched nothing cannot make the comparison a bundle against itself. **The schematic does not distinguish a guarded edge either**: a second stroke would tell a reader the branch is uncertain beside a Security number computed as though it were certain, which is a picture-contradicts-the-score defect this codebase has shipped before | CONFIRMED — the owner instructed this wave on **2026-08-30** with *"use ultracode mode to implement all the things discussed"*, after reviewing and agreeing a decision register that covered it |
| D-98 | The exporter emits a node `class` derived from the card's `type` (with its full ontology ancestor chain) and its `phases`, every name prefixed `dp-`. | `lib/core/attractor/emit.ts` (`attractorClassesFor`, `attractorClassName`, `CLASS_PREFIX`, `emit.ts:1007`) | OWNER-STATED, 2026-08-30 — *"use ultracode mode to implement all the things discussed"*, after the owner reviewed and agreed a decision register | IMPLEMENTED — Attractor spec §2.10 derives classes from subgraph labels by lowercasing and hyphenating, so an unprefixed `agent` would collide with a reader's own subgraph labelled `Agent`, on a different set of nodes. **Every class DarkPrint emitted until 2026-09-04 was one name containing spaces**: the list was joined on a space where §2.12 says comma-separated and §8.2's `ClassName ::= [a-z0-9-]+` admits none, so no `.dp-*` selector in any exported bundle has ever matched anything. Two guards agreed with the emitter instead of with the spec, because both rebuilt the expected value with the emitter's own separator. Fixed by `join(",")`. **The ergonomic cost is priced in the code rather than discovered**: a `model_stylesheet` rule written against Attractor's own `agent` class matches none of DarkPrint's nodes, so every rule aimed at an emitted class must name `dp-agent`, and a reader who does not know that writes a rule that silently matches nothing. That is a *missing* answer; the alternative is a layout subgraph silently re-routing every agent in the factory to a different model, which is a *wrong* one. The full ancestor chain is included because subsumption is why the vocabulary carries `broader` and it is the same walk `attractorKindFor` does for the shape, so a local `berti/simulation-node` under `agent` emits `dp-berti-simulation-node dp-agent` and a stylesheet rule for every agent catches a type its author never heard of. DarkPrint adopts no stylesheet and states no opinion about which model any class runs on | CONFIRMED — the owner instructed this wave on **2026-08-30** with *"use ultracode mode to implement all the things discussed"*, after reviewing and agreeing a decision register that covered it |
| D-99 | The private / runtime-read line is **declared as data and checked in both directions**, and every emitted file opens with a derived list of what a DarkPrint blueprint could not express. | `lib/core/attractor/emit.ts` (`ATTRACTOR_EMITTED_ATTRIBUTES`, `DARKPRINT_EMITTED_ATTRIBUTES`, `ATTRACTOR_UNEXPRESSED_ATTRIBUTES`) | OWNER-STATED, 2026-08-30 — *"use ultracode mode to implement all the things discussed"*, after the owner reviewed and agreed a decision register | IMPLEMENTED — the first names every attribute the emitter writes with Attractor's own meaning and must be a **subset** of the reserved sets; the second names every one it writes that Attractor ignores and must be **disjoint** from them; the third is `ATTRACTOR_RESERVED` minus the first minus one deliberately withheld name (node `type`, Attractor's handler override — DarkPrint's node type reaches the runner as `shape`, so listing it as unexpressed would tell a reader the type was dropped). It computes to **10 graph, 18 node, 3 edge**, all derived and nothing hand-written, so a name Attractor reserves tomorrow reaches the header on its own and a name the emitter learns to write leaves it on its own. **The failure this catches is silent in both directions**: a private field parked on a reserved name configures a run, and a runtime field under an unreserved name is dropped by the runner while the bundle claims to carry it. The header also carries two sentences a derived list cannot: that `type` is withheld rather than missing, and that a node's `prompt` is all the runner receives from its card, so ports, dependencies, declared prohibitions and risk markers are enforced by nothing in the file. Derived rather than transcribed, because a list of what is missing goes stale silently and is read on a machine holding no other DarkPrint document | CONFIRMED — the owner instructed this wave on **2026-08-30** with *"use ultracode mode to implement all the things discussed"*, after reviewing and agreeing a decision register that covered it |
| D-100 | The Attractor export is an **opt-in command**, never a file in a published folder. | `packages/cli/src/export.ts`; `packages/cli/src/run.ts`; `lib/content/bundle-export.ts` | OWNER-STATED, 2026-08-30 — *"use ultracode mode to implement all the things discussed"*, after the owner reviewed and agreed a decision register | IMPLEMENTED — `darkprint export <dir> --attractor` writes Attractor DOT to **stdout** and nothing to disk: a file-writing verb needs a path, an overwrite rule and a collision rule, and the shell owns all three. `exportBundle` still writes only `topology.dot`, `cards/*.yaml`, `README.md` and, where used, `ontology/extensions.yaml`, so the 2026-08-25 owner instruction about folder contents is **not** reversed. The format is required and `EXPORT_FORMATS` is a list of one, because a default format is a promise the default never changes. It composes `readBundleDirectory` + `validateBundle` + `emitAttractorDot`, so the bundle this command refuses is the bundle `darkprint validate` and `POST /api/validate/bundle` refuse, in the same words, by construction. Refuses rather than half-answers, because a DOT compiled from a refused bundle looks complete while carrying a node with no `prompt`; warnings go to STDERR while the DOT alone goes to STDOUT, so redirecting stdout leaves a parseable file. The MCP tool `export_pipeline` runs the same compiler over a fetched release (SEAM-117, and see D-107) | CONFIRMED — the owner instructed this wave on **2026-08-30** with *"use ultracode mode to implement all the things discussed"*, after reviewing and agreeing a decision register that covered it |
| D-101 | **Storable and approved are different questions**, and what may refuse an artefact is a named list of diagnostic codes rather than a severity. | `lib/core/gate.ts` | OWNER-STATED, 2026-08-30 — *"use ultracode mode to implement all the things discussed"*, after the owner reviewed and agreed a decision register | IMPLEMENTED — `DIAGNOSTIC_GATE` is an exhaustive `Record<DiagnosticCode, GateClass>` over all 61 codes (60 until `attractor/condition-syntax` joined on 2026-09-04, classified `approval` and added to `INFERRED_CODES` in the same change, per rule 4) and **fails to compile** if a code joins the union unclassified or is classified after leaving it. Four rules govern it: storable is near-unconditional (it parses, it is addressable, it is attributable); approved is a score and a score is allowed to be bad; an author-declared prohibition may still block because the card's own author asked for it; **nothing DarkPrint infers may ever block**. The blocking set is twelve codes, derived from the table rather than transcribed, and `INFERRED_CODES` (38) is written by hand against a *different* question precisely so the disjointness cell means something — a derived pair would be disjoint by construction and prove nothing. `card/missing-field` and `card/bad-type` are narrowed a second time by the field they already name (`ADDRESS_FIELDS = id, version`), forced by a real case rather than by taste: holding either whole would make `darkprint import` refuse a legible, addressed, attributed folder over one field still to be written. **Both remaining gates are wired as of D-109**: `publish` and `/upload` asked `hasErrors` through the FROZEN `components/upload/progress.ts` when this row was written, and now ask `isReleasable` | CONFIRMED — the owner instructed this wave on **2026-08-30** with *"use ultracode mode to implement all the things discussed"*, after reviewing and agreeing a decision register that covered it as to the split. The two unwired gates were the one open half and were ruled on the same day: see D-109 |
| D-102 | The round-trip gate is the witness for the Attractor compatibility claim, and its corpus lives **in this repository**. | `tests/attractor-round-trip.test.ts`; `tests/attractor-corpus/`; `lib/core/attractor/import.ts` | OWNER-STATED, 2026-08-30 — *"use ultracode mode to implement all the things discussed"*, after the owner reviewed and agreed a decision register | IMPLEMENTED — 44 cells over twelve hand-authored pipelines. **"The Attractor half" is the named constant `ATTRACTOR_EMITTED_ATTRIBUTES`**, so nothing in the gate transcribes a list of names, and both sides' values pass through `unquoteAttractorString` so the comparison is what a *runner* would read. No network, because a compatibility claim that depends on somebody else's uptime is not a claim you can check. The corpus files are stated as hand-authored against the constructs the spec gives a handler or a reserved attribute to, and explicitly **not** as transcriptions of the spec's own examples: this repository does not vendor that document, and a false quotation would be worse than an honest description. **Losses are declared per file with the exact value and the reason, and compared in BOTH directions, so a declared loss that stops happening reds as loudly as a new one.** The nine shipped bundles round-trip byte-identical, the `// Blueprint digest:` line aside. `darkprint import` is the other half and synthesises a **draft**: every card at `0.1.0` because the first thing a draft needs is to be edited, `author` required with no default, `provenance` carrying `derived:attractor <origin>` — two different facts in two different fields, because a prompt is somebody's writing and an import is attributed or it does not happen | CONFIRMED — the owner instructed this wave on **2026-08-30** with *"use ultracode mode to implement all the things discussed"*, after reviewing and agreeing a decision register that covered it |
| D-103 | The hand transcription of Attractor's reserved names carries a **pin to the document it was read from**, and a scheduled job checks whether that document moved. | `lib/core/attractor/reserved.ts` (`ATTRACTOR_SPEC_PIN`); `scripts/check-attractor-drift.mjs`; `.github/workflows/attractor-drift.yml` | OWNER-STATED, 2026-08-30 — *"use ultracode mode to implement all the things discussed"*, after the owner reviewed and agreed a decision register | IMPLEMENTED — the pin records `rawUrl`, `blob`, `sha256`, `bytes`, `upstreamCommit`, `movedOn` and `verifiedOn`, sits beside the transcription it certifies, and the script reads it out of the source by regex rather than restating it, so there is no second number to keep in step. **Exit codes are three-valued on purpose** — 0 unchanged, 1 drifted, 2 could not check — because an unreachable GitHub answering 1 would send somebody to re-verify a document nobody had touched, which is the one wrong answer this script could give. On drift it names any attribute upstream tabulates that the transcription lacks; the reverse direction is deliberately not reported, because eight names are carried on purpose that the spec's Appendix A never tabulated, and listing them every time would train a reader to ignore the output. It runs as a **non-blocking** GitHub Actions job, following `docs-drift.yml`'s own precedent: a red here reports that somebody else changed a document and must not block an unrelated merge. The transcription was re-verified by hand before `verifiedOn` was written, and needed no correction | CONFIRMED — the owner instructed this wave on **2026-08-30** with *"use ultracode mode to implement all the things discussed"*, after reviewing and agreeing a decision register that covered it |
| D-104 | `FACTORY_DOT` is **deleted** from `lib/content/bundle-export.ts`, and the two FROZEN suites that were its only consumers were unfrozen to remove the pins. | `lib/content/bundle-export.ts`; `components/blueprint/download-name.test.ts`; `components/bundle/files.test.ts`; `tests/server/t261/frozen-tests.test.ts` | OWNER-STATED, 2026-08-30 — *"use ultracode mode to implement all the things discussed"*, after the owner reviewed and agreed a decision register | IMPLEMENTED — the constant survived the 2026-08-25 folder removal only because two test suites imported the name, which is an export existing to be imported by a test. Both suites now spell the name as a local literal and **no assertion was removed, weakened or re-pointed**; the negatives (a bundle never writes it, a listing never names it) stand unchanged in force. **Dropping `factory.dot` from `components/bundle/files.test.ts`'s `ALLOWED` allow-list is a strengthening, and it was falsified**: until then a seeded listing could still advertise the file and pass, which is the exact failure that file's own banner describes; adding it to a seeded draft now reds. Both frozen blobs were re-pinned deliberately, with the cause named in place as `tests/server/t261/frozen-tests.test.ts` demands. `BUNDLE_AGENTS` stays, on a real product consumer rather than a test pin (see D-52) | CONFIRMED — the owner instructed this wave on **2026-08-30** with *"use ultracode mode to implement all the things discussed"*, after reviewing and agreeing a decision register that covered it |
| D-105 | The ontology version **stays at `0.1.0`** through the pre-launch period, and the "adding a term is a MINOR bump" rule is suspended until the first real publish. | `lib/core/ontology/core.ts`; `lib/server/versioning/ontology-bump.ts` | OWNER-STATED, 2026-08-30 — the owner was walked through every open item one at a time and ruled on each, on this row specifically | IMPLEMENTED — nothing changed, which is the point: `parallel`, `parallel.fan-in` and `manager-loop` keep `since: 0.1.0`, `DARKPRINT_CONFIG.ontologyVersion` keeps `0.1.0`, `core.test.ts`'s "every term's `since` equals the ontology version" invariant holds, and the eight version literals across `t040`/`t080`/`t100`/`t250` and `lib/server/seed` stay put. **The rule is suspended, not repealed**, and the trigger is written here so it cannot be lost: the owner's ground is that no score has ever been published under any vocabulary, every row in the system is seed the owner wrote, and there is therefore nothing for a `0.2.0` to be distinguishable *from*. That ground expires the moment a score reaches somebody who is not the owner. **From the first real publish, `ontology-bump.ts`'s rule binds again** and an added term is a MINOR bump, because from then on the version string is the only thing telling a reader that two scorecards were computed against different vocabularies | CONFIRMED — owner ruling, 2026-08-30, taken on this row in isolation after the alternatives (bump to `0.2.0`, declare `1.0.0`) were put to them. Supersedes this row's earlier CONTRADICTED reading, which was correct about the rule and had not been given the pre-launch premise. **SUPERSEDED 2026-09-05 by D-131, and resolved by deletion rather than by ruling.** The owner was asked whether the rule resumes and answered by removing the thing it prices: there is no `Ontology.version` and no `CORE_ONTOLOGY.version`, so *adding a term is a MINOR bump* has no number to move and `lib/server/versioning/ontology-bump.ts`'s `ontology` namespace has no subject. D-117's correction to this row is discharged the same way — two scorecards under one version string is not a state that can arise once there are no version strings. `V01 = "0.1.0"` survives in `lib/core/ontology/core.ts:32` as the RELEASE every term declares in `since`, and its own docblock at `:26` says it is not a version; `since` is the residue named in D-131 |
| D-106 | `darkprint report` **stays**, and D-270-01 C2-C5's withdrawal no longer binds it, because one of that ruling's three grounds has expired. | `packages/cli/src/report.ts`; `backend.md` §T270; `tests/server/t270/instrument.test.ts` | OWNER-STATED, 2026-08-30 — the owner was walked through every open item one at a time and ruled on each, on this row specifically | IMPLEMENTED — the verb ships unchanged. **The expired premise, named so the supersession is checkable rather than asserted**: D-270-01 C2-C5 withdrew `publish` and `report` on three grounds, of which "no route accepts a run report" became FALSE when T280 shipped `POST /api/blueprints/{owner}/{slug}/runs`, session-guarded and with the release digest resolved server-side. The other two still hold — no write route accepts a key, and no key-to-actor path exists — which is why the verb takes a session explicitly rather than pretending otherwise, and why **an API-key path is owed follow-up work** rather than a nicety. **`backend.md` §T270 is left byte-intact on purpose**: `tests/server/t270/contract.ts` parses its withdrawal clause with a regex that THROWS when it fails to match, and a single-verb clause would not match, so amending it would red a merged blind suite over a documentation edit. §T270 stays the historical record of what T270's contract published; this ledger is where the ruling lives, per `CLAUDE.md`. The verb is consequently invisible to the T270 suite, which is correct: it was not built to that contract | CONFIRMED — owner ruling, 2026-08-30, after the two premises were separated and the alternatives (rename the verb, or remove it until a key path exists) were put to them |
| D-107 | The MCP tool set is **five**, and `export_pipeline` satisfies the "read access and nothing else" contract rather than breaching it. | `packages/mcp/src/tools.ts`; `packages/mcp/src/server.test.ts`; `app/mcp/page.tsx` | OWNER-STATED, 2026-08-30 — the owner was walked through every open item one at a time and ruled on each, on this row specifically | IMPLEMENTED — the tool ships. **The test that guarded the closed set was widened, never weakened**: `server.test.ts`'s `tools/list` cell still asserts an EXACT set and still refuses a superset, so a sixth tool reds exactly as a fifth did. The ground the owner ruled on: the tool reaches no route `fetch_release` does not already grant, writes nothing, and returns a transformation the caller can reproduce with `darkprint clone` plus `darkprint export --attractor` — so it saves a step rather than granting a capability, which is the test the original comment's own wording implies. `app/mcp/page.tsx`'s `OPERATIONS` array stays at four rows and the page discloses the tool in prose, because `tests/server/t220/surface.test.ts` holds that array to `backend.md` §T220's four names and the page is Forbidden to both halves of T220 precisely so it acts as a second author | CONFIRMED — owner ruling, 2026-08-30, after the read-and-derive shape was distinguished from a capability grant and the alternatives (a `format` parameter on `fetch_release`, or deletion) were put to them |
| D-108 | `will_not` is priced on the way **out**: withdrawing an undertaking is MAJOR, stating one is MINOR. | `lib/core/version/bump.ts` (`compareWillNot`); `lib/core/version/bump.test.ts` | OWNER-STATED, 2026-08-30 — the owner was walked through every open item one at a time and ruled on each, on this row specifically | IMPLEMENTED — the first draft priced both directions MINOR, on the ground that nothing in the engine reads `will_not` so no pinned bundle can break either way. **That argument inverts on inspection, and the inversion is the decision**: because nothing reads the field, the version number is the ONLY witness a reader gets that a published undertaking disappeared, and pricing that at the same level as an addition which costs them nothing puts the one available signal where it cannot be seen. It also restores symmetry with `cannot`, which is major when it GAINS an entry: the two fields break in opposite directions, because `cannot` constrains the graph while `will_not` only reassures a reader. `bump.test.ts` asserts all four corners in one cell, so neither field can drift onto the other's schedule, and the reorder cell was re-pinned to `major` with its reason rather than loosened to "not patch" | CONFIRMED — owner ruling, 2026-08-30, after the opposite-direction argument was put to them alongside keeping the symmetric MINOR and pricing both as patch |
| D-109 | The publish gate **splits by lifecycle stage**: a draft is near-unconditional, a release must additionally resolve. | `lib/core/gate.ts` (`RELEASE_ONLY_CLASSES`, `isReleasable`); `components/upload/progress.ts`; `components/upload/ValidationReport.tsx`; `components/upload/UploadFlow.tsx`; `lib/server/export/build.ts` | OWNER-STATED, 2026-08-30 — the owner was walked through every open item one at a time and ruled on each, on this row specifically | IMPLEMENTED — completes D-101's one open half. A fifth `GateClass`, `unresolved-reference`, holds `bundle/missing-card` and `bundle/unpinned-card`: storable, because an imported foreign pipeline routinely has nodes DarkPrint cannot resolve and refusing those makes `darkprint import` useless, and not releasable, because a release is what a stranger pins and its scorecard would otherwise be computed over a graph with a hole in it. **The line is referential completeness, not correctness**, and rule 4 survives the split intact: a port that does not fit, a type that cannot flow and a term nobody has minted still refuse nothing at either stage, and a cell asserts `RELEASE_BLOCKING_CODES` shares no member with `INFERRED_CODES`. `blocksRelease` is a separate exported function rather than a defaulted `stage` parameter, deliberately: a publish path that forgot the argument would silently get the draft answer, and a gate whose loose reading is what you get by forgetting is the accident `gate.ts` exists to end. `STORAGE_CLASSES` is now written out rather than derived as "not approval", so a class added later blocks nothing until somebody lists it. **Falsified in both directions** before landing: reverting the reclassification reds 2 cells, and moving the new class into `STORAGE_CLASSES` reds 4. `bundleProgress`'s three states turned out to BE the two gates, so its hand-kept `AWAITING_CARD` set and `bundle/missing-dependency` shadow rule are deleted — `gate.ts` had reached the same two codes from the other direction. Both halves of the D-263-05 frozen pair were re-pinned with the cause named in place, which is what that freeze exists to force | CONFIRMED — owner ruling, 2026-08-30, after the alternatives (re-point both stages fully, or leave publish strict until import needs it) were put to them. **Known consequence, recorded rather than absorbed**: T100's AC2 fixture built its `in-error` refusal out of port mismatches, which no longer refuse; it now pins a digest no card hashes to (`bundle/digest-mismatch`, `unaddressable`), and the refusal kind, its count and its message form are unchanged |
| D-111 | Widening `scripts/check-docs-drift.mjs`'s `CODE_DIRS` to see `packages/` is **owed, and lands in a change of its own**. | `scripts/check-docs-drift.mjs`; `docs/architecture/seams.md` | OWNER-STATED, 2026-08-30 — the owner was walked through every open item one at a time and ruled on each | IMPLEMENTED 2026-09-02, in the separate change the ruling asked for. `CODE_DIRS` is now `["app", "components", "lib", "packages", "scripts"]`, and `dist` joins `node_modules`/`.next` in the walk's skip list because `packages/mcp/dist` is gitignored build output whose declaration files would carry a second copy of every anchor. **The deferral's own justification did not survive measurement and is corrected here rather than repeated:** the widening does NOT move the output for the other ids. Diffing the checker's output before and after, exactly one line changes — SEAM-117 leaves — and nothing is added in either direction, so the list goes 45 → 44 and every other id is reported byte-identically. The conflict the implementing session flagged was real and narrow: this change does make its own new entry pass, and it does nothing else. Falsified in both directions before landing — a probe anchor under `packages/cli/src` is picked up (so the walk reaches the tree, not just the one file), and a probe under `packages/mcp/dist` is picked up only when the `dist` skip is removed. **This is not a clean bill and the row says so rather than stopping at the win.** The checker still exits 1 and still names 44 ids as documented-but-unanchored, none of them silenced by this change. Two of the 44, SEAM-19 and SEAM-62, are the same blind spot one tree over: their only `TODO(SEAM-xx)` anchors sit under `tests/`, while `seams.md` points their rows at product code. `tests` was deliberately NOT added to `CODE_DIRS`, because a test comment satisfying a row about a component is a weaker guard rather than a wider one, and it would clear those two for the wrong reason. The other 42 have no anchor in any file of any type, and roughly half of those rows say the seam is CROSSED and the anchor was correctly deleted (SEAM-24's row states it outright). The real defect underneath them is the doc->code rule demanding a live anchor from every documented seam, retired ones included, which is a larger change and was not smuggled in here. **Nobody sees any of the 44 today**: `.github/workflows/docs-drift.yml:23` carries `continue-on-error: true` indented under the step, so the job concludes success, and that file's own comment at lines 6-10 claims the opposite. Dropping it while 44 ids drift would turn every PR red, so the ordering is to fix the doc->code rule first | CONFIRMED as a deferral — owner ruling, 2026-08-30, choosing a separate reviewed change over widening it here or leaving it unfixed. The separate change is the one recorded in this cell |
| D-112 | `/mcp` advertises **five** operations, and the one that is not a server verb is named as a composition rather than counted as one. | `app/mcp/page.tsx` (`OPERATIONS`); `backend.md` §T220; `tests/server/t220/contract.ts` (`COMPOSED`); `tests/server/t220/surface.test.ts` | OWNER-STATED, 2026-08-30 — the owner was walked through every open item one at a time and ruled on each, on this row specifically | IMPLEMENTED — D-107 shipped the tool and left the page listing four, so the site under-described its own server. §T220's contract line and the page's `OPERATIONS` array are amended together, as the owner required. **The guard was kept exact rather than relaxed**, which is the part worth reading: `surface.test.ts` held "the barrel publishes one verb per advertised operation", an equality that was only true while every operation happened to be a server read — `export a pipeline` is `mcpFetchRelease` plus a pure local compile and has no barrel verb. Relaxing the cell to `>=` would have stopped it detecting the one thing it was written for, a tool published against no verb at all. Instead `COMPOSED` names the exceptions and what each composes, the count subtracts them, and a second loop holds every named constituent to being published. **Falsified on both axes**: a sixth operation with neither a verb nor an entry reds, and a `COMPOSED` entry naming a verb the barrel lacks reds with the verb named. The §T220 line also stopped restating `fetch a release`'s file names, which had drifted to `blueprint.dot` and `AGENTS.md`; it names the exporter as the authority instead | CONFIRMED — owner ruling, 2026-08-30, after the alternatives (leave the prose-only disclosure, or drop the tool) were put to them |
| D-113 | Documents that record a superseded rule get a **superseded-by pointer and nothing else** — the original text is never edited. | `architecture/ontology.md`; `architecture/node-card.md`; `files/darkprint-ontology-v0.1.md`; `docs/audit/CLAIMS_RAW.md` | OWNER-STATED, 2026-08-30 — the owner was walked through every open item one at a time and ruled on each, on this row specifically | IMPLEMENTED — D-110 left the design documents divergent so the record of what was *specified* survives, which left a reader no way to know a rule had been retired. A blockquote naming the superseding decision is added above each affected passage, the quoted text below it untouched: the `requires_human` cross-field rule in three documents (D-92, D-110), and the "version is stamped on every card and every bundle" claim in one (D-93). **`docs/audit/CLAIMS_RAW.md` is annotated in its HEADER, not in its rows**, because that file's own contract is to quote verbatim and unedited — a note on row FON-007 would destroy the evidence of what the document said when it was read, which is the only thing the file is for. The header names all seven affected rows instead | CONFIRMED — owner ruling, 2026-08-30, after the alternatives (leave them entirely alone, or bring the design documents up to date) were put to them |
| D-114 | **An API key gates no read and is required for every write.** **NARROWED 2026-09-05 by D-138 and not overturned**: the read half is untouched, and the write half now reads *a key required for a write must have been minted with `scope: "write"`*. Every key minted before that date is read-scoped for life, so this row's promise holds for the whole life of every key issued under it. MCP reads stay usable with no key, at a ceiling; identity is required only where an action is attributed. | `lib/server/mcp/http.ts` (`mcpSubject`); `lib/server/limits/keys.ts`; `backend.md` B-17, §T220 AC6 | OWNER-STATED, 2026-08-31 — the owner was asked whether MCP use should require registering for a key, and agreed with the recommendation against it | NOT A CODE CHANGE — it ratifies the shipped behaviour and constrains the work that has not happened yet, which is why it is written down rather than left implicit. `mcpSubject` returns `{tier: "anonymous"}` for an unresolvable or absent secret and falls to the lower ceiling instead of refusing; **that arm must survive the arrival of key-authenticated writes.** Three grounds, all checkable: B-17 already priced this when it superseded D-83, and backend.md records that "the product's discoverability by agents depends on that ceiling being generous"; §T220 AC6 is a shipped criterion that an unkeyed client is limited rather than refused; and **the gate would protect nothing** — the same bytes are served by the website, by `/api`, and by the static `public/bundles/**` mirror, so requiring a key on the polite transport adds a step for the compliant and stops nobody. It also cuts against the registry's own goal of not creating friction for a community whose distribution model is handing a URL to an agent. **The write side is the opposite and for a different reason**: a run report with no actor cannot be attributed or revoked, and would put unattributable numbers into the `Cost / time` median that the whole path exists to unseed. Constrains D-106's owed follow-up: keys gain a scope, and no key minted under Settings §06's current promise ("carries no identity, so it authorizes no write") may gain write power without being reissued | CONFIRMED — owner ruling, 2026-08-31 |
| D-115 | The stored registry rows are brought onto the post-D-91/D-92/D-93/D-101 card shape by an **operator script against one named database**, and `content/` is the authority for 57 of the 58 stored cards rather than their own bytes. | `scripts/migrate-stored-cards.ts`; `scripts/stored-card-migration.ts`; `scripts/stored-card-migration.test.ts` | REPORTED OWNER-STATED, 2026-09-01, and **not reproduced**: this session received the instruction from the session that ran it, with no owner quotation. Under this document's own bar (§ *The provenance bar used here*) that is the `the author asked X with no quotation` class, so it counts as `UNATTRIBUTED` here, and the relay is named rather than laundered | IMPLEMENTED and **APPLIED ONCE, to the development database `darkprint`, on 2026-09-01**, with `--write --rescore-analysis --rollback-sql`. Measured after the run, by re-reading the database rather than by reading the script's report: 58 of 58 `card_version.body` carry `willNot`, 0 carry `requiresHuman`, 0 carry `ontologyVersion`, and 0 of 16 `release.manifest` carry `ontologyVersion`. The 87 entries the pre-migration corpus held under `cannot` are now 85 under `will_not` plus the 2 that were always term ids (`acceptance-criteria`, on `code-builder@1.0.0` and `targeted-debugger@1.0.0`), so the arithmetic closes and nothing was dropped. **It re-derives rather than edits, and the reason is a digest**: the same wave that split `cannot` also rewrote five cards' `notes` (pinned as `NOTES_REWRITTEN_BY_THE_WAVE`), `notes` is inside `cardDigest`, and 10 of the 16 releases pin one of those five, so transforming the stored bytes would have produced release digests that disagree with the committed `public/bundles/<slug>/README.md` for the same content. **One card has no archive counterpart**: `alessandro-smoke-checker@1.0.0` is database-only, gets the mechanical transform on its own bytes, and has its card and release digests pinned as literals computed before the run, because the obvious oracle is vacuous for it (both sides derive from the same new bytes, and deleting its one prose refusal outright also loads clean and also produces a self-consistent pair). **Not a numbered migration**, because `lib/db/migrate.ts` reads `<id>.down.sql` unconditionally and no SQL inverts this transform, and because `lib/db/migrate.test.ts` runs migrations against an empty database where a data migration is a green no-op. **Not a republish**, because there is no update path: `lib/server/cards` and `lib/server/archive` export no updater for a card version or a release, `publish()` refuses at `version-not-higher`, and `publishCard` compares stored `source` bytes and raises `cardConflict`, whose kind `runImport` counts as `skipped`, so a re-import that refused every card would report "already done". Bypassing the writers means bypassing their guards, so the guards are called directly on the values about to be written, before the transaction opens: `loadCard`, `storedCardGaps`, `findWellFormednessIssue`, `parseStoredVocabulary`, `loadBundle` + `isReleasable`, `cardDigest`, `bundleDigest`. **The digest cascade is total**: all 58 card digests and all 16 release digests moved, and 15 of 16 `release.local_vocabulary` values moved with them (they had drifted from `content/ontology/extensions.yaml` by one comment paragraph and now match it byte for byte). **What bounded the blast radius was this database's contents, not the migration's design**: `run_report.release_digest` is deliberately not a foreign key (`lib/db/schema.ts`, D-05-01) and both `run_report` and `notification_queue` were empty, so no run report was orphaned and no notification was enqueued. That is a fact about the development database and carries no promise about any other one. Two independent post-run audits recomputed rather than read: 58/58 card and 16/16 release digests recompute from the stored rows, 57/57 archive files reparse with zero diagnostics at any severity and match both the stored digest and the stored `source` byte for byte, and the field sets the encoder actually reads (`cardText` and `manifestText`, `lib/server/search/reembed.ts`) are byte-identical before and after, so the 42 card and 7 release vectors still describe their subjects | **PENDING-OWNER-REVIEW.** `CLAUDE.md` forbids stamping a row `CONFIRMED` on a ruling this conversation did not hear, and this one was relayed. Two things are owed and neither is done: the three `scripts/` files are **untracked**, with `package.json` and `vitest.config.ts` modified, so the tooling this row describes has never been committed; and the migration has **not** been applied to production (D-117) |
| D-116 | **GATE 1: the migration recomputes the stored scorecards** (`--rescore-analysis`), so six of sixteen release rows lose the autonomy figure they were published with. | `scripts/migrate-stored-cards.ts` (§4 of its production header, and the `rescore` branch of the release patch); `lib/core/analysis/autonomy.ts`; `public/bundles/frontline-triage/README.md:78` | REPORTED OWNER-STATED, 2026-09-01, relayed to this session as "Gate 1" and **not reproduced**. Same class as D-115: no quotation, so `UNATTRIBUTED` under this document's own bar | IMPLEMENTED. `--write` refuses without exactly one of `--rescore-analysis` or `--keep-analysis`, so neither is a default and the operator has to answer. With the flag, `autonomy`, `security` and `phase_coverage` are written from the same `loadBundle` the script already runs. **Measured, pre against post**: 6 of 16 release rows read `Conditional` level 3 before and read `Supervised` level 2 after; the other 10 read `Closed-loop` level 4 both times; 0 rows carry `Conditional` now. The six are two rows each for `frontline-triage`, `guarded-merge-bot` and `incident-commander` (a `darkprint` original and an `alessandro` copy of the same content, which is why the script keys its UPDATEs by `release.id` and not by digest). **The cause is D-95, not a renaming**: `Conditional` is still a live label at `lib/core/analysis/autonomy.ts:337` and no level moved. What moved is that autonomy now reads two shares and the band takes the weaker, so `frontline-triage`'s 6-of-7 headcount is overruled by its 2-of-3 control-point share, which the stored rationale sentence now says in full. **It changes no digest**: `bundleDigest` takes `{dot, cardDigests}` and reads none of the three columns. The ground for doing it at all is that `public/bundles/frontline-triage/README.md:78` has said `Supervised` since `b217ccf` while the row said `Conditional`, and after this migration **one digest addresses both documents** | **PENDING-OWNER-REVIEW as recorded here** (the ruling is relayed, not reproduced), and the row is not disputing it. **The tension with D-93, stated rather than smoothed.** D-93's supersession argument was that a release stores its whole scorecard at publish time, so a published score is a fact written once and never recomputed. **That premise is true of the product and this ruling does not touch it**: `release` has exactly one writer, `addRelease` (`lib/server/archive/release.ts:256`), reached only from `publish()` and `forkBundle`, and nothing under `lib/` issues an UPDATE against the `release` table at all. What the ruling establishes was never in D-93's scope: an **operator, from outside the product**, may correct a stored score when the analysis model under it has been replaced. So this **narrows D-93 by naming an exception rather than contradicting it**, and D-93's deleted vocabulary registry stays deleted, because the exception resolves nothing historical. It runs the current model over the current bytes. **The rule for a future rescore, written here so the exception cannot widen by precedent**: a stored scorecard may be recomputed only by an operator action outside the product, only when the analysis model itself changed under it, only across the whole corpus in one run (a partial rescore leaves two rows incomparable while both still claim the same ontology version), and only beside a rollback that can restore the prior bytes. Moving one release's score to a number somebody prefers is not this exception, and no product path can do it in any case. **One thing this row does not fix and does not hide**: `AutonomyResult.ontologyVersion` reads `0.1.0` on the old scorecard and on the new one, so the version string does not distinguish them. That is D-105's subject, and D-117 is what makes it urgent. **Amended 2026-09-05 (D-131): the field is gone, so the un-distinguishing string is gone with it.** That removes the misleading half and adds nothing: a stored score still carries no record of which model produced it, and the rescore rule in this cell — whole corpus, model change only, rollback beside it — is now the only thing keeping two rows comparable |
| D-122 | **Every branch edge in the archive carries a `condition`**, and every fork is one key against its own negation. | `content/blueprints/*/topology.dot` (24 guards over 12 forks, each with its reasoning in place); Attractor spec §3.3, §10.2, §10.4 | OWNER-STATED, 2026-09-04 — the owner's instruction of that date, chosen from options put to him, recorded here in the agent's own words per bar 1 | IMPLEMENTED — before this, no edge in `content/` had a guard, so §3.3 fell through to Step 5's lexical tiebreak on the target node id and eight of the nine topologies took a branch their own description contradicts. One key against its own negation is the only shape §10 can make total: it has `=`, `!=` and `&&` and no disjunction, so a two-clause arm leaves a state matching neither and §3.2 step 6 ends such a run silently as SUCCESS. In every pair the negation arm is the safe one, and §10.4 resolves a missing key to the empty string, so a node that says nothing escalates rather than ships. Three keys: `outcome` (7 forks), `preferred_label` (3), `context.lane` (2, written by `intent-router`'s `spec` since 2026-09-05 through Appendix C's `context_updates` — §11.0 Q17) | PENDING-OWNER-REVIEW as to the pairing: the instruction is owner-stated, the choice of keys and the direction of each negation are the agent's |
| D-123 | **`tool` maps to `box`/`codergen`, and a new `shell-tool` term takes the parallelogram**, carrying its command in `params.tool_command`. | `lib/core/ontology/core.ts`; `lib/core/attractor/emit.ts:196-198`; `lib/core/card/schema.ts:315`; Attractor spec §2.8, §4.10 | OWNER-STATED, 2026-09-04 — chosen from options put to him | IMPLEMENTED — §4.10's parallelogram handler FAILs with no `tool_command`, and all 25 of the archive's 57 cards typed `tool` instruct with prose, so every one of them compiled to a node a runner refuses on sight. `params` rather than a new top-level card field, so no wire-format key and no card digest moves; the reason is written at `readToolCommand` in `emit.ts`. `content/` holds no `shell-tool` card, deliberately: the term exists so an imported Attractor tool node round-trips, not because anything in the archive is one | PENDING-OWNER-REVIEW as to the carrier: the mapping and the term are owner-stated, `params.tool_command` and the term's spelling are the agent's |
| D-124 | **The archive's iteration cap is spelled `max_retries` and counted the way Attractor §2.6 counts it** — additional attempts after the first. | `content/cards/*@1.1.0.yaml` (six cards); `lib/core/card/iteration-cap.ts` | OWNER-STATED, 2026-09-04 — chosen from options put to him | IMPLEMENTED — six cards version-bumped `1.0.0 → 1.1.0`, and the old files REMOVED rather than kept beside them, because a card version is immutable and an archive shipping both spellings of one promise ships the wrong one to whoever pins the older ref. `targeted-debugger@1.1.0` carries `max_retries: 2`, which is the same three total attempts `@1.0.0` meant by `max_iterations: 3` while its own prose said "stop after three attempts" and permitted four. **`runbook-executor@1.1.0` carries `max_retries: 0`, not 1**: it declares `arbitrary-code-execution` and `secret-access` and its spec refuses to retry a failed step, so the old spelling authorised a runner to re-execute a half-applied production mitigation automatically. `field-normalizer` and `test-runner` did NOT move, because their prose counts re-entries, which is already §2.6's additional attempt | PENDING-OWNER-REVIEW as to the arithmetic: the spelling is owner-stated, the per-card numbers including `runbook-executor`'s 0 are the agent's |
| D-125 | **The unexpressed-attribute disclosure belongs in `README.md`.** | the owner's instruction, 2026-09-04; `ATTRACTOR_UNEXPRESSED_ATTRIBUTES` / `ATTRACTOR_DEFAULTING_ATTRIBUTES` / `ATTRACTOR_HANDLER_NEEDED_ATTRIBUTES` (`lib/core/attractor/emit.ts`) | OWNER-STATED, 2026-09-04 | **NOT-IMPLEMENTED.** `README.md` mentions Attractor nowhere. Recorded as owed at `docs/ARCHITECTURE.md` §11.0 Q20 rather than described anywhere as done. The three groups are derived and exported, so the disclosure would be rendered from them rather than transcribed | CONFIRMED (the decision; the build is owed, not the ruling) |
| D-128 | **§10.7's FUTURE operators are REFUSED rather than accepted**, on an asymmetry of costs. | `lib/core/attractor/condition.ts`, the header section headed "§10.7's FUTURE operators are REFUSED, and here is the bill" | AGENT-PROPOSED | IMPLEMENTED — refusing one Attractor later accepts costs a note on a file that will run, and nothing in this namespace blocks anything, so the author reads the section number and publishes anyway. Accepting one Attractor refuses today costs the thing the module exists to stop: DarkPrint calls the bundle clean, the author ships it, and the runner refuses the whole pipeline before the first node. §10.7 closes by telling implementations not to add these without updating the grammar, so no conforming implementation has them today. The message names §10.7 by number, so a reader sees *not yet* rather than *never* and knows the fix is to rewrite the guard | PENDING-OWNER-REVIEW |
| D-129 | **`attractor/condition-syntax` is graded WARNING although Attractor grades `condition_syntax` ERROR**, and the decisive fact is in this repository rather than in the spec. | `lib/core/attractor/lint.ts:105-117`; `lib/core/gate.ts` (rule 4, `DIAGNOSTIC_GATE`, `INFERRED_CODES`); `lib/server/registry/graphs.ts:182`; `lib/content/read.ts:265`; `packages/cli/src/run.ts:295` | AGENT-PROPOSED | IMPLEMENTED — `lint.ts`'s own docblock gives two reasons and both are load-bearing. **A DarkPrint topology is not the file a runner takes**: `emit.ts` compiles that one, so a finding here is about a file nobody executes as it stands. And `lintAttractor` is merged into `resolveBundle`, so `hasErrors` is what decides whether a published blueprint renders at all and what the CLI exits 1 on. An error here would take a live blueprint off the site over DarkPrint's parse of somebody else's expression language, whose own §10.2 and §10.5 disagree with each other. `gate.ts`'s rule 4 pre-authorises exactly this call and names the Attractor lint in it, so the all-warnings invariant survives; what Attractor would do with the finding travels in the hint instead. ~~**The priced cost is that `AttractorCompatibility.tsx`'s "None of these stops a runner" is now false** and has not been rewritten (§11.0 Q16)~~ — **that cost is discharged, and by two changes on 2026-09-05 rather than one**: Q16 rewrote the sentence so the findings branch says a runner reads the file and then declines it, and Q30 then deleted the whole component (D-143). The evidence for this ruling is now `lib/core/attractor/lint.test.ts`'s relocated cells, which assert the grade disagreement on `lintAttractor` directly, and `lint.ts`'s severity docblock no longer gives the panel as a reason | PENDING-OWNER-REVIEW |
| D-130 | **§10.2's grammar wins over §10.5's reference evaluator where the two differ**, and the parser is deliberately permissive about exactly one thing. | `lib/core/attractor/condition.ts`, the header section headed "§10.2's grammar wins over §10.5's evaluator where they differ" | AGENT-PROPOSED | IMPLEMENTED — §10.5 accepts a bare key as a truthiness check, falls back to a direct context lookup for an unqualified key, returns true for an empty condition and skips an empty clause; §10.2 admits none of the four, and §7.2 grades the rule on the grammar. Every refusal states what a §10.5 runner would do instead, so the report is true under either reading, and `condition=""` gets the sharpest message, because §3.3 reads an empty condition as NO condition and a guarded edge becomes unconditional. The one deliberate looseness is the `Literal` production, which admits no float: `context.confidence=0.8` is ACCEPTED, because §10.5's `parse_literal` returns text unexamined and every comparison in §10.3 is a string comparison, so refusing a decimal would be DarkPrint inventing a rule against an expression that demonstrably runs | PENDING-OWNER-REVIEW |
| D-131 | **Ontology versioning is removed in full, including the database**, and the down migration can restore the shape but not which vocabulary a stored score was computed under. | `lib/core/ontology/types.ts:76-86`; `lib/core/ontology/core.ts:26-32`; `lib/core/config.ts:117`; `lib/db/migrations/0009_drop_ontology_versioning.up.sql` / `.down.sql`; `content/ontology/extensions.yaml:13-24` | OWNER-STATED, 2026-09-05 — the owner asked the concept removed in full and was shown the unrecoverable down migration before choosing it | IMPLEMENTED. **The reasoning, so it does not have to be re-derived**: DarkPrint's vocabulary exists to name what an Attractor node IS, Attractor fixes those shapes in its own spec and carries no vocabulary version, so a DarkPrint-only semver on top was a second thing to keep in step with nothing — which is why D-105 had already suspended its own bump rule for want of a reason to apply it. **Removed from the engine**: `Ontology.version` and `CORE_ONTOLOGY.version`; `AutonomyResult.ontologyVersion`, `SecurityResult.ontologyVersion` and `BlueprintAnalysis.ontologyVersion` (**three copies, not one**, with a test asserting the three agreed); `DarkprintConfig.ontologyVersion`; `ImportPlan.ontologyVersion`; `BundlePanel`'s `scoredOntologyVersion` prop, the row that drew it and the disclaimer *"Two scores from different ontology versions are not comparable"*, which is removable only because the state it disclaims — two versions existing at once — is what is being deleted. `content/ontology/extensions.yaml` lost a `version` key `lib/content/ontology-file.ts` never parsed. The generated bundle README no longer prints an `ontology vX.Y.Z` line and all nine `public/bundles/*/README.md` are regenerated without it. **Removed from the database by `0009_drop_ontology_versioning`, the first destructive migration in this repository**: `release.scored_ontology_version_id`, then `ontology_term`, then `ontology_version`, in dependency order and with no `CASCADE`. `lib/db/schema.ts` goes 24 `pgTable` declarations to 22. `ontology_term` went with its parent because `ontology_term.ontology_version_id` is NOT NULL, references it, and is half the table's only unique key; neither table had a reader anywhere under `lib/server/**`, verified by grep before the migration was written. **Three freeze guards were amended by ruling rather than by edit, and the two lists took opposite answers on the same two tables** — `tests/server/t005/existing.test.ts` keeps both names in `BASE_TABLES` (a historical claim about what T000 shipped, compared against an immutable baseline), licenses the nineteen removed cells by name in `LICENSED_DELTAS`, and adds a cell asserting the two are ABSENT so a later migration cannot recreate them unnoticed; `tests/server/migrations.test.ts` SHORTENS `CONTRACT_TABLES` ten to eight, because that list is a claim about what a migrated database must contain NOW; `tests/server/t261` re-pins `components/blueprint/severity-word.test.ts` a third time (0+/1-, a deletion the compiler forces) and drops `scoredOntologyVersion` from the `BundlePanel` prop table; `tests/server/t260/contract.ts` drops `app/nodes/[...id]/page.tsx` from `FORBIDDEN_CONSUMERS`, because the route stopped importing from `components/ontology/TermTable` when D-133 removed its panels. **Where the simplification stops short, and it is owed rather than hidden**: `OntologyTerm.since` and `TermDeprecation.since` survive on the type, in `content/ontology/extensions.yaml:36` and on all 54 core terms as `V01`. `since` means *the ontology version that introduced the term* and there is no ontology version. `lib/core/ontology/core.ts:26` rewrites `V01` as the RELEASE rather than a version; the field itself is §11.0 Q25 | CONFIRMED as to the removal and the migration, including the data loss: the owner was shown that `release.scored_ontology_version_id` is the only column that ever named which published vocabulary a stored score used as a foreign key, that a down migration can recreate the column and not repopulate it, and chose the drop. **The consequential drop of `ontology_term` is CONFIRMED as forced rather than chosen** — the foreign key leaves no third option that is not a wreck. **PENDING-OWNER-REVIEW: the surviving `since`**. **D-154 (2026-09-06) CITES THIS ROW AS ITS REASON AND IS ABOUT A DIFFERENT MECHANISM**, which is recorded here so this row cannot be read as having authorised it: removing versioning did not remove the local overlay, and the overlay is live in the same tree this row was verified against. That correction is stated on D-154 and the consequence is `docs/ARCHITECTURE.md` §11.0 Q58 |
| D-144 | **The ontology's closing governance band comes off `/ontology`**, following the versioning removal it modelled, and the extension model it described stays. | the owner's instruction, 2026-09-06; `components/ontology/OntologyCatalog.tsx:141-158`; ~~`app/ontology/page.tsx:113-121`~~ (that route was deleted later the same day by D-153 and the band it names had already gone; the rail copy travelled to `app/spec/ontology/page.tsx` with the browser); `app/spec/ontology/page.tsx`; `components/ontology/canonical-route.test.ts`, whose line numbers moved when D-153 rewrote its header and are dropped here rather than guessed | OWNER-STATED, 2026-09-05 for the premise — *"we do not offer anymore the versioning of the ontology; we just keep the terms from the attractor spec"* — and 2026-09-06 for the band, which is that ruling applied to the one surface still drawing a model of it | IMPLEMENTED. The band drew three layers: the curated core, namespaced local terms, and **promotion into the core**, and the third is what took the other two with it — promotion described a review workflow nobody built, and it only meant anything while the vocabulary carried versions to promote a term between. `partitionTerms` came out with the band because nothing else counted the split, and the rail's sixth row went too: a rail row is a link, and the fragment it pointed at is no longer declared. **The MECHANISM is not gone, and the guard asserts both halves** — a bundle still declares its own terms in `ontology/extensions.yaml` and the resolver still merges them over the core, so `canonical-route.test.ts:130` pins the removal AND the two surviving places a reader learns it (the page lead's own sentence, and the `/spec/ontology` button above the catalog). A pass that removed either would have turned *we stopped versioning the vocabulary* into *you cannot add to it*, which is a different and false claim. `/spec/ontology:414` states in place that it carried a governance model until 2026-09-06. **The band's heading is deliberately not reproduced in this row or in those files**: the guard is a string check over `OntologyCatalog.tsx`'s own bytes, and a comment quoting the sentence its guard forbids reds a correct file. **AMENDED 2026-09-06, AND THE AMENDMENT IS THE POINT OF READING THIS ROW: the second clause of the decision above is CONTRADICTED by D-154, which the owner stated later the same day.** The band's removal stands. *And the extension model it described stays* does not: D-154 takes the overlay band and the checks band off `app/spec/ontology/page.tsx`, which is where this row says the surviving statement lives. So the paragraph above, arguing that a pass removing either half would turn *we stopped versioning the vocabulary* into *you cannot add to it*, now describes what has happened rather than what was avoided. **The premise was withdrawn by the owner rather than falsified by an agent**, and the row is left standing instead of being rewritten, because a justification a later ruling withdrew is worth more to the next reader than one that quietly changed. Two consequences of the amendment, both checked rather than assumed. **The guard citation was stale before the amendment and is history after it**: the `:130` this row named had already moved when D-153 rewrote the file's header, the cells that actually pinned the surviving half were `:236-239` (`The overlay`, `ONTOLOGY_ROWS`) and `:285-297` (`OVERLAY_RULES`, `in a namespace of their own`), and D-154 has replaced both. The first of them carried a comment saying it *survived every flip untouched*; it did not survive this one. What stands in their place is named on D-154. **The mechanism is still live and is now undocumented**, which is a different state from either of the two this row contemplated: `docs/ARCHITECTURE.md` §11.0 Q58 carries it, with the four rules and the nearest surviving statement for each | CONFIRMED as to the removal of `/ontology`'s governance band, which is unaffected. **The clause *and the extension model it described stays* is CONTRADICTED as of 2026-09-06 by D-154.** That is this ledger's healthy case, a later owner decision superseding an earlier one, and not a document disagreeing with the tree. **SUPERSEDED A SECOND TIME, later the same day, by D-156**, and the two supersessions are different in kind rather than cumulative: D-154 withdrew the clause's PREMISE by removing the bands it pointed at, and D-156 removes the PAGE they were on. `/spec/ontology` no longer exists and the surviving vocabulary bands are on `/spec/card`. The clause is marked superseded twice over rather than deleted, because a withdrawn justification is worth more to the next reader than a quietly rewritten one, and because the row's argument — that removing both halves turns *we stopped versioning the vocabulary* into *you cannot add to it* — is exactly the hazard §11.0 Q58 is still open on |
| D-154 | **THE OVERLAY BAND AND THE CHECKS BAND COME OFF `/spec/ontology`, and the site stops documenting a mechanism the product still ships.** The owner's words, verbatim and including the reason, because the reason is part of the record: *"remove \"The overlay / Anyone can add a term, in a namespace of their own\" section as it become false as we remove the versioning od the onotology and also the \"The checks / What the engine holds the vocabulary to\" section"*. | the owner's instruction, 2026-09-06 (given in conversation, quoted here in full); `app/spec/ontology/page.tsx`, the band labelled `overlay-heading` and the band labelled `ontology-checks-heading`; `components/spec/rows.ts:251` (`ONTOLOGY_ROWS`, whose only mount was the second band); `components/ontology/canonical-route.test.ts`, which this ruling inverts for the second time in a day: the two cells asserting both bands were present are replaced by `:285` *took the overlay and the checks bands off and left the rest of the page standing*, which checks the absences only after asserting the vocabulary band and both listings are still there, and `:433` *still ships, merges and enforces the overlay the page stopped documenting*, which holds the mechanism against the code in both directions and is written as a positive so it does not go red on its own repair; `docs/ARCHITECTURE.md` §11.0 Q58 and [4 · Sitemap](architecture/routes.md) | **OWNER-STATED, 2026-09-06.** Stated in this conversation in the owner's own words, which is the one condition `CLAUDE.md` allows for `CONFIRMED` | **THE RULING STANDS. ITS STATED REASON DOES NOT HOLD, and that is recorded here once because `CLAUDE.md` says the code wins and the divergence gets reported.** Ontology VERSIONING and the local OVERLAY are different mechanisms: versioning went on 2026-09-05 (D-131), and the overlay is live today. `partitionTerms` still separates a namespaced term from a core one (`lib/core/ontology/resolve.ts:104-112`); `ontologyView(CORE_ONTOLOGY, terms)` still merges the file over the core (`lib/content/read.ts:146`); `validate()` still refuses a local term that reaches no core parent, as `ontology/local-term-unrooted` at ERROR (`resolve.ts:394-406`), and still prices an unweighted local marker at `unknownMarkerWeight: 0` (`resolve.ts:409-421`, `lib/core/config.ts:167`); `exportBundle` still writes `ontology/extensions.yaml` into any bundle whose cards name a local term (`lib/content/bundle-export.ts:253-255`). All four `OVERLAY_RULES` entries were checked against the tree before this sentence was written and all four are true of it. `The checks` has no tie to versioning of any kind: five of `ONTOLOGY_ROWS`' six rows name diagnostics the engine raises about a local overlay. That is the whole of the correction and the ruling is not this ledger's to argue. **The freeze pin over that guard is RED on this change and is not this row's to fix**: `tests/server/t260/frozen-tests.test.ts:206` still pins blob `f596675`, which the rewritten file no longer hashes, and D-78's ritual makes the re-pin an owner-attributed act rather than a fix. Measured 2026-09-06 by running the suite twice, forty seconds apart, which is also how the working hash was seen to move between the two runs. The pin is named without a replacement hash for that reason: whoever re-pins takes it off the file they are freezing. **IMPLEMENTED as a removal of two surfaces and of nothing else**: no engine behaviour, no diagnostic code and no exported file moves, which is exactly why the gap below exists. **What stops being said anywhere on the site**, recorded because a claim's disappearance leaves no diff a reader will find: the four overlay rules (a local term is namespaced; it must name a core parent; a marker with no weight counts zero; the file travels inside the bundle because there is no registry to ask at resolve time), and the six check rows over the vocabulary, five of which describe the overlay's own enforcement. `docs/ARCHITECTURE.md` §11.0 Q58 names the nearest surviving statement for each, and for one of the four there is none | **CONFIRMED, 2026-09-06.** **READ THIS WITH D-144, WHICH IT REVERSES IN PART, AND WITH D-131, WHOSE REMOVAL IT IS OFFERED AS A CONSEQUENCE OF. THEY POINT AT EACH OTHER.** D-144 removed `/ontology`'s governance band on the explicit ground that `/spec/ontology` keeps the extension model, and argued the case at length; this ruling withdraws that ground. D-131 removed versioning and is the reason given here, and it is a different mechanism from the one being removed. **AND WITH D-156, WHICH DELETED THE PAGE THIS RULING TOOK THE TWO BANDS OFF**, later the same day. That changes nothing about the ruling and nothing about the size of the gap it opened: neither band came back, neither found a home, and the surviving vocabulary bands travelled to `/spec/card` with the browser. What moved is the address a new home would sit at. §11.0 Q58 is re-checked against the fold and unchanged |
| D-155 | **`ONTOLOGY_ROWS` is deleted from `components/spec/rows.ts`.** The owner's word, in full: *"remove it"*, answering a question that named both options — delete the orphaned export, or find the six rows a home. | the owner's instruction, 2026-09-06; `components/spec/rows.ts`, which keeps a comment where the constant stood; `docs/ARCHITECTURE.md` §6.2, §11.0 Q54 (resolved) and Q58 (updated) | **OWNER-STATED, 2026-09-06.** Asked as a direct question with the cost stated — that five of the six rows were the site's only written account of what the engine refuses about a local vocabulary — and answered in one sentence | **IMPLEMENTED, and it is a SECOND removal rather than a sweep.** D-154 took the checks band off the page, which stopped the site SAYING these things; this stops the repository HOLDING them. Nothing else moved: no engine behaviour, no diagnostic code, no test. Verified before deleting — no guard named the constant (`rows.test.ts` imports only `CARD_ROWS` and `TOPOLOGY_ROWS`; the byte-frozen `components/site/honesty.test.ts` imports only `CARD_ROWS`), so no freeze was fired and no assertion was weakened to allow it. The two sibling exports are untouched and still mounted. **What is gone as text**: six rows naming what the engine checks about the vocabulary, five of them diagnostics it still raises about a local overlay — `ontology/local-term-unrooted` among them. The behaviour is unchanged and `lib/core/ontology/resolve.ts` is where a reader has to go for it now, which is code rather than prose. §11.0 Q58 records that this makes that gap harder to close, because whoever closes it now writes the rows again instead of moving them | **CONFIRMED, 2026-09-06.** **READ WITH D-154**, which orphaned the export by removing its only mount, and whose own cell carries the correction that the overlay is still live |

## 4 · Autonomy, and how it is shown

| ID | Decision in one sentence | Source path and line | Provenance | Verifiable in code | Status |
|---|---|---|---|---|---|
| D-31 | No autonomy ordinal on any user-facing surface, no badge, no leaderboard, no sorting by autonomy — the class name is what a reader sees. | `PROJECT.md:57-58`; `files/darkprint-ontology-v0.1.md:125`; `architecture/engine.md:55-56,63-64`; commit `feaa9bc` | OWNER-STATED — commit `feaa9bc` records the owner deleting `AutonomyBar` and reverting the documented exception, so "the rule is back to unqualified" | IMPLEMENTED — no `components/ui/AutonomyBar.tsx`; `components/ui/autonomy-surfaces.test.ts`; `components/gallery/GalleryBrowser.tsx:162` filters and never sorts | CONFIRMED |
| D-32 | The segmented autonomy gauge is a deliberate, documented, narrowly scoped exception to D-31. | `docs/superpowers/specs/2026-07-29-visual-polish-design.md:24-48,74-78`; `docs/superpowers/plans/2026-07-29-visual-polish.md:21-23,527-529`; `docs/superpowers/specs/2026-07-29-content-cli-design.md:116-128` | OWNER-STATED (Q1: `Status: approved` at `2026-07-29-visual-polish-design.md:4`), then reversed by the owner at commit `feaa9bc` | CONTRADICTED — `AutonomyBar` was built (commit `6c69200`) and deleted (commit `feaa9bc`), together with the PROJECT.md and engine.md exception notes | CONFIRMED |
| D-33 | A human-gated blueprint is first-class; where a person acts is drawn in violet, never in the alarm colour the site spends on defects, and nothing frames a human node as a shortfall. | `PROJECT.md:59-61`; `files/darkprint-onboarding-positioning.md:52,236`; `sol_feedback.md:151-152` | AGENT-PROPOSED (Q2/Q3: doc 2 §1.1, §5.3) — the sibling rule D-31 is OWNER-STATED; this one is not | IMPLEMENTED — `components/ui/autonomy-surfaces.test.ts:100-127`; `HUMAN_PRESENCE_MARK` | PENDING-OWNER-REVIEW |
| D-34 | The 1–5 organisational maturity ladder and the per-blueprint autonomy class are two different scales and must never be confused. | `PROJECT.md:63-68`; `files/darkprint-onboarding-positioning.md:66` | UNATTRIBUTED | IMPLEMENTED — `lib/core/analysis/autonomy.ts` header; `components/home/levels.test.ts` | PENDING-OWNER-REVIEW |
| D-95 | Autonomy reads **two** shares and the band takes the weaker: the headcount, and the same share over **control points** alone. | `lib/core/analysis/autonomy.ts`; `lib/core/config.ts` (`autonomy.minControlPoints`) | OWNER-STATED, 2026-08-30 — *"use ultracode mode to implement all the things discussed"*, after the owner reviewed and agreed a decision register | IMPLEMENTED — `staffingFraction` is the old headcount unchanged to the digit; `control.fraction` is the share of control points that run unattended; `fraction`, the number every surface prints and the band reads, is the weaker of the two, or the headcount alone below `minControlPoints`. **The reason a headcount alone is not enough**: it weighs every node the same, so it cannot distinguish six branches running unattended from one node running unattended, and it has no way to say that a `manager-loop` decides whether other nodes run. **A minimum rather than a weighted blend**, because a blend would put a number in the score no document justifies, and because the class names stay true under a minimum — `assisted` is a graph the machine assists a person in, which is exactly a graph whose every routing call is a person's. `minControlPoints: 2` because one control point yields exactly 0 or 1, and swinging a class off one node is reading a distribution from one observation; the precedent is `telemetry.minRuns`, named in the config file itself. Below the floor the reading is still computed, still reported and still in the rationale, it just does not decide the band, and the rationale always quotes the number the band actually read so a page's percentage and the sentence under it cannot disagree. **Three seed blueprints moved `conditional` → `supervised`** (`frontline-triage`, `guarded-merge-bot`, `incident-commander`), and **no `isDarkFactory` badge moved**, structurally rather than by luck: `autonomousNodes === totalNodes` already says every node has a card and nobody in it, and every control point is a node. D-31 is untouched — the class name is still all a reader sees | CONFIRMED — the owner instructed this wave on **2026-08-30** with *"use ultracode mode to implement all the things discussed"*, after reviewing and agreeing a decision register that covered it |
| D-96 | `OntologyTerm.governsFlow` is a term flag that **is** a membership rule, unlike `impliesHuman`, which explicitly is not. | `lib/core/ontology/types.ts`; `lib/core/ontology/core.ts`; `lib/core/ontology/resolve.ts` (`controlCitation`, `isControlPoint`) | OWNER-STATED, 2026-08-30 — *"use ultracode mode to implement all the things discussed"*, after the owner reviewed and agreed a decision register | IMPLEMENTED — set on `evaluative`, `orchestration` and `human-gate`, inherited down `broader`, and read nearest-first so a local type rooted at `decision` cites `decision` rather than the category above it. A flag rather than an `isA` test because **the set spans three categories** and `human-gate`'s single `broader` slot is already spent on `human-in-the-loop`: no `isA` test can name the set, and a second parent per term would change every walk in `resolve.ts` for one metric. The asymmetry with `impliesHuman` is documented in all three files rather than left to be inferred. Membership lives in one place and is the mirror of `humanCitation` / `requiresHuman`, so nothing re-implements it. **A local overlay term cannot declare the flag** — `parseOntologyTerms` does not read it — but inherits it by rooting its `broader` chain at one of the three | CONFIRMED — the owner instructed this wave on **2026-08-30** with *"use ultracode mode to implement all the things discussed"*, after reviewing and agreeing a decision register that covered it |

## 5 · Site structure and navigation

| ID | Decision in one sentence | Source path and line | Provenance | Verifiable in code | Status |
|---|---|---|---|---|---|
| D-35 | The header is two nouns plus one Learn menu, dropping from eight flat items to three plus a menu. | `docs/superpowers/specs/2026-08-04-ia-redesign.md:83,116` ("Approved:", no attribution) | OWNER-STATED (Q1) — **superseded by D-88** | CONTRADICTED — `components/site/SiteHeader.tsx:52-95` is three Browse rows, a Design menu, a Publish button, Learn and an account menu | CONFIRMED |
| D-36 | `/what-a-blueprint-is` is a new page, the one page that teaches the object, and the only addition to the route list. | `docs/superpowers/specs/2026-08-04-ia-redesign.md:142` ("New — approved by the author, 2026-08-04") | OWNER-STATED | IMPLEMENTED — `app/what-a-blueprint-is/page.tsx`; stop 00 of `components/spec/sequence.ts:220-225` | CONFIRMED |
| D-37 | The documentation group is named Learn, ordered 00–06, with the same rail on every page in it. | `IMPLEMENTATION_PLAN.md:31-32,51-52` | AGENT-PROPOSED | IMPLEMENTED (partly) — `components/spec/sequence.ts` runs 00–06 and `components/learn/LearnShell.tsx` renders one rail, but the rail is on the **left**, not "at the bottom of every page" as `IMPLEMENTATION_PLAN.md:31-32` asks | PENDING-OWNER-REVIEW |
| D-38 | `/spec` is deleted, its three layer pages kept, and `/spec/scoring` merged into `/reading-the-radar`. | `PROJECT.md:268-278`; `components/spec/sequence.ts:13-28`; `next.config.ts` | UNATTRIBUTED — "The author asked the grading door off the spec index and asked `/spec` itself deleted" (`PROJECT.md:268-270`) is reported, never quoted | IMPLEMENTED — no `app/spec/page.tsx`; `next.config.ts` 308s `/spec` and `/spec/scoring` | PENDING-OWNER-REVIEW |
| D-39 | `/spec/ontology` is the canonical ontology index and `/ontology` is a compatibility URL. | `IMPLEMENTATION_PLAN.md:33-35,53` | AGENT-PROPOSED — and the provenance does not move. The owner ruled the same POSITION on 2026-09-06 (D-153); they did not adopt this plan, which reached the conclusion from a different premise and lost it in between | IMPLEMENTED — **RESTORED 2026-09-06. This cell has now read `IMPLEMENTED`, then `CONTRADICTED`, and reads `IMPLEMENTED` again.** It was contradicted while `app/ontology/page.tsx` existed and `next.config.ts` carried no `/ontology` redirect; the owner has since folded the browser into the spec page and deleted the index (D-153), so `/spec/ontology` is the canonical route for the vocabulary and `/ontology` is a 308 onto it, which is this row's sentence exactly. `components/ontology/canonical-route.test.ts` asserted the reversal by name and now asserts the restoration by name, carrying all three positions in its own header. **CONTRADICTED AGAIN, 2026-09-06, BY D-156** — the fourth position, arrived at hours after the third. This row's sentence names `/spec/ontology` as the canonical ontology index, and that route is deleted: the canonical route for the vocabulary is `/spec/card`, and `/spec/ontology` is a compatibility URL alongside `/ontology`. **The SHAPE of the row survives and its subject does not**, which is worth separating: one canonical route plus compatibility 308s is exactly what ships, and only the route's name is wrong. This cell has now read IMPLEMENTED, CONTRADICTED, IMPLEMENTED and CONTRADICTED, which is the whole argument of §11.0 Q60 | PENDING-OWNER-REVIEW **as this row's own provenance**, which is unchanged: what the owner confirmed is the outcome, not the document that first proposed it. Read with D-153 |
| D-40 | One word for the concept everywhere: `/ontology` is "Ontology" (the browser), `/spec/ontology` is "Ontology file (YAML)" (the format). | `components/site/SiteHeader.tsx:37-39` (owner quoted, 2026-08-12: "adopt the term Ontology also for /ontology page … be consistent through all the website") | OWNER-STATED | IMPLEMENTED as to the WORDS, and **half of this row's premise fell on 2026-09-06 with the route it named.** `components/build/WorkspaceStage.test.ts` went with `/build` (D-145), so the surviving witness is the chrome itself. D-153 folded the browser into `/spec/ontology` and deleted the index, and repointing the Browse row would have put both of this row's spellings on one destination on one screen — the state `components/site/nav.test.ts` refuses. **So the Browse row was DELETED**: "Ontology" no longer ships in the header or the footer, Browse is two rows rather than three, and `/spec/ontology` keeps the file form because it is stop 03 of `SPEC_SEQUENCE` and `spec-routes.test.ts` pins that sequence's shape. `components/site/SiteHeader.tsx`'s own header docblock carries the reversal and the choice, measured 2026-09-06. **The second half of this row's premise fell later the same day**: D-156 deleted `/spec/ontology` too, so neither route this row assigns a word to still exists. The concept keeps one word and it is now a section of `/spec/card` rather than a route with a name of its own | CONFIRMED as to the naming rule, which the owner stated and which nothing here repeals — **one word for the concept everywhere is exactly what survived**, and it survived by losing a surface rather than by gaining a synonym. The owner's 2026-08-12 instruction was that `/ontology` take the word "Ontology"; that route no longer exists, so the instruction has no subject left rather than being contradicted. Read with D-153 |
| D-153 | **THE VOCABULARY HAS ONE ROUTE, AND IT IS `/spec/ontology`.** The owner's words, verbatim: *"move the ontology page in the /spec/ontology substituing the \"every term\" box. Then, you can delete the /ontology page"*. The term browser becomes a band on the layer-03 spec page, `app/ontology/page.tsx` is deleted, and `/ontology`, `/ontologies` and `/ontologies/:slug` all 308 onto the spec page in one hop each. **`app/ontology/[...term]/page.tsx` does not move and no term URL changes.** | the owner's instruction, 2026-09-06 (given in conversation, quoted above in full); `components/ontology/canonical-route.test.ts`'s header, which carries all three positions in order; `next.config.ts`'s redirect block, which keeps the argument for the split beside the redirect that ends it; `docs/ARCHITECTURE.md` §11.0 Q57 and [4 · Sitemap](architecture/routes.md) | **OWNER-STATED, 2026-09-06.** Stated in this conversation, in the owner's own words, which is the one condition `CLAUDE.md` allows for `CONFIRMED` | **IMPLEMENTED.** `VocabularyBrowser` and `OntologyCatalog` mount from `app/spec/ontology/page.tsx` in the slot the amber `Every term →` `RouteBoxLink` held; `app/ontology/page.tsx` is gone; `next.config.ts` holds fifteen redirects, three of them onto `/spec/ontology`. **The three ontology redirects are REPOINTED rather than chained** — `/ontologies` could have been left pointing at `/ontology`, which now 308s onward, and every link written before the §1 rename would pay two hops; that cost is recorded at `/how-to-build-a-dark-factory` and is not paid twice. **The asymmetry under `/ontology` is deliberate and is verified rather than reasoned**: a `source` with no path parameter compiles to an anchored exact pattern in the matcher Next 16.2.11 ships, so `/ontology` 308s while `/ontology/<term>` resolves to the surviving catch-all | **CONFIRMED, 2026-09-06.** **READ THIS WITH D-39, WHICH IT RESTORES, AND WITH `components/ontology/canonical-route.test.ts`, WHICH IT INVERTS. THEY POINT AT EACH OTHER.** **This is the THIRD position this repository has held on one question, and the history is the reason this row is long.** (1) The catalog was a band on `/spec/ontology` while `/ontology` was 308'd away, because the §1 rename left one core ontology and an index route would have been a second door onto one room — D-39's original sentence. (2) The accounts pass changed the premise: the registry held three things, the chrome named all three, and pointing Vocabulary at a specification document is the *one route, two names* defect `components/site/nav.test.ts` exists for. So the index became a page again, the two split by question, and `canonical-route.test.ts` was written **to hold that split so neither could quietly absorb the other again**. That file was then byte-frozen by `backend.md`'s D-260-10, which cites it by name as what holds both routes in place — this ledger's own record of position (2) is D-39's `CONTRADICTED` cell and nothing else, so a reader looking for a `D-xxx` row that argues the split will not find one. (3) The owner has absorbed one into the other. **A guard written to prevent exactly this has been inverted by the person entitled to invert it.** That is legitimate and it is not an agent's call, which is why it is recorded as a reversal rather than quietly rewritten: the split's argument survives verbatim in `canonical-route.test.ts`'s header and in `next.config.ts`'s comment, and it does not get to win again by being the only version a later reader finds. **What the second position bought is NOT paid for another way, and that is the honest sentence.** The accounts pass split existed so the chrome could name the registry's third shelf without landing a reader on a specification document; the fold removes the shelf, and rather than repoint the Browse row onto the spec page — which would put "Ontology" and "Ontology file (YAML)" on one destination on one screen, the defect `components/site/nav.test.ts` refuses — **the Browse row was deleted**. Browse is two rows now. A reader loses the word "Ontology" from the bar and keeps every route into the page: the Learn menu, the footer's Specification column, the `/ontology` 308, and `/ontology/<term>` untouched. **This is a real cost of the ruling and it is recorded at the moment it was incurred rather than smoothed**, the treatment D-151's own cost got at §11.0 Q52. **The freeze is the mechanical consequence and it was PAID in the same wave**: `canonical-route.test.ts` is byte-pinned in `tests/server/t260/frozen-tests.test.ts` under `backend.md`'s D-260-10, this ruling rewrites it, and the pin was moved in the same wave with the owner's instruction named beside the digest — the shape D-142 and §11.0 Q55 both took. Verified 2026-09-06 by re-hashing the file against the pin and running the suite. **No digest is quoted here on purpose**: the file moved twice while this row was being written and a sha copied into prose is a reading of one moment. `git hash-object components/ontology/canonical-route.test.ts` against the `blob:` beside the entry is the check, and it takes one line. **D-40's question is answered rather than left open**, and the answer is above: it assigned two words to two routes, there is one route, and the word whose route was deleted was deleted with it. [2 · Glossary](architecture/glossary.md)'s Vocabulary row carries the same finding against the chrome it was measured from. **AMENDED 2026-09-06 BY D-156, AND THE AMENDMENT IS ONLY THE URL.** This row's decision — the vocabulary has ONE route — is the one D-156 keeps; what D-156 changes is which route, folding `/spec/ontology` into `/spec/card` and deleting the layer page. So this became the third position for a few hours and the fourth arrived the same day, which is why the standing question is no longer *which route* but whether a guard over a route name can be an invariant at all: `docs/ARCHITECTURE.md` §11.0 Q60. **Everything this row records as PAID stays paid**: Browse is still two rows, the Learn menu still carries one name for the vocabulary, and `app/ontology/[...term]/page.tsx` survived a second fold with no term URL moving. The three 308s this row created are repointed onto `/spec/card` rather than chained onto the new `/spec/ontology` 308, for the reason this row already states about `/ontologies` |
| D-156 | **THE ONTOLOGY KEEPS ITS VOCABULARY AND LOSES ITS DOCUMENT. `/spec/ontology` folds into `/spec/card` and is deleted; the Attractor crosswalk moves to the front of the Learn sequence; `/spec/topology` names the two DarkPrint attributes its own DOT carries.** This is the owner accepting an argued recommendation rather than issuing an instruction, and the record says so because the two are different acts. They asked: *"do we really need the ontology defined in /spec/ontology or we can keep only the attractor? It seems to me that this create confusion in the user; i'd prefer to unify everything under the attractor specs. What do u think about?"* The answer argued AGAINST deleting the vocabulary and FOR three changes. They replied: *"The motivations you provided are sound. Apply them."* | the owner's words, quoted verbatim in the dispatch to this lane, 2026-09-06. `components/spec/sequence.ts` (the five-stop order); `app/spec/card/page.tsx` (the fold's destination); `app/spec/topology/page.tsx` (the dialect band); `next.config.ts` (the fourth 308 onto `/spec/card`); `lib/core/ontology/core.ts`, `lib/core/attractor/emit.ts` and `lib/core/attractor/reserved.ts` (the evidence, all three re-verified below); `docs/ARCHITECTURE.md` §11.0 Q59 and Q60, [4 · Sitemap](architecture/routes.md), [5 · Journeys](architecture/journeys.md) | **OWNER-STATED, 2026-09-06.** The owner's sentences reached this lane through the orchestrator's dispatch rather than in its own conversation, and that is stated rather than smoothed. What they stated is a decision on a recommendation whose reasoning is reproduced below, so the reasoning is part of the ruling and not a gloss on it | **IMPLEMENTED, and the three evidence claims the recommendation rests on were re-verified against the tree before this row was written.** (1) **At most 13 of 54 core terms overlap Attractor, and the true figure is 10.** `lib/core/ontology/core.ts` declares 54 terms: 13 `node-type`, 15 `data-type`, 12 `tool`, 9 `risk-marker`, 5 `phase`. The 41 that are not node-types have no Attractor equivalent — `ATTRACTOR_EDGE_ATTRIBUTES` is exactly `label, condition, weight, fidelity, thread_id, loop_restart`, and no data-type, tool, marker or phase appears anywhere in Attractor's reserved sets. **The recommendation said 13 and the tree says 10, which is a correction in the ontology's own favour and is recorded rather than rounded.** `ATTRACTOR_TYPE_SHAPES` has ten keys, and the three node-types missing from it — `human-in-the-loop`, `evaluative`, `orchestration` — are the abstract categories the other ten declare as their `broader` (`core.ts:160`, `:167`, `:177`). Attractor has no notion of a category above a shape at all, so those three are a taxonomy layer with no counterpart, and a card typed as one of them resolves through `attractorKindFor`'s ancestor walk to `FALLBACK_KIND`. **So 44 of 54 terms have no Attractor equivalent, not 41.** (2) **The 10 that do overlap REFINE Attractor rather than duplicate it.** `ATTRACTOR_TYPE_SHAPES` maps `agent`, `tool` and `validation` all onto `shape=box` with handler `codergen`, so three DarkPrint node-types are one Attractor shape, and the ontology is the layer that tells them apart. The subsumption those three abstract parents carry is load-bearing rather than decorative: it is how a LOCAL type reaches a shape at all (`berti/simulation-node ⊂ agent` draws as a box with no row of its own), which is a mechanism Attractor cannot express and the registry depends on. Attractor specifies EXECUTION; the ontology specifies DESCRIPTION, which is the layer Attractor leaves open and the thing that makes a registry searchable. (3) **The friction was never the vocabulary; it was the dialect.** A shipped `topology.dot` carries `card="event-intake@1.0.0"` on nodes and `in=`/`out=` on edges. None of the three is in Attractor's Appendix A, `emit.ts` compiles the file into Attractor DOT, and the crosswalk explaining that sat at Learn stop 04 behind three DarkPrint documents. **So the three changes, and what each one is FOR.** (a) **The fold.** Every ontology term exists to be a legal value of a CARD FIELD: `type`, `phases`, `riskMarkers`, `tools`, and a port's `type`. The DOT never references a term, it references a card, and the card references terms. Put each term beside the field that consumes it and one document disappears without one word of vocabulary being lost. `VocabularyBrowser` and `OntologyCatalog` mount from `app/spec/card/page.tsx`, which inherits `export const dynamic = "force-dynamic"` with them; `app/spec/ontology/page.tsx` is deleted and `/spec/ontology` 308s onto `/spec/card`, joining `/ontology`, `/ontologies` and `/ontologies/:slug`, all four repointed rather than chained. (b) **The crosswalk moves to stop 01.** The sequence was 00 what-a-blueprint-is, 01 topology, 02 card, 03 ontology, 04 attractor, 05 towards-a-dark-factory. It is 00 entry, 01 attractor, 02 topology, 03 card, 04 towards, and it is five stops rather than six. (c) **`/spec/topology` names the dialect in its first band**, where the reader meets it. **One correction to the recommendation's own wording, found while verifying it and recorded because this ledger's convention is that a checked claim beats a repeated one**: the dialect is THREE attributes and not two. `in=` and `out=` both appear on shipped edges, both are read by `lib/core/bundle/resolve.ts:417,563` to pin which declared port an edge wires, and neither reaches the compiled file at all, because `ATTRACTOR_EMITTED_ATTRIBUTES.edge` is `label, condition, weight`. `card` behaves differently from both: it travels verbatim into the compiled file as an attribute Attractor ignores (`DARKPRINT_EMITTED_ATTRIBUTES`). A band naming two attributes is short by one, and a band saying all three "compile into Attractor" is wrong about two of them | **CONFIRMED, 2026-09-06.** **READ THIS WITH D-144, D-153 AND D-154, WHICH ARE THE EARLIER POSITIONS ON THE SAME QUESTION. THEY POINT AT EACH OTHER.** D-153 gave the vocabulary one route and made it `/spec/ontology`; this ruling moves that route to `/spec/card` and deletes the layer page, so **D-153's decision survives whole and only its URL moved**. D-154 removed two bands from `/spec/ontology`; this ruling deletes the page those bands were removed from, which changes nothing about the gap D-154 opened and moves its address — `docs/ARCHITECTURE.md` §11.0 Q58 carries that unchanged. D-144's surviving clause named `/spec/ontology` as where the extension model still lived; that clause was already withdrawn by D-154 and this ruling removes the page as well, so **it is superseded twice over and is marked so rather than deleted**. **What this ruling explicitly did NOT do, because it is the half the owner asked about and the answer was no**: the vocabulary is not deleted, not collapsed into Attractor, and not reduced. All 54 core terms, the local overlay, the resolver and every diagnostic are untouched. **The mechanical consequence is `components/ontology/canonical-route.test.ts`, inverted for the second time in one day and byte-pinned under D-78** — its re-pin is an owner-attributed act, and the standing question of whether a guard over a URL can be an invariant at all is `docs/ARCHITECTURE.md` §11.0 Q60 |
| D-41 | `/install` splits into two routes, one for the skill and one for MCP. | `next.config.ts` (owner quoted, 2026-08-07: "I prefer two pages, one for the skill and one for the mcp.") | OWNER-STATED | IMPLEMENTED — `app/skill/page.tsx`, `app/mcp/page.tsx`, `/install` 308s to `/skill` | CONFIRMED |
| D-42 | `/towards-a-dark-factory` is demoted, not deleted; nothing inside it is removed. | `docs/superpowers/specs/2026-08-04-ia-redesign.md:418-419`; `sol_feedback.md:659-660` | UNATTRIBUTED ("which is what the author asked for", reported, not quoted) — **moot, superseded by D-89** | CONTRADICTED — the route survives as Learn stop 06, but both children were deleted: `next.config.ts` 308s `/towards-a-dark-factory/the-climb` and `/which-tasks` onto the parent, and the comment there records the-climb as deleted by the author | PENDING-OWNER-REVIEW |
| D-43 | The header's action button stays `[Validate]` until publishing exists, because a `+ Share` label on every page would be the one promise the site cannot keep. | `docs/superpowers/specs/2026-08-04-ia-redesign.md:109-114`, under the `Approved:` at `:83` | OWNER-STATED (Q1) — **superseded by D-88** | CONTRADICTED — `components/site/SiteHeader.tsx:74` reads `label: "Publish"`; the page it opens still says publishing has no backend (`components/upload/UploadFlow.tsx:1185`) | CONFIRMED |
| D-44 | `/blueprints` is designed as a search-first result page from day one, even while it holds nine, because the target is community-scale. | `docs/superpowers/specs/2026-08-04-ia-redesign.md:242-245,262-264` (the owner's expectation of "thousands" is reported at `:242-244`, not quoted) | UNATTRIBUTED | IMPLEMENTED — `components/gallery/GalleryBrowser.tsx` (search + tag/category/phase/autonomy/fork facets, URL-backed) | PENDING-OWNER-REVIEW |
| D-45 | Account surfaces exist as presentation only: `/settings` and five profile tabs render seeded fixtures, and every control that would need a backend is drawn switched off and says so. | commit `b22ee53`; `app/settings/page.tsx:23-52`; `lib/data/account.ts:1-27` | UNATTRIBUTED — the commit describes the work; nothing records who asked for it | IMPLEMENTED — `app/settings/page.tsx`, `app/u/[username]/{blueprints,cards,saved,terms,[slug]}`, `components/profile/tabs.ts:41-47` | PENDING-OWNER-REVIEW |
| D-46 | The authoring skill route is named "Assisted Design", and `/build` is deliberately not a row in the Design menu. | `components/site/SiteHeader.tsx:60-76` ("on the author's instruction", "the omission is the author's call" — reported, not quoted) | UNATTRIBUTED | IMPLEMENTED — `components/site/SiteHeader.tsx:65,74-76` | PENDING-OWNER-REVIEW |
| D-89 | `/towards-a-dark-factory` as it stands is the current shape: one page, with `/which-tasks` folded in and `/the-climb` gone. | Owner, 2026-08-12 (answer: "I'm not interested in the children. it is fine the current page") | OWNER-STATED | IMPLEMENTED — `app/towards-a-dark-factory/page.tsx`; `next.config.ts` 308s both former children onto it | CONFIRMED |
| D-90 | The cost/time axis never renders a real reported median as a bar or radar spoke (no unit normalisation exists to make one honest), and once a page reads live reports the radar plots four spokes: D-180-07's "keep the seeded placeholder on the radar" clause is superseded, because the 2026-08-25 wiring instruction retires seeded figures wherever a real source stands and a live page carries no placeholder to keep. | `backend.md` D-180-01 and D-180-07 (owner-delegated); owner instruction of 2026-08-25 ("verify whether it has been implemented... if it has been implemented, wire it") | AGENT-PROPOSED (the supersession is the wiring wave's inference from that instruction, not a quoted ruling on the radar itself) | IMPLEMENTED — `lib/content/view.ts` costMetric, `components/ui/ScoreRadar.tsx` (live cost carries `value: undefined` and the spoke drops), `components/ui/MetricBars.tsx` rendered `{runs, median, p10–p90}` as text in the reporter's own units and was DELETED on 2026-09-04 once nothing mounted it; the load-bearing half of the rule — a real median never lands on the 0–100 axis — is unmoved and still enforced at `components/ui/ScoreRadar.tsx:64-69`. **CORRECTED 2026-09-06: `ScoreRadar.tsx` is still the only witness left, and it is now UNMOUNTED, which is a different state from the one this clause describes.** `/build` and `components/build/**` were deleted (D-145) and `ScorePanel.tsx` was its last route-reachable mount; the name now resolves to `lib/content/view.ts` and four test files and to no page. **The rule is enforced in a component nothing renders** — real, and unreachable, which is the shape D-140 named when it found a ledger row that could not tell *still disclosed* from *disclosed to nobody*. The clause is left standing rather than retired, because the constraint is about what may be drawn IF anything draws it, and an unmounted component is the cheapest place for that to keep being true. `docs/ARCHITECTURE.md` §11.1 carries the same *only witness left* sentence and belongs to another lane; the correction is reported there rather than made here. **CORRECTED AGAIN 2026-09-06, and this correction is a change of KIND rather than of count: `ScoreRadar.tsx` is DELETED (D-152, on the owner's instruction "remove ScoreRadar"), so there is no witness at all.** The rule outlives its witness, and the state it leaves is three separate facts, each measured against the tree on 2026-09-06 rather than inferred from the deletion. **(1) There IS a producer-side enforcement site.** `lib/content/view.ts`'s `costMetric` sets `value: undefined` on the live branch unconditionally, with or without a report on file, and its docblock was rewritten in the same change to say that this guarantee is now the whole of the rule in code — there is no number left for any future chart to give a length to, whatever that chart decides `undefined` means. `lib/types.ts:53-56` and `:78-80` carry the same constraint on `Metric.value` and `Metric.reported`, and `app/api/blueprints/[owner]/[slug]/runs/route.ts:24` refuses unit normalisation at the door. **(2) There is NO renderer.** No shipping component draws a cost row, a bar or a spoke; `MetricBars` went on 2026-09-04 and `ScoreRadar` on 2026-09-06, and nothing stands behind them. So the disclosure a reader could once meet on a page is gone. **(3) There is NO test.** `lib/content/index.test.ts:161-176` is the nearest candidate and disqualifies itself in its own comment: the fixture path it reads never takes the live branch, so its `?? NaN` satisfies the type checker and guards nothing. **The accurate sentence is that the constraint now holds BY CONSTRUCTION in one function and is asserted by nothing** — a smaller claim than "still enforced in a component nothing renders", and a larger one than "the rule has no enforcement site", which is how the wave's own dispatch described it before the tree was read | PENDING-OWNER-REVIEW |
| D-152 | **`components/ui/ScoreRadar.tsx` is DELETED**, and the honesty rule its last comment carried outlives it. The owner's words: *"remove ScoreRadar"*. | the owner's instruction, 2026-09-06; `lib/content/view.ts`'s `costMetric` docblock, rewritten in the same change to carry the rule it inherited; `components/learn/figures.test.ts`, `components/ui/scorecard-glance.test.ts`, `components/home/levels.test.ts` and `components/viz/scene-labels.test.ts`, each recording in place what stopped being checked | **OWNER-STATED, 2026-09-06** | **IMPLEMENTED.** The component had zero product mounts after `/build` was deleted (D-145), and its deletion was blocked on an honesty disclaimer rather than on tidiness, which is why `docs/ARCHITECTURE.md` §11.0 Q45 said no agent could close it in either direction. **What the rule looks like now, measured against the tree rather than inferred**: a PRODUCER-side enforcement site and nothing else. `costMetric` sets `value: undefined` on the live branch unconditionally, so a caller-reported cost median is never produced as a number a chart could give a length to; `lib/types.ts:53-56` and `:78-80` carry the same constraint on the type. **No renderer** draws any of the six metrics anywhere on the site. **No test** asserts it: `lib/content/index.test.ts:161-176` reads the fixture path, which never takes the live branch, and says so itself. `components/learn/figures.test.ts`'s radar row is removed with a written note that it had been measuring a placement nobody rendered since `/build` went | **CONFIRMED, 2026-09-06.** **Read with D-90 and D-180-01/D-180-07, which this row amends by deleting their last renderer.** `CLAUDE.md` forbids removing an honesty disclaimer without the owner's explicit instruction; the instruction is quoted above and is the whole authorisation. **One divergence is reported rather than reconciled**: the dispatch that authorised this wave said the rule would be left with no enforcement site in code, and the tree says otherwise. The code wins, and the correct sentence is that the constraint holds by construction in one function and is asserted by nothing |
| D-85 | MCP is a top-level navigation destination, placed by its intended importance at release rather than by whether the server exists; it is described separately from the authoring skill. | `IMPLEMENTATION_PLAN.md:48-49`; `sol_feedback.md:369-373` | AGENT-PROPOSED | IMPLEMENTED — `components/site/SiteHeader.tsx:64`, with `BUILD_BLURB` distinguishing it from `/skill` | PENDING-OWNER-REVIEW |
| D-88 | The header as it stands is the current shape — three Browse rows, a Design menu, a `Publish` button, Learn and the account menu — and it supersedes both the 2026-08-04 "two nouns and one Learn menu" approval and the `[Validate]` rule. | Owner, 2026-08-12 (answer: "keep the current", asked against D-35 and D-43) | OWNER-STATED | IMPLEMENTED — `components/site/SiteHeader.tsx:52-95` | CONFIRMED |
| D-121 | **"How a blueprint is graded" comes off the site, and it is a redirect rather than an unlisting.** `/reading-the-radar` is deleted, `next.config.ts` 308s it to `/build`, and `/spec/scoring` is REPOINTED from it to `/build` rather than chained. | the owner's instruction, 2026-09-04; `next.config.ts`; `components/spec/sequence.ts:440-455` | OWNER-STATED, 2026-09-04 | IMPLEMENTED — `app/reading-the-radar/` and `components/learn/radar-page.test.ts` are gone; the route left `SPEC_SEQUENCE`, the header nav and the footer's Learn column, so the Learn sequence is six stops and the essay renumbered 06 → 05. Repointed and not chained because a 308 to a 308 costs every link written before §4.2 two hops, which `next.config.ts` records twice and which set the precedent at `/how-to-build-a-dark-factory`. ~~**`components/spec/ScoringModel.tsx` and its test are KEPT and mounted by nothing**~~ — the component, its test and the honesty ledger row that pinned it were **DELETED on 2026-09-05** on the owner's explicit instruction (D-140, §11.0 Q13). The reasoning recorded here was that deleting it would remove honesty assertions about something not being deleted in the same change, which is exactly the case `CLAUDE.md` gates behind an owner instruction; that instruction arrived. `#weights` was declared inside it and is now declared nowhere at all rather than merely being unreachable | CONFIRMED, and the open half is CLOSED: **what happened to `ScoringModel` was ruled on 2026-09-05 and is D-140** |
| D-150 | **`/spec/scoring` and `/reading-the-radar` are REPOINTED onto `/what-a-blueprint-is`**, with the honest note that no destination really answers what either URL was asking. | `next.config.ts` (the argument is in that file's own redirect header); `components/spec/sequence.ts:482-487`; [4 · Sitemap](architecture/routes.md)'s redirect table | AGENT-PROPOSED — the owner deleted `/build` (D-145) and did not name a new destination for the two 308s that landed on it, so the choice is the implementing session's | IMPLEMENTED. **Repointed rather than chained**, on D-121's own precedent: a 308 onto a route that itself 308s costs every link written before the merge two hops, which `next.config.ts` records twice and which set the precedent at `/how-to-build-a-dark-factory`. Pointing either at `/spec` would have bought exactly that. **The destination is a demotion rather than a shrug, and the redirect header says so in place**: the argument for `/build` was that it was the one surface left where a reader watched a score move, nothing on the site does that now, and therefore **no destination can answer the question either old URL was asking**. What survives of the grading is the vocabulary those scores were computed over, and `/what-a-blueprint-is` is where the Learn sequence starts and where that vocabulary is introduced — so a reader arriving on a bookmarked `/reading-the-radar` gets the beginning of the explanation rather than a 404 or a second hop. **`#weights` dies with the page either way**: `ScoringModel` owned that id, D-140 deleted it, and a fragment never reaches the server, so no redirect could ever have carried it. Four of the fourteen redirects now share one destination where two pairs shared two | PENDING-OWNER-REVIEW |
| D-126 | **The DarkPrint-to-Attractor crosswalk is published as a fourth `/spec` route.** | the owner's instruction, 2026-09-04; `app/spec/attractor/page.tsx`; `components/spec/crosswalk.ts`; `components/spec/sequence.ts:380-418` | OWNER-STATED, 2026-09-04 | **IMPLEMENTED since 2026-09-06, and this cell read `NOT-IMPLEMENTED` until then.** `app/spec/` holds four children now and `/spec/attractor` is stop 04 of the specification run. Every table on it renders from the constants the exporter itself reads, which is the ground the owner chose a page over an Attractor clause appended to each `/spec/card` field row: one URL you can hand a stranger, against a dozen sentences that each go stale on their own. `ATTRACTOR_SPEC_PIN` is PRINTED and never branched on, because a compatibility claim with no revision on it has no shelf life. **It takes 04 without being a fourth layer** — a blueprint is still three files, the three layer pages still say `Layer 0n of 03`, and the eyebrow reads `Compatibility` for that reason, since reusing the layer eyebrow would announce a fourth layer in the one word a reader scanning the header reads first. Taking 04 moved the practice run down a rung, which is what D-145 did to it from the other side. The material that landed on `/spec/topology` in the 2026-09-04 wave stays there: `condition` and `weight` were documented there for the first time, and `shape` is refiled as the handler selector §2.8 makes it rather than as Graphviz layout | CONFIRMED (the decision; the build landed 2026-09-06) |
| D-133 | **Three panels come off the node-card page — `Risk and autonomy` (`#evaluation`), `Identity` (`#identity`) and `Card source` — and its hand-rolled header takes the blueprint page's shape.** | the owner's instruction, 2026-09-05; `app/nodes/[...id]/page.tsx`; `components/nodes/VersionHistory.tsx:183-199` | OWNER-STATED, 2026-09-05 | IMPLEMENTED — the file goes 2166 to 1862 lines, and about 465 of those are the body de-indenting one step: the two removed panels emptied the `<aside>`, and a `lg:grid-cols-3` with a `col-span-2` main column and no third child leaves a third of a 1200px page permanently blank, so the grid collapsed to one full-width column. **The route publishes five in-page anchors now, not eight**: `#specification`, `#interfaces`, `#prohibitions`, `#fields`, `#version-history`. `#evaluation`, `#identity` and `#card-source` are gone, and the header's risk chip and a `seeHref` inside the field table both pointed at `#evaluation`, so both moved to `#fields`. **One fact was rescued rather than deleted with its panel**: the FULL card digest existed as text in exactly two places, the Identity panel and the Card source panel, and its own comment recorded that it had been moved out of a `title` tooltip because a tooltip is unreachable by keyboard and touch. It now renders as text in `VersionHistory`, so removing both panels did not silently revert an accessibility fix. `serveCardSource` is still called and its bytes still back the header's `Download card` data URL, so SEAM-10 and SEAM-22 stay LIVE | CONFIRMED as to the three panels and the header shape. **PENDING-OWNER-REVIEW as to the digest's new home**: keeping it was the agent's reading of the accessibility comment, not an instruction |
| D-134 | **The comment composer draws the viewer's own avatar to the left of the box**, and a viewer with no handle gets a mark that is visibly not a person. | the owner's instruction, 2026-09-05; `components/blueprint/Comments.tsx:318-356` | OWNER-STATED, 2026-09-05 | IMPLEMENTED — a `panel` row with a 32px (`md`) avatar in a fixed left column, matching the posted-note rows. With a handle it is the viewer's OWN identity through the same `Avatar` the notes use, initials from `displayName ?? handle` and hue from `avatarHue ?? hueFrom(handle)`; with no handle (signed out, or signed in before the handle step) it is an empty dashed 32px ring, `aria-hidden`, with no initials, no gradient and no glyph. `LiveNotes["viewer"]` gained optional `displayName` and `avatarHue`, read only to draw this. No endpoint, no seam and no change to what is posted or stored. **The viewer's own mark is deliberately not a link** where a posted note's is: a link on your own compose box sits one tab stop before your half-typed draft and offers only to navigate away from it | CONFIRMED as to the avatar. **PENDING-OWNER-REVIEW as to the signed-out case**: refusing to mint a placeholder person, rather than reusing a commenter's face or a generic glyph, is the agent's call and is pinned by two cells |
| D-135 | **The vocabulary browser's panels run the full width of `container-page`**, and every reading-measure cap in `components/ontology/**` comes off. (It read *"the `/ontology` panels"* until 2026-09-06; D-153 deleted that route and the rule followed the components onto `/spec/ontology`, and D-156 deleted that route later the same day and the rule followed them again onto `/spec/card`. The rule has outlived two of its own page references, which is why its enforcement site names no route at all.) | the owner's instruction, 2026-09-05; `components/ontology/full-width.test.ts`, which is the enforcement site and does not name a route at all | OWNER-STATED, 2026-09-05 | IMPLEMENTED — seven live `max-w-*` caps across three files (`OntologyCatalog` twice, `VocabularyBrowser` three times including a wrapper div whose only job was the cap, and `max-w-3xl` on the term page's lead) and eight `.prose-lane` applications, which are the same rule spelled through the design system rather than through Tailwind. The remaining `max-w-` strings in that folder are comments recording the removal, and `components/ontology/full-width.test.ts` reds when one comes back. The column templates did NOT need re-tuning at 1200px: the name track is a fixed `minmax(0,15rem)` and only the `1fr` description track absorbs the extra width, so the longest description the core ships goes from one line to two | CONFIRMED (the instruction). CONFIRMED (the instruction). ~~**Divergence reported rather than fixed, per `CLAUDE.md`**: `app/globals.css` still describes `--measure` / `.prose-lane` as *"One token, one class, one number, for every paragraph of body prose on the site"*.~~ **FIXED 2026-09-05 on the owner's ruling (D-142)**: that header now says the full container is the default and the measure is the exception, so the standing rule and the stylesheet agree |
| D-142 | **`app/globals.css` is corrected to say the full container is the default and `--measure` is the exception**, closing the divergence D-135 reported rather than fixed. | the owner's ruling, 2026-09-05 (§11.0 Q27); `app/globals.css:24-38`, `:170-172`, `:583-586` | OWNER-STATED, 2026-09-05 | IMPLEMENTED. The header block no longer calls `--measure`/`.prose-lane` *"one token, one class, one number, for every paragraph of body prose on the site"*; it names the measure as the exception, says THE DEFAULT IS THE FULL CONTAINER and that new work follows it, and lists the four occasions the owner asked for a cap to come off. The `SectionHeading` lead is still explicitly out of scope, which was a standing ruling and did not move. **`docs/ARCHITECTURE.md` §9.1, §9.4 rule 2 and the "divergence from a prior working note" paragraph were all rewritten against it in the same pass**, and the census was re-derived rather than carried: **eight** live applications, not the "~15 direct consumers" §9.1 claimed | CONFIRMED (the correction), and **the example list is now FIXED rather than pending (2026-09-05, §11.0 Q38)**. Nothing was put to the owner and nothing needed to be: the ruling was right and only its illustration was wrong, which is a measurement rather than a decision. The header now names the eight applications and the two routes that carry them, `/upload` (5) and `/spec/card` (3), says outright that the old list was wrong in BOTH directions, and carries the grep that re-derives it together with the reason a bare grep over-counts: about four in five hits are comments recording where a cap came off. **The IMPLEMENTED cell above was itself half true when it was written**, and that is the finding worth keeping: the sentence *"one token, one class, one number, for every paragraph of body prose on the site"* had TWO copies in `app/globals.css`, the header block and the `.prose-lane` rule's own comment. D-142 corrected the first and left the second standing for a day, which is how a claim survives its own retraction |

## 6 · `/build` and the authoring path

> **The route this section is named for was deleted on 2026-09-06 (D-145), and the heading
> is deliberately not renamed.** Section numbers and names are how every citation of this
> ledger addresses a row, and renaming a section to tidy it breaks the citations without
> changing a decision. D-47 to D-53 record what `/build` was FOR and are the record of a
> route that existed; D-127 is about `/upload` and always was. Read the section as the
> authoring path, of which the sandbox was one stop.

| ID | Decision in one sentence | Source path and line | Provenance | Verifiable in code | Status |
|---|---|---|---|---|---|
| D-47 | The build objective is to show a user how to create a blueprint, as a breakdown they can follow *and* hand to their own Claude Code, Gemini or Codex — supported by core animations and no dense text. | `docs/superpowers/specs/2026-08-04-ia-redesign.md:308-311` and `docs/superpowers/specs/2026-08-06-build-restructure-design.md:30-32` (owner quoted, twice) | OWNER-STATED | IMPLEMENTED, but not at `/build` — the creation entry is `app/skill/page.tsx` ("Assisted Design") and the agent-facing half is the bundle `AGENTS.md`; `/build` is an optional worked example (`components/spec/sequence.ts:368-378`) | CONFIRMED |
| D-48 | The deliverable of `/build` is a folder an agent can act on, not a blueprint the reader has studied. | `docs/superpowers/specs/2026-08-06-build-restructure-design.md:22-23,34-35` | AGENT-PROPOSED (the spec's reading of the D-47 quotation) | IMPLEMENTED — `lib/content/bundle-export.ts:68` writes `AGENTS.md` into every download | PENDING-OWNER-REVIEW |
| D-49 | `/build`'s eight-step guided path is replaced by one workspace — one graph as the stage, three simultaneous controls, five tabs, two co-equal exits — without changing a byte of the bundle any set of choices produces. | `docs/superpowers/specs/2026-08-06-build-restructure-design.md:142-176,193-235`; `docs/superpowers/plans/2026-08-06-build-workspace.md:5,13-14` | AGENT-PROPOSED | IMPLEMENTED — `components/build/BuildWorkspace.tsx`, `WorkspaceStage.tsx`, `ALL_COMBINATIONS` at `components/build/choices.ts:150`; no `GuidedPath.tsx`, `steps.tsx` or `path-state.ts` | PENDING-OWNER-REVIEW |
| D-50 | Every one of the 80 pre-resolved combinations must resolve through the real engine, and one error-severity diagnostic fails the build. | `docs/superpowers/specs/2026-08-06-build-restructure-design.md:221-223`; `docs/superpowers/plans/2026-08-06-build-workspace.md:14`; `files/darkprint-onboarding-positioning.md:225` | UNATTRIBUTED | IMPLEMENTED — `app/build/page.tsx` walks `ALL_COMBINATIONS` through `loadBundle` at build time | PENDING-OWNER-REVIEW |
| D-51 | `/build` is renamed to what it is — an optional worked example, "Customize the starter" — rather than being the general blueprint designer. | `sol_feedback.md:232-233` (offered as the *lower-cost alternative* to `sol_feedback.md:229-231`'s preferred option); `IMPLEMENTATION_PLAN.md:21-22` | AGENT-PROPOSED | IMPLEMENTED — `components/spec/sequence.ts:368-378` | PENDING-OWNER-REVIEW |
| D-52 | Every bundle carries an agent-facing `AGENTS.md` whose generated half always exists, so nobody is blocked from publishing. | `docs/superpowers/specs/2026-08-04-ia-redesign.md:335-337` ("Approved, 2026-08-04"), `:389-391` | OWNER-STATED (Q1) | REVERSED, 2026-08-25 — `lib/content/bundle-export.ts` no longer writes `AGENTS.md` into any bundle; the constant and its old line numbers (`:68,430`) are gone from the generator. ~~`BUNDLE_AGENTS` stays exported only because two FROZEN test suites (`components/blueprint/download-name.test.ts`, `components/bundle/files.test.ts`) import the name~~ — **false since 2026-08-30**: both suites dropped the import, and `BUNDLE_AGENTS` survives on a real product consumer, `components/upload/BundleDropzone.tsx`, which reads it so a reader who drops an `AGENTS.md` from an older download is told what the file is. `FACTORY_DOT`, which had no such consumer, is deleted (D-104). The reversal of D-52 itself is unchanged | REVERSED (OWNER-STATED, 2026-08-25 — "remove `factory.dot` and `AGENTS.md` from the blueprint folder entirely. A published blueprint folder must contain only: `topology.dot`, `cards/*.yaml`, `README.md`, and `ontology/extensions.yaml` when the bundle uses local terms."); CONFIRMED from `2026-08-04` until then |
| D-53 | The author-written half of a bundle's `AGENTS.md` is optional, prompted at upload, and shown as a quality signal on the blueprint page. | `docs/superpowers/specs/2026-08-04-ia-redesign.md:355-356` (owner quoted: "the AGENTS.md should be provided by a user once uploading a blueprint."), `:391-392` | OWNER-STATED | NOT-IMPLEMENTED — `components/upload/UploadFlow.tsx` has no `AGENTS.md` prompt, and no blueprint surface renders one as a quality signal | CONFIRMED |
| D-127 | **An Attractor pipeline can be imported in the browser, on `/upload`.** | the owner's instruction, 2026-09-04; `lib/core/attractor/import.ts`; `packages/cli/src/import.ts` | OWNER-STATED, 2026-09-04 | **NOT-IMPLEMENTED.** Nothing under `components/upload/**` or `app/upload/**` reaches `importAttractorDot`. Recorded as owed at `docs/ARCHITECTURE.md` §11.0 Q20 rather than described anywhere as done. The engine half is shipped and isomorphic — `importAttractorDot` is `lib/core`, so it already runs in a tab — and `darkprint import` drives it from the CLI, so what is missing is a surface rather than a capability | CONFIRMED (the decision; the build is owed, not the ruling) |
| D-145 | **`/build` and `components/build/**` are DELETED**, and the tree-wide copy guard that happened to live in that suite MOVES rather than going with them. | the owner's instruction, 2026-09-06: *"it is not useful and make confusion"*; `components/spec/sequence.ts:31-47`, `:427`, `:482-487`; `components/site/copy-rules.test.ts:1-24`; `next.config.ts` | OWNER-STATED, 2026-09-06 | IMPLEMENTED. `app/build/` and the whole `components/build/` tree are gone. `SANDBOX` left `SPEC_SEQUENCE`, so the practice run is one stop and the sequence is six: the door, the three layers, the Attractor crosswalk and the essay. The essay takes 05 the way it took 05 when the graded page went, and for the reason recorded there — a sequence with a hole where a number should be tells a reader they missed a page. The two 308s that landed here were repointed rather than chained (D-150). **What did NOT go with it, and what nearly did**: `components/build/workspace.test.ts` held doc 2 §2.5's punctuation rule and §1.1's evaluative-language rule over directory walks that reach `components/`, every route's `page.tsx` and two files in `lib/`, and neither rule was ever about this route. Both moved WHOLE into `components/site/copy-rules.test.ts`, where the site-wide guards already live. **A walk that stops running reports nothing rather than failing**, so deleting a route would otherwise have taken the site's punctuation rule with it, silently. What correctly went with the subject: the eighty-combination enumeration, the score arithmetic, the two exits' copy and the README round trip. **`lib/starter/` survives with no product mount at all**, and SEAM-99's anchor sits in it — all four workspace seams are retired in place in [8 · Seams](architecture/seams.md) §N, and that section says why the row a live anchor names must not be deleted | CONFIRMED |

## 7 · Evidence, scores and community signals

| ID | Decision in one sentence | Source path and line | Provenance | Verifiable in code | Status |
|---|---|---|---|---|---|
| D-54 | "Static risk exposure" is the public label for the structure-derived risk reading, and it is never represented as a security audit. | `IMPLEMENTATION_PLAN.md:46-47`; `sol_feedback.md:308-310` | AGENT-PROPOSED | IMPLEMENTED — `lib/content/view.ts:202`, `components/ui/ScoreRadar.tsx:45`, `components/spec/ScoringModel.tsx:264`, and eleven other surfaces | PENDING-OWNER-REVIEW |
| D-55 | Stars are the public support metaphor, with a control and count beside every blueprint and node-card title, counts visibly seeded and starring browser-local. | `IMPLEMENTATION_PLAN.md:36-37,54-55`; commit `6e9d0ac` | AGENT-PROPOSED | IMPLEMENTED — `components/ui/FavoriteStar.tsx`, `components/ui/community-support.test.ts` | PENDING-OWNER-REVIEW |
| D-56 | Structural evidence, community assessment and run evidence are three distinct layers with visible provenance, and a missing value is a real product state rather than something to fill. | `IMPLEMENTATION_PLAN.md:26-28`; `sol_feedback.md:282-285,296-306` | AGENT-PROPOSED | WAS IMPLEMENTED at `components/blueprint/EvidenceLayers.tsx:10-45`, **and has no surface since 2026-09-04**: that panel was the three-layer reading, and it was deleted with the rest of the blueprint page's scoring block on the owner's instruction. The three layers survive as a shape in `SEAM-06` and in `lib/content/view.ts`'s `MetricSource`, and no page draws them | PENDING-OWNER-REVIEW |
| D-57 | Popularity sorting stays out until the event semantics behind it are defined. | `IMPLEMENTATION_PLAN.md:61-62,89-91`; `sol_feedback.md:279-281`; `docs/superpowers/specs/2026-08-04-ia-redesign.md:262-264` | AGENT-PROPOSED | IMPLEMENTED — `components/ui/autonomy-surfaces.test.ts:252` ("does not offer popularity sorting until event semantics are defined") | PENDING-OWNER-REVIEW |
| D-58 | Sixteen product policies — publishability, fork identity, blueprint versioning, privacy, trust signals, run normalisation, ontology governance, the MCP retrieval contract, licensing, malicious-bundle boundaries, account roles, event semantics, ranking, moderation, retention, the star service — are deliberately left open, and the UI may not imply any of them. | `IMPLEMENTATION_PLAN.md:61-91`; `sol_feedback.md:762-794` | AGENT-PROPOSED | IMPLEMENTED as a restraint — no surface asserts any of the sixteen; `app/mcp/page.tsx:163` marks the retrieval contract a proposal | PENDING-OWNER-REVIEW |
| D-59 | Cost and time are reported by whoever ran the blueprint, never measured by the platform, and the card shows the number of runs and the dispersion rather than only a mean. | `files/darkprint-design.md:268-274` | AGENT-PROPOSED (Q2/Q3: doc 1 §8) | NOT-IMPLEMENTED — there is no run-count or dispersion surface; `lib/data/community.ts:51` carries a single seeded stand-in for the cost/time axis | PENDING-OWNER-REVIEW |
| D-120 | **The whole scoring reading comes off the canonical blueprint page, and the UX is simplified as far as it will go.** The Score panel (`ScoreRadar` + `MetricBars`), the ballot, the header autonomy meter, the explainability panel and the evidence layers all leave `/blueprints/{owner}/{slug}`. | the owner's instruction, 2026-09-04; `app/blueprints/[owner]/[slug]/page.tsx` (each removal commented in place at the position it stood) | OWNER-STATED, 2026-09-04 | IMPLEMENTED — the rail went from seven rows to six and `#evidence` is gone; `components/bundle/VoteControl.tsx`, `components/ui/MetricBars.tsx` and `components/blueprint/EvidenceLayers.tsx` with its two tests are DELETED, and the autonomy meter also left `ContentCard` and `Pinned`. **`lib/core/analysis/**` is untouched**: the registry still computes and stores a scorecard, and `scoresOf` still answers the page's 404 gate and builds `bp.analysis`. ~~`GET`/`POST .../votes` is still LIVE.~~ **It is not: the route and the ballot write path were deleted on 2026-09-05 (D-141).** What changed is that no page renders any of it. Every honesty disclaimer removed was attached to a control deleted in the same change, which is the one condition `CLAUDE.md` allows; the page's own `Use this blueprint` disclosure, which is a claim about the product rather than about the panel, is unchanged and still ships | CONFIRMED (the removal). **The open half is ANSWERED IN PART on 2026-09-05 (D-141)**: the votes route and the ballot write path were put to the owner and deleted. Two of the three remain open — `lib/server/ballot/`'s two surviving TYPES and `blueprintViewOver`'s now-unread `metrics` array are one question, since the types exist only to declare the field, and §11.0 Q34 carries it |
| D-132 | **The blueprint header carries GitHub's trio — `Watch`, `Fork`, `Star` — and `Save` folds into `Star`**, so a signed-out reader can no longer keep a blueprint. | the owner's instruction, 2026-09-05; `components/ui/ActionPill.tsx`; `components/ui/FavoriteStar.tsx:24-42,463-529`; `components/bundle/BundleHeader.tsx:24-49` | OWNER-STATED, 2026-09-05 — *"starring is the gesture, and the saved shelf reads stars"*, recorded in `FavoriteStar`'s own header | IMPLEMENTED. Four controls became three: `Get the folder` left the header (the file list's `Code` dropdown is the download, so one place fetches a bundle and it sits beside the files) and `Save` merged into `Star`. Each control is a split pill through the new `components/ui/ActionPill.tsx` — a label half and a mono figure half through `compact()`, a cyan `active` state, and drawn-and-disabled with a `title` naming what is missing for every switched-off one. Signed out, `Star` is disabled with *"Sign in to star this. A star is also what keeps it on your saved list."* rather than silently doing nothing, which is the accepted cost of the fold stated where it bites. A seeded `count` still draws the same pill switched off, keeping its `◐` and its disclaimer, because D-78 moves a marker one way and the fold changed how many controls a reader sees rather than whether a number is real | CONFIRMED as to the fold and the trio. **PENDING-OWNER-REVIEW as to the mechanism that keeps the shelf alive**: `lib/server/counters` publishes `getSignals`/`getSignalsMany` over targets the caller names and has no per-account reader, so *the saved shelf reads stars* is not implementable literally today. What ships instead is one click writing both stores for a CARD — the counter through `star.api` and the account save through `POST`/`DELETE /api/account/saves` — so the `save` row is the shelf's index of stars. That is the agent's choice, not the owner's. Two consequences ride on it and are §11.0 Q23 and Q24: `Remove` on the Saved tab deletes the save and leaves the star standing, and a blueprint star still reaches no shelf at all (D-262-04's gap, unchanged) |
| D-146 | **The blueprint page loses `Watch`, its long description and its Visibility panel; the graph runs the full container width, and the header row is star, fork, download.** | the owner's instruction, 2026-09-06, quoted at each removal site; `components/bundle/BundleHeader.tsx:28-40`, `:237`; `app/blueprints/[owner]/[slug]/page.tsx:144`, `:324-331`, `:404-410`, `:706-716`; `components/profile/OwnedBundles.tsx:54-56`; `components/graph/framing.ts` | OWNER-STATED, 2026-09-06 — four instructions in the owner's own words, each recorded at the position it acts on: *"remove the Code button and move on top right on the side right of the Star; the order should be: star, fork, download blueprint"*, *"remove the description in the blueprint template above the panel that lists the files"*, *"move on that part the The graph panel (extend full horizontal length as the other elements)"*, and *"remove the panel visibility from the blueprint card; such option should be visible only on the user account list of the blueprints"* | IMPLEMENTED, and the four removals are load-bearing on each other rather than four separate edits. **`Watch` left the band and `components/bundle/WatchButton.tsx` was DELETED**: `/api/authors/{handle}/watch` follows the PERSON who owns the bundle, so a pill on a bundle's band offered to subscribe a reader to something they were not looking at. The route is untouched and `components/profile/SocialControls.tsx` still draws the real control on the profile it addresses, which is why SEAM-57 keeps its status and only loses two anchors. **`Get the folder` came BACK as the `download` slot**, after one pass as a `Code` dropdown on the file list's header row. **The Visibility panel MOVED rather than being deleted** — `RowVisibility` in `components/profile/OwnedBundles.tsx`, one control per row on `/u/[username]`, plus the surviving `DraftLanding` mount for a bundle with no release — so SEAM-67 keeps its status and changes its surface, and the published view now renders the same thing for the owner and for a stranger. **That move is what emptied the `<aside>`, which is what let the two-column body grid go, which is what let the graph take the full container width**: 729px of canvas to 1124px, and six of the nine archive drawings are legible against the 10px floor where one was before. `framing.ts` lost `BODY_GRID_GAP`, `BODY_GRID_COLUMNS`, `GRAPH_COLUMN_SPAN`, `COLUMN_FROM` and `columnCanvasWidthAt`; `canvasWidthAt` is the only canvas expression left and has no breakpoint in it. `#blueprint-source` came off in the same wave and `DotBreakdown` keeps every other mount | CONFIRMED |
| D-137 | **The marketing and Learn copy is audited for scoring terminology after the feature's removal**, and the audit's boundary is drawn at what the code actually still renders. **CORRECTED 2026-09-06: this row's own counter-example is deleted, so the sentence it falsified is true again of everything except `/upload`.** | the owner's instruction, 2026-09-05; ~~`components/build/ScorePanel.tsx:291`~~ (deleted with `/build`, D-145); `components/upload/ValidationReport.tsx:320` | OWNER-STATED, 2026-09-05 (the instruction to audit). The per-sentence judgements below are the agent's | IMPLEMENTED IN PART, and the brief's own premise was falsified first. *Nothing renders a scorecard* was FALSE against the tree when this row was written: `components/build/ScorePanel.tsx:291` drew `<ScoreRadar>` behind `/build`'s Score tab (mounted from `WorkspaceStage.tsx`) and `components/upload/ValidationReport.tsx:320` draws the autonomy block and the security ledger (mounted from `UploadFlow.tsx:1647`). Both read a graph the READER is holding rather than one the registry published, so both routes' scoring copy was true and was left standing; stripping it would have replaced one false statement with its opposite. **On 2026-09-06 the owner deleted `/build` and its component tree (D-145), and half of that counter-example went with it.** The premise is now TRUE of everything except `/upload`: `ValidationReport` is the last mount that draws a reading, it still reads the reader's own graph, and its copy still stands on that ground alone. **So the boundary this row leaves open NARROWS rather than closing.** What was cut is copy about a reading the registry no longer publishes. **What was deliberately kept, with the argument each still makes**: `components/home/SectionSameRun.tsx`'s score ledger is the READER's own harness output and is the landing's reproducibility argument; `components/explain/RunLayers.tsx` and `RunSystemMap.tsx` define the eval world a blueprint is handed to, not anything DarkPrint does; `WhichTasksGlance.tsx:362` (*"These are not points. Three out of four is not a score"*) is a disclaimer refusing a score and gets stronger with the feature gone | CONFIRMED (the instruction to audit). **PENDING-OWNER-REVIEW as to the boundary, NARROWED 2026-09-06**: whether `/upload` is meant to lose its scoring too was not put to the owner, and the sentences left standing on that route are true only while that one mount is. `/build` answered its half by being deleted rather than by being ruled on. The residue is §11.0 Q28 |
| D-140 | **`components/spec/ScoringModel.tsx`, its test and the honesty ledger row that pinned it are DELETED**, and one claim stops being made anywhere in the repository as a result. | the owner's explicit instruction, 2026-09-05 (§11.0 Q13, closing Q29 with it); `components/site/honesty.test.ts`; `tests/server/t260/frozen-tests.test.ts` | OWNER-STATED, 2026-09-05 — `CLAUDE.md` gates deleting an honesty assertion behind an explicit owner instruction, and this is that instruction | IMPLEMENTED. **What stopped being said, verbatim: *"Nothing on this site measures a run. These two filters describe a design."*** It was written in exactly one place, that component's telemetry paragraph, and it is still TRUE — there is no runner, and `submitReport` (`lib/server/runs/write.ts:89`) accepts a caller's report on well-formedness alone. No page had printed it since `/reading-the-radar` was deleted on 2026-09-04 (D-121). **NEAREST SURVIVING STATEMENT, weaker and on one route**: `/upload`'s *"Cost and time are reported by whoever runs the graph. The platform never sees the execution."* and the metric legend's *"Cost / time is reported by whoever runs it."* **The deleted ledger row was VACUOUS at removal rather than load-bearing** — since 2026-09-04 it had been asserting that a claim was readable *in the open* on a route no reader could reach, and passing. That is what §11.0 Q29 was, and the lesson is recorded at the removal site: **a ledger row pinned over a COMPONENT rather than a route cannot tell *still disclosed* from *disclosed to nobody*, and several rows in `honesty.test.ts` are still shaped that way.** `frozen-tests.test.ts`'s pin over that file was re-pinned a fourth time, recording that this amendment differs in kind from the three before it | CONFIRMED (the deletion). **PENDING-OWNER-REVIEW as to the honesty position it leaves**: the site no longer states anywhere that nothing on it measures a run, and whether the surviving `/upload` pair is enough was not put to him. §11.0 Q36 |
| D-141 | **The votes route and the ballot write path are DELETED, and the three promises that rode on them go with them.** | the owner's instruction, 2026-09-05 (§11.0 Q14); `app/api/blueprints/[owner]/[slug]/votes/**`; `lib/server/ballot/`; `app/settings/page.tsx` §05; `components/upload/UploadFlow.tsx` | OWNER-STATED, 2026-09-05 | IMPLEMENTED. `GET`/`POST /api/blueprints/{owner}/{slug}/votes` and its scratch test are gone; `lib/server/ballot/` keeps `index.ts` and `types.ts`, publishing `Aggregate` and `MetricAggregate` and **zero functions and zero error classes**, so `error-hygiene` goes 47 to 45. **The ruling's conditional resolved against the read half too**: `getAggregate`'s only callers were the deleted route and `tests/server/t160`, and the blueprint page had already dropped the read on 2026-09-04, so `aggregate.ts` and `read.ts` went with the writer. **`tests/server/t160/**` was deleted with its subject — eleven files, 3501 lines** — and nothing is lost: its ten surviving cells were about the `ballot` TABLE and `tests/server/t005/ballot.test.ts` covers each more strictly. **The `ballot` TABLE stays**, because `lib/server/lifecycle/bundle-deletion.ts:96` cascades through it. **Three promises removed, each with the control it qualified**: `/settings` §05's *"Casting a ballot happens on a blueprint's own page…"* (also in that page's `<meta name="description">`), its `· weight ×N on community metrics` line, and `/upload`'s *"Efficacy, reliability and transparency come from weighted community and validator votes."* in two places. **THE VALIDATOR BADGE SURVIVES THE WEIGHT**: a weight asserts an effect on a number and there is no number left, a badge asserts the registry granted this account a status and that stayed true. **This does not re-litigate D-WAVE-08** (*"the weight is the lever, the badge is not"*) — it is that ruling's consequence once the lever is gone, and it is written down so nobody restores the weight to `/settings` citing D-WAVE-08 | CONFIRMED (the deletion, the write path, and the three promises). **PENDING-OWNER-REVIEW as to what is left**: `lib/server/ballot/`'s two types exist only for `lib/content/view.ts`'s `live.aggregate`, which now has no producer at all, and whether that field and that folder should exist was not put to him (§11.0 Q34) |
| D-143 | **Two surfaces mounted by nothing are DELETED — `components/blueprint/AttractorCompatibility.tsx` and the `Forks` export in `components/bundle/Aside.tsx` — and the lint coverage MOVES rather than going with them.** | the owner's instruction, 2026-09-05 (§11.0 Q30); `lib/core/attractor/lint.test.ts`; `components/bundle/Aside.tsx` | OWNER-STATED, 2026-09-05 | IMPLEMENTED. Both had been off the page since 2026-09-04 (D-120, and the owner's *"remove the fork panel from blueprint"*). `Aside.tsx` goes 399 lines to 343 and six exports to five, and `blueprintHref` left its imports with the panel, which is how the deletion is known to be complete rather than half-attached. **The `attractor/condition-syntax` cells moved to `lib/core/attractor/lint.test.ts` and now assert on `lintAttractor` directly, in BOTH directions.** The falsification the ruling prescribed — delete rule 7 — is one-directional by construction: it reds the positives and leaves a `not.toContain` green, because a rule that never fires satisfies a negative. Two further mutations were run for that reason: rule 7 firing on every edge (49 reds, the negative among them) and rule 7 leaking onto a bad node id (5 reds, of which the negative cell is the only relocated one). **The severity did NOT move** and is still `warning` (D-129). **What stopped being said**: `Aside.tsx`'s *"Public forks only. A private fork is never announced here…"*, whose property is enforced by `forksOf` rather than by the sentence, and `AttractorCompatibility`'s open limit block, still stated on `/spec/topology` (`app/spec/topology/page.tsx:217`, `components/spec/rows.ts:66`) | CONFIRMED (both deletions and the move). **D-129's priced cost is DISCHARGED BY DELETION rather than by rewriting**: its evidence column named *"`AttractorCompatibility.tsx`'s 'None of these stops a runner' is now false and has not been rewritten"*, and Q16 rewrote the sentence hours before Q30 deleted the file |

## 8 · The authoring skill

| ID | Decision in one sentence | Source path and line | Provenance | Verifiable in code | Status |
|---|---|---|---|---|---|
| D-60 | The skill writes files and nothing else: it runs no graph, starts no server, publishes nothing, and cannot score the bundle. | `skills/darkprint/SKILL.md:3,22-27`; `skills/darkprint/references/preflight.md:3` | AGENT-PROPOSED (Q5: the owner has not reviewed the skill's contents) | IMPLEMENTED — the skill ships as documents only; `lib/core` is not on the author's machine | PENDING-OWNER-REVIEW |
| D-61 | The skill does not emit `factory.dot`; DarkPrint's own exporter compiles it, so the emit rules cannot drift in two places. | `skills/darkprint/SKILL.md:23`; `skills/darkprint/references/dot-and-attractor.md:5,106` | AGENT-PROPOSED (Q5) | IMPLEMENTED — `lib/core/attractor/emit.ts` is the only emitter | PENDING-OWNER-REVIEW |
| D-62 | The skill interviews rather than fills in a form: one question at a time, a recommended answer with every question, facts looked up and only decisions asked, and no file written until the author confirms. | `skills/darkprint/SKILL.md:51-69` | AGENT-PROPOSED (Q5) | UNVERIFIABLE (a rule about agent behaviour, not about this codebase) | PENDING-OWNER-REVIEW |
| D-63 | The "skill for learning to use a dark factory" is this tutorial repackaged for Claude, not a separate artefact. | `files/darkprint-onboarding-positioning.md:293` | AGENT-PROPOSED (Q2/Q3: doc 2 §5.8) | CONTRADICTED — `skills/darkprint/SKILL.md` is a blueprint-authoring interview with its own five references and two templates, sharing only the criteria-isolation example with the `/build` tutorial | PENDING-OWNER-REVIEW |
| D-64 | Two reference files are generated from engine source and test-locked; when a reference disagrees with memory, the reference is right. | `skills/darkprint/references/ontology.md:2-5`; `skills/darkprint/references/card-schema.md:2-3`; `skills/darkprint/SKILL.md:44` | AGENT-PROPOSED (Q5) | IMPLEMENTED — `scripts/generate-skill-refs.ts`, `scripts/skill-refs.ts`, `scripts/generate-skill-refs.test.ts` | PENDING-OWNER-REVIEW |
| D-65 | The documented way to get the skill is `npx skills@latest add Brotherhood94/darkprint`, and a third-party skill vendored under `.claude/skills/` is handed to strangers as ours unless it is locked out. | `AGENTS.md:9-13,34-39` | UNATTRIBUTED | IMPLEMENTED — no `.claude/skills/` in the tree, no `skills-lock.json`, and `eslint.config.mjs` carries no `.claude/skills/**` ignore | PENDING-OWNER-REVIEW |
| D-86 | This repository ships one skill of its own, `skills/darkprint/`, created deliberately — but as a test, and it needs to be made concretely useful. | Owner, 2026-08-12 (answer Q4: "yes. But I just left there as a test. I need to make it concrete useful"); `AGENTS.md:9-10` | OWNER-STATED | IMPLEMENTED — `skills/darkprint/` with five references and two templates; the contents are unreviewed (Q5) | CONFIRMED |
| D-87 | Skills that push a line of development the owner has not chosen are disabled rather than followed. | Owner, 2026-08-12 (answer Q13: "yes. I felt that some skills where forcing me to stay on a path of developement that I was not sure"); `.claude/settings.local.json` `skillOverrides`; commits `be76e32`, `3ceff81`, `5b92067` | OWNER-STATED | IMPLEMENTED — ~19 skills disabled locally; both vendored skills and `skills-lock.json` deleted 2026-08-11 | CONFIRMED |
| D-139 | **The skill's install command is qualified on every surface that prints it, and the qualification says the REPOSITORY is private rather than that the skill is unfinished.** | the owner's instruction, 2026-09-05 (§11.0 Q8); `lib/skill.ts:81`; `components/skill/SkillSetup.tsx`; `components/build/AgentHandoff.tsx`; `components/bundle/DraftLanding.tsx`; `components/hero/SetupChips.tsx`; `app/skill/page.tsx`; `app/capabilities/page.tsx` | OWNER-STATED, 2026-09-05 | IMPLEMENTED on all six surfaces, plus `app/skill/page.tsx`'s `metadata.description` as a seventh, because that is the sentence a reader acts on from a search result without opening the tab. **The distinction the ruling turns on is real and was measured**: `Brotherhood94/darkprint` is private and `api.github.com` answers 404 unauthenticated, so the command fails for every reader but its owner — which is not the same claim as the skill being unfinished, and `/capabilities`' own copy had already been careful about it. **A `ComingSoonBadge` is impossible on two of the six and that is forced rather than chosen**: `components/hero/Wordmark.test.ts` FORBIDS the string "coming soon" on both landing-band cells (the case was inverted rather than deleted in 2026-08-12, precisely to catch a badge being reinstated), and `components/skill/SkillSetup.test.ts` pins `/skill` at exactly one coming-soon marker, below the page's rule. Both carry the qualification in words instead, which is the doctrine both files cite. `app/skill/page.tsx`'s *"The DarkPrint skill installs today"* was REMOVED rather than qualified, and a new inverted assertion forbids any surface saying it — added because reverting `AgentHandoff`'s label alone reddened nothing: a surface can carry the qualification AND a sentence contradicting it | CONFIRMED (the qualification and its wording). **PENDING-OWNER-REVIEW as to the landing band's colour**: `SetupChips`' skill cell was emerald, which that file's own docblock reserves for "the command that runs today", so it takes `/mcp`'s blue register and no cell on the band is emerald now. Dropping a claim made in colour was the agent's reading of the instruction, not part of it |

## 9 · Working rules addressed to agents

| ID | Decision in one sentence | Source path and line | Provenance | Verifiable in code | Status |
|---|---|---|---|---|---|
| D-66 | Read `node_modules/next/dist/docs/` before writing any route or config code, because this Next.js has real breaking changes. | `AGENTS.md:4`; `PROJECT.md:311-312`; `architecture/website.md:7-8`; `docs/superpowers/plans/2026-08-06-build-workspace.md:15` | UNATTRIBUTED | UNVERIFIABLE (a working instruction) | PENDING-OWNER-REVIEW |
| D-67 | Never run prettier: this repo has no prettier config and it reflows to 80 columns, producing a spurious diff. | `docs/superpowers/plans/2026-08-06-build-workspace.md:21` | UNATTRIBUTED | IMPLEMENTED — no prettier config or dependency in `package.json` | PENDING-OWNER-REVIEW |
| D-68 | Never delete or loosen an assertion about accessibility, contrast, label overlap or an honesty disclaimer. | `docs/superpowers/plans/2026-08-06-build-workspace.md:22,104,448` | UNATTRIBUTED | IMPLEMENTED as practice — but see D-16, where the owner's own instruction removed one honesty disclaimer and both its guards | PENDING-OWNER-REVIEW |
| D-69 | Verify agent and tool reports before trusting them, and run the gates yourself. | `PROJECT.md:319-322`; `docs/superpowers/specs/2026-07-31-content-reorg-design.md:173-175` | UNATTRIBUTED (`PROJECT.md:140`'s first-person "Ordered by my read of the value" is the closest thing to an owner voice in that file, and whose voice it is cannot be established) | UNVERIFIABLE | PENDING-OWNER-REVIEW |
| D-70 | When you add a guard, falsify it: break the code on purpose, watch the test fail with the right message, restore. | `PROJECT.md:323-325`; `architecture/engine.md:197-199` | UNATTRIBUTED | UNVERIFIABLE | PENDING-OWNER-REVIEW |
| D-71 | Measure prose, not pixels and not raw word count, and rank by site-wide words (per page × instances). | `PROJECT.md:149-157`; `docs/superpowers/specs/2026-07-31-content-reorg-design.md:45-46` | UNATTRIBUTED (the originating complaint, "too much information that can only distract a user", *is* an owner quotation at `2026-07-31-content-reorg-design.md:13`, but the metric is the agent's answer to it) | IMPLEMENTED — `scripts/measure-prose.ts`, `scripts/measure-prose.test.ts`, `npm run measure:prose` | PENDING-OWNER-REVIEW |
| D-72 | Never take a `fullPage` screenshot: Chrome grows the viewport to document height and every `100vh` block balloons into a layout no reader ever sees. | `docs/superpowers/plans/2026-08-06-build-workspace.md:336` | UNATTRIBUTED | UNVERIFIABLE (a tooling instruction) | PENDING-OWNER-REVIEW |
| D-73 | Comments explain *why*, citing the doc section or the defect that forced the decision; a comment restating the code is noise. | `PROJECT.md:313-315` | UNATTRIBUTED | IMPLEMENTED — the convention is followed throughout `app/`, `components/` and `lib/` | PENDING-OWNER-REVIEW |
| D-74 | No em dash as a pause in `components/home`, and no AI-writing patterns ("not X, but Y", rhythmic triplets, empty emphasis) anywhere in the site's text. | `docs/superpowers/plans/2026-08-10-landing-reproducibility-beat.md:17`; `files/darkprint-onboarding-positioning.md:131` | AGENT-PROPOSED (Q2/Q3: doc 2 §2.5) | IMPLEMENTED — the guard is in `components/build/workspace.test.ts`, not in `components/home` as `2026-08-10-landing-reproducibility-beat.md:17` states | PENDING-OWNER-REVIEW |
| D-75 | Cyan is interactive, violet is where a person acts, emerald is a figure read off the engine, signal is a defect, and amber is "not built yet". **AMENDED 2026-09-06 (D-151): amber is also the CARD register, and cyan is the blueprint register.** The first four clauses are untouched. | `docs/superpowers/plans/2026-08-06-build-workspace.md:18`; `app/globals.css:411`; the owner's instruction, 2026-09-06 | UNATTRIBUTED as to the original. **OWNER-STATED, 2026-09-06, as to the amendment** | IMPLEMENTED — `app/globals.css`'s accent block, which now states amber's three jobs and marks `--color-copper-line`/`--color-copper-ink` deprecated in place rather than deleting them, because Tailwind emits a `@theme` colour's utilities only while it can see one used | PENDING-OWNER-REVIEW as to the original. **The amendment is CONFIRMED (D-151)** |
| D-76 | The canonical spacing tiers are 8/12/16/20/40/64/80-112, and 48, 56, 32 and 96 are banned. | `docs/superpowers/plans/2026-08-06-build-workspace.md:17` | UNATTRIBUTED | NOT-IMPLEMENTED as a rule — no test, lint rule or Tailwind theme restriction holds it; it survives only as a sentence in one plan | PENDING-OWNER-REVIEW |

## 10 · The backend, if it is ever wanted

| ID | Decision in one sentence | Source path and line | Provenance | Verifiable in code | Status |
|---|---|---|---|---|---|
| D-77 | There is no backend: no accounts, no publishing, no push, no votes, no telemetry, no MCP server. | `PROJECT.md:131-132`; `README.md:47`; `skills/darkprint/SKILL.md:391` | UNATTRIBUTED (a statement of the current build, restated as a constraint) | IMPLEMENTED — and now partly stale: account *surfaces* exist as seeded presentation (D-45), which `PROJECT.md:131-132` and `README.md:47` do not mention | PENDING-OWNER-REVIEW |
| D-78 | The moment any backend feature lands, every seeded marker and every disclaimer has to be revisited in the same change. | `PROJECT.md:293-295`; `app/settings/page.tsx:44-47` | UNATTRIBUTED | IMPLEMENTED as a recorded precondition — `app/settings/page.tsx:44-47` names it | PENDING-OWNER-REVIEW |
| D-79 | Supabase covers auth, database and storage; no separate service for each. | `files/darkprint-design.md:357` | AGENT-PROPOSED (Q2/Q3: doc 1 §9.3 "Decisione" — decided by whoever wrote doc 1, not by the owner) | NOT-IMPLEMENTED — no Supabase dependency in `package.json` | PENDING-OWNER-REVIEW |
| D-80 | Immutable content lives in object storage indexed by hash, not in Git; Git is at most a later export function. | `files/darkprint-design.md:169-187` | AGENT-PROPOSED (Q2/Q3: doc 1 §5.1 "Decisione") | NOT-IMPLEMENTED — content lives in `content/` in this Git repository and is copied to `public/bundles/` at prebuild | PENDING-OWNER-REVIEW |
| D-81 | No GitHub OAuth, no linking of user repositories, no Git as the blueprint archive — a product constraint, not a constraint on the author's own workflow. | `files/darkprint-design.md:31-33` | AGENT-PROPOSED (Q2/Q3: doc 1 §0 constraint 4) | CONTRADICTED — superseded by the owner's answers of 2026-08-13, recorded as `backend.md` B-02: sign-in **is** GitHub OAuth. Implemented and merged at `ec516fa` (`lib/server/auth/**`), tagged `t000-verified`. Repository linking and Git-as-archive are untouched by that reversal and remain out | CONTRADICTED |
| D-82 | Private blueprints are excluded from the MCP semantic-search index and from the usage counts that trigger ontology promotion. | `files/darkprint-onboarding-positioning.md:326-327`; `files/darkprint-ontology-v0.1.md:144` | AGENT-PROPOSED (Q2/Q3: doc 2 §6.4, doc 3 §7) | NOT-IMPLEMENTED — `lib/core/config.ts:161` holds promotion thresholds nothing reads; `architecture/engine.md:182-183` says so | PENDING-OWNER-REVIEW |
| D-83 | Do not build anything actively anti-scraping; put rate limiting on the APIs instead, because "not scrapable" conflicts with SEO, LLM ranking and MCP. | `files/darkprint-onboarding-positioning.md:367-369` | AGENT-PROPOSED (Q2/Q3: doc 2 §7.3) | CONTRADICTED — superseded by the owner's answers of 2026-08-13, recorded as `backend.md` B-17: rate limits apply to reads **and** writes, with API keys for high-volume consumers. Rate-limiting reads is the anti-scraping measure D-83 ruled out; the cost to MCP and LLM discoverability was accepted knowingly. Not yet built (T230) | CONTRADICTED |
| D-84 | An account is asked for on *save*, never on download. | `files/darkprint-onboarding-positioning.md:303` | AGENT-PROPOSED (Q2/Q3: doc 2 §6.1) | NOT-IMPLEMENTED — downloads are open and there is no save; `app/u/[username]/saved/page.tsx` renders a seeded list | PENDING-OWNER-REVIEW |
| D-117 | **`darkprint.io` is live, backend-era, database-backed and UNMIGRATED**, healthy only because its code is unmigrated too, so the deploy of `backend` is gated on the migration; and the existence of that deployment **falsifies the premise D-105 was ruled on**. | Measured over HTTPS from this session on 2026-09-01 (read-only, no database connection): `https://www.darkprint.io/settings`, `/blueprints/darkprint/starter-software-factory`, `/api/files/cards/spec-planner@1.0.0`, `/mcp`, `/blueprints/darkprint/frontline-triage`; `scripts/migrate-stored-cards.ts` §1-2 of its production header; `lib/core/card/validate.ts` on `origin/backend` | AGENT-PROPOSED as a decision (nobody ruled that the deploy is gated; the code and the site say so), and the underlying facts were **independently re-measured here** rather than taken on report: the site's own responses are the source, and nothing in this row rests on an agent's summary | **NOT-IMPLEMENTED, which is the entire finding.** The rows are pre-D-91/D-92/D-93: `GET /api/files/cards/spec-planner@1.0.0` serves `requires_human: false` (line 40), `ontology_version: 0.1.0` (line 55) and two prose sentences under `cannot:` (lines 37-38), with no `will_not:` anywhere. **The code is behind the four unpushed local commits too**: `/mcp` renders `read a card`, `inspect provenance` and `fetch a release` and no fifth operation, so `18a6b66` is not deployed. **The site is healthy** (`/settings` 200, `/blueprints/darkprint/starter-software-factory` 200) precisely because both halves are old and agree with each other. **There is no safe order, and both orders are measured** (written out at length in the second header block of `scripts/migrate-stored-cards.ts`). Deploy-first is a **total outage**: `storedCard` declares `willNot` required and no unmigrated body carries one, so `/blueprints` renders empty and every blueprint and node page 404s, which is exactly the state the development database was in before the run. Migrate-first is a **quiet partial degradation**: `will_not` is not in `origin/backend`'s `CARD_KNOWN_KEYS`, so it lands as `card/bad-type` at INFO severity and all 85 stated-limit sentences vanish silently from every rendered card and every download, and `confidence-escalation@1.0.0` and `maintainer-approval@1.0.0` are `human-gate` types that raise `card/human-type-inconsistent` at ERROR once `requires_human` is removed. The two operations therefore have to be adjacent, and the way to shrink the window is to build first and promote after the migration. **NOT ESTABLISHED and deliberately not guessed**: which Supabase project production uses. `.env.local` names two, `POSTGRES_HOST`/`POSTGRES_URL`/`SUPABASE_URL` one project ref and `NEXT_PUBLIC_SUPABASE_URL`/`S3_ENDPOINT` another; the production `DATABASE_URL` has to be read out of the deployed environment. `--expect-db` is close to useless as a guard there, because every Supabase project's database is named `postgres`. **`main` is 1603 commits behind `backend`, local `backend` is 4 commits ahead of `origin/backend`, and nothing has been pushed** | **PENDING-OWNER-REVIEW**, and it carries a correction to an existing row rather than a decision of its own. **D-105 is corrected here and its text is left alone, per D-113.** D-105 holds the ontology version at `0.1.0` on the ground, quoted in its own cell, that *"no score has ever been published under any vocabulary, every row in the system is seed the owner wrote, and there is therefore nothing for a `0.2.0` to be distinguishable from"*, and it writes down its own expiry: *"That ground expires the moment a score reaches somebody who is not the owner."* **That ground is false, and was already false when the row was written.** `GET https://www.darkprint.io/blueprints/darkprint/frontline-triage` serves publicly, with no session, an autonomy reading of `Conditional` against `ontology_version: 0.1.0`. The same release now reads `Supervised` in the migrated development database, and the committed README for the same content has said `Supervised` since `b217ccf`. So **two different scorecards for one release exist under one version string**, which is the exact confusion a version was supposed to prevent, and D-105's own trigger has fired. What is owed is an owner ruling on the version taken with the deployment in front of him; neither the bump to `0.2.0` nor the continued suspension is decided here. **A second row inherits the correction**: §10's heading, *"The backend, if it is ever wanted"*, is falsified by the same measurement, and the four rows under it that read the current build as backendless (D-77 first among them) are describing a repository, not a product |
| D-118 | A frozen folder left at a **superseded** release digest is unreachable garbage rather than stale content that could be served, so clearing the object store after a digest-moving migration is **optional hygiene and never a correctness step**. | `lib/db/storage.ts:24-34,49-57`; `lib/server/publish/artefacts.ts:59-65`; `lib/server/export/persisted.ts:73-80`; `lib/server/export/serve-file.ts:133-136`; `lib/server/export/lookup.ts:121-131`; `lib/server/archive/release.ts:281-292` | AGENT-PROPOSED. Nobody ruled this; it is a reading of the code, recorded so the question is not re-asked at every migration | IMPLEMENTED, and each half checked at its own site rather than inferred from the shape. **Nothing enumerates the bucket**: `ObjectStorage` publishes `put`, `get` and `delete` and nothing else, and no list verb (`ListObjects`, `ListBuckets`, `listObjects`) appears anywhere under `lib/`, `app/`, `scripts/`, `tests/` or `packages/`, so no reader can find a key it was not handed. **A digest becomes a key in exactly one place**, `keyForDigest`, which refuses anything that is not `sha256:` plus 64 lowercase hex. **The only writer is `persistArtefacts`, keyed by the RELEASE digest**, so a card digest is never a key. **The only reader is `frozenFolder`**, and `serveFile` calls it with `release.digest` taken off a row it has already resolved. A caller-supplied digest reaches storage only through `resolveRelease` then `getRelease`, which matches it against a live `release` row **of that bundle** and answers `undefined` otherwise. So a superseded digest is not merely unlikely to be served; there is no path that can name it. **Measured locally after the migration**: 167 objects under the `sha256/` prefix, 9 of them at a current release digest, 9 more at digests this migration superseded, and 0 overlap between those two sets. **`--purge-frozen` is therefore not owed by correctness, and its own limit is worth writing down**: it knows only the NEW digests, so recovering the old keys means reading them back out of the rollback file, and deleting them is the cheap half of a rollback to give up. Delete last, or not at all | PENDING-OWNER-REVIEW |
| D-119 | A stored vector records **the identity of the exact text the encoder was handed** (`embedded_input_sha256`, migration `0008`), because D-200-03's argument that no such column was needed rested on a premise commit `ed3ae85` falsified; and it is a hash of that INPUT, never a digest. | `lib/db/migrations/0008_embedding_input.up.sql` / `.down.sql`; `lib/db/schema.ts:259,625-655,669-684`; `lib/server/search/reembed.ts` (header, `embeddedInput`, `embedManifest`, `embedCards`); `lib/server/search/staleness.scratch.test.ts` | AGENT-PROPOSED. Nobody ruled this. It is recorded because a shipped argument was found false and rewritten, and a reader who meets the new column has to be able to see what changed and why | IMPLEMENTED 2026-09-02. **The prose question first, because the column is the smaller half.** `lib/server/search/reembed.ts:10-18` and `lib/db/schema.ts:628-631` both argued, in the same words, that AC6's idempotency reads a row's presence and needs no second column: a `release` row is content-addressed, `digest` is derived from the bytes so it CANNOT CHANGE, therefore a row here is already a vector for that digest, and an `embedded_digest` would be a second source for one quantity. That is an IMMUTABILITY claim about the described row, not a discipline claim about the writers, and `schema.ts:259` stated it outright ("One immutable row per `(cardId, version)`"). **It was falsified by a real event.** `ed3ae85` ran `update card_version set body, source, digest where id = ...` over all 58 rows and `update release set manifest, card_digests, digest` over all 16, in place, under unchanged primary keys. Every digest moved. The `onDelete: cascade` that `schema.ts` named as the thing making a stale vector unrepresentable never fired, because nothing was deleted, and the migration's own header counts "cannot lose an embedding" as a virtue of doing it that way. 42 card and 7 release vectors written 2026-08-25 outlived a rewrite of their subjects on 2026-09-01. **The conclusion survived the event anyway, by luck about which fields moved, and that was measured rather than argued.** `cardText` reads `id, name, type, action, spec, phases, tools, riskMarkers`; `manifestText` reads `slug, title, summary, description, category, tags`. `ed3ae85` moved `cannot`, `will_not`, `notes`, `requires_human` and `ontology_version`, and the intersection is empty. Re-encoding every subject from its POST-migration body reproduced every stored vector, and the strongest form of that is a byte diff rather than a cosine: all 49 pre-existing rows carried a NULL stamp, so the repair path re-encoded every one of them and moved `created_at` from 2026-08-25 to 2026-09-02, yet comparing `embedding::text` row by row against a dump taken between the migration and the sweep gives 42 of 42 card and 7 of 7 release vectors IDENTICAL, 0 changed, 0 removed. The instrument was falsified first: a counterfactual `cardText` that appends `cannot` and `willNot` flips 42 of 42 to CHANGED. **So nothing was stale, no vector needed repair, and this row does not claim one did.** What was gone is the invariant. **The column is deliberately NOT the `embedded_digest` the old paragraph rejected, and that objection is kept and narrowed rather than overruled.** A column mirroring a CURRENT value can only agree or be a bug. `release.digest` is also the wrong quantity twice over: `bundleDigest` hashes `{dot, cardDigests}` with no manifest in it, while the release vector is nothing but manifest fields, so it would have called all 16 releases stale last week and would not move at all if someone edited `manifest.title`; `cardDigest` covers the whole card minus `author`/`provenance` and is over-broad in the same direction. The stored value is `sha256:<hex>` over `MODEL_BLOB.sha256 + "\n" + text`, a PAST value, and disagreement with today's input is the signal. **The model pin is inside the hash on purpose**, so swapping the weights invalidates every row; the residual is stated in the code rather than left implicit, because changing the pooling or the normalisation inside `embed.ts` moves no stamp. **The writer had to change too**: the shipped one was `onConflictDoNothing` with no DELETE anywhere, so detection alone would have found a stale vector it could not repair. It is now an upsert reached only when the stamp disagrees or is absent, so AC6's byte-identical `embedding` and `created_at` on a second call is preserved by an early return instead of by the conflict clause. **What moved in the data, stated without dressing it up**: sweeping all 16 releases through `reembedRelease` took `card_version_embedding` 42 -> 58 and `release_embedding` 7 -> 16, so 25 vectors now exist that did not, and **not one pre-existing vector's bytes changed**. That coverage hole was wiring date and not visibility (all 58 card versions are `public` and pinned; the 9 unembedded releases predate the `publish()` call site landing in `f30e664`). **Falsified before landing**: six mutations, each verified to land by grep, red between 1 and 4 of the 9 cells in `staleness.scratch.test.ts`. **Two of the six were re-run by the session writing this row, against the same tree, rather than taken on report**, and both reproduced: reverting the card writer to `.onConflictDoNothing()` reds exactly the two repair cells (the in-place rewrite of an embedded field, and the pre-0008 null-stamp row) and nothing else; removing `MODEL_BLOB.sha256` from `embeddedInput` reds exactly the two stamp cells, which are the only two that run without a database. **The other four are reported and not re-run here**, among them stamping `JSON.stringify(body)` instead of the encoder's input, which is the digest mistake this row rejects. Baseline is 9 of 9 green, and the file was restored and byte-compared after each mutation. **`backend.md`'s D-200-03 is NOT edited and the divergence is reported instead**, because that file belongs to neither wave in this tree: its ruling at `:19774` still says a row's digest "cannot change" and that `reembedRelease` "writes nothing when the row is there", the first falsified by `ed3ae85` and the second now false of the code. **AC6 survives that distinction rather than being narrowed by it**: AC6 asks that a second call over UNCHANGED content leave `embedding` and `created_at` byte-identical, which the early return preserves, while D-200-03's stronger sentence covered a case AC6 never reached. **Priced and left open, in `docs/ARCHITECTURE.md` §11.1 rather than absorbed here**: no repository entry point sweeps an archive, so the coverage repair reached the development database through a throwaway script and reaches no other; `scripts/migrate-stored-cards.ts` now disagrees with that database twice (its `7`/`42` embedding controls, and a runbook paragraph telling an operator search drifts silently); and production must take migration `0008` before this branch's code, because `reembedRelease` now SELECTs the column on every publish | PENDING-OWNER-REVIEW |
| D-138 | **An API key carries a SCOPE chosen once at minting, and every key minted before 2026-09-05 is read-scoped for the rest of its life.** | the owner's instruction, 2026-09-05 (§11.0 Q3); `lib/db/migrations/0010_key_scope.up.sql`; `lib/server/limits/{types,keys}.ts`; `app/api/account/keys/route.ts`; `components/settings/ApiKeys.tsx` | OWNER-STATED, 2026-09-05 — the owner ruled that keys gain a scope and that no key minted under Settings §06's existing promise may gain write power without being reissued | IMPLEMENTED. `0010_key_scope` adds a Postgres enum `api_key_scope` ('read','write') and a `NOT NULL` `api_key.scope` defaulting to `read`, **and the default is KEPT after the backfill** so an uninformed writer mints least privilege instead of raising a NOT NULL violation. `writeActorFor(db, key)` is the only published key-to-`Actor` path and **re-reads the row at the moment of the write**, requiring `scope='write' AND revoked_at IS NULL`: the `ResolvedKey` brand proves PROVENANCE and can never prove liveness, and T231 had already refused a type that claimed otherwise. It INNER JOINs `account` for the real handle rather than fabricating a null, because `requireHandle` reads a null handle as *sign-up unfinished* and would refuse a key holder for a condition that is not true. **The write scope reaches no route**: nothing in `app/api/**` calls `writeActorFor`, the panel's write-scope copy says so, and that sentence must change in the same commit as the first route that accepts a key. `ApiKeyRecord` gained a sixth member, which took `backend.md:19114` and `tests/server/t230/contract.ts:413` in ONE change because `surface.test.ts` compares the parsed block against the hand-typed floor. **This narrows D-114 without overturning it**: a key still gates no read | CONFIRMED (the scope, and that existing keys stay read-only). **PENDING-OWNER-REVIEW on three implementation choices that were not put to him**: an ENUM rather than text plus a CHECK, which costs two migrations to add a third scope because `migrate.ts` wraps each in BEGIN/COMMIT; keeping the column default after the backfill; and the `account` join in `writeActorFor` |
| D-147 | **A forked card's id is always `<forker handle>/<name>`** — the namespace is REPLACED rather than nested under, and it is never the caller's to choose. | `lib/server/lineage/fork-card.ts` (DECISION 1, in the file's own header); `lib/core/card/schema.ts:167` (`CARD_ID`); `app/api/cards/[...ref]/route.ts` | AGENT-PROPOSED — decided by the implementing session while writing the route, never put to the owner in these terms | IMPLEMENTED. Two arguments, and the second is what makes it a rule rather than a taste. **The grammar forces the replacement**: `CARD_ID` admits exactly ONE optional namespace segment, so forking `lupo/planner` cannot produce `berti/lupo/planner` — that id does not parse, and a card whose id does not parse cannot be pinned by a DOT node, which is the whole purpose of a card id. A fork of a fork lands one segment deep like the first one did. **The security argument forbids a caller-chosen target**: a freely chosen id makes this endpoint a probe for private cards, because 201 and 409 are distinguishable and the 409 would confirm that somebody holds `<somebody>/<guess>`. A caller who may only write inside their own namespace can only ever learn about their own namespace. That is why the request body carries an optional `name` and no `id` at all. **A taken id is REFUSED rather than appended to**: `addCard` alone would have added a version to whatever chain already sits at that id, including one owned by somebody else — nothing in `lib/server/cards` compares a new version's owner against the owner of the versions already there, and the unique index is on `(card_id, version)`, so a squatter on `<your handle>/planner` would have received a version written by you. `cardIdInUse` closes that at this door and is deliberately owner- and visibility-BLIND, because the filtered question cannot see a private squatter at all. **The same hole is open for every other writer, and the file reports it rather than closing it** | PENDING-OWNER-REVIEW |
| D-148 | **A forked card KEEPS the source version**, so a repin onto a fork always moves `cardDigest` and therefore `bundleDigest`. | `lib/server/lineage/fork-card.ts` (DECISION 2, in the file's own header); `lib/core/card/schema.ts:290-300`; `lib/core/attractor/import.ts` (the opposite case) | AGENT-PROPOSED — the implementing session's reading of what a version MEANS, never put to the owner | IMPLEMENTED. A card version pins a node to exact bytes and travels to Attractor as `card="id@version"`, so the question is what a fork's version should mean. **Kept, it means what it meant upstream**: `berti/planner@1.2.0` is the content the upstream published as `1.2.0`, and the next edit's bump is priced by `checkVersionChain` against the real predecessor. **Reset to `0.1.0` it would mean a generation that never existed** — the same bytes claiming to be an early draft, with a later breaking change priced against a version nobody ever published. `import.ts` resets for the opposite case, a card compiled from a pipeline that was never a card at all, and says so in its own header; a fork needs no edit to be correct. **The consequence is the half worth recording**: `cardDigest` hashes every field except `author` and `provenance`, so the id is inside it and a fork's digest differs from its upstream's even though nothing about the node's behaviour changed. `bundleDigest` is taken over the DOT and those digests, so a blueprint repinning from `planner@1.2.0` to `berti/planner@1.2.0` moves both. **A fork can never be a silent substitution inside a release**: the pin, the card digest and the bundle digest all change together. The one identity that does NOT move is the content key `archive/registry.ts` computes for `duplicates()`, which holds `id`, `version`, `author` and `provenance` aside — so a fresh fork and its upstream group as the same card wearing two labels, which is true and is what that query is for | PENDING-OWNER-REVIEW |
| D-149 | **A card id's namespace segment now MEANS "this account's handle", and the fork door is the first place in the tree that enforces it.** | `lib/server/lineage/fork-card.ts`; `lib/server/cards/**` (the writers that do not check); `lib/core/card/schema.ts:167` | AGENT-PROPOSED — a consequence the implementing session drew from D-147 and wrote down, never ruled on | IMPLEMENTED AT ONE DOOR ONLY, which is the whole content of this row. `berti/solver-a` is berti's, which is what a reader already assumes it says; **nothing in the tree enforced that before `forkCard`, and nothing else enforces it now.** Every other writer into `lib/server/cards` may still publish a version at an id inside somebody else's namespace, because no writer compares a new version's owner against the owner of the versions already there. So the meaning is real at the fork door and aspirational everywhere else, and a reader who takes it as a general invariant is taking a claim the tree does not keep. **Whether it should become one is the owner's call and it is a larger change than it looks**: enforcing it at every writer would refuse ids that already exist | PENDING-OWNER-REVIEW |

---

## Counts

After the owner's answers of 2026-08-12 (Q1–Q13, recorded at the foot of
`docs/audit/CONTEXT_CONFLICTS.md`):

| Provenance | before answers | after |
|---|---|---|
| **Decisions total** | 85 | **89** |
| `OWNER-STATED` | 14 | **23** |
| `AGENT-PROPOSED` | 13 | **35** |
| `UNATTRIBUTED` | 58 | **31** |

| Status | before | after |
|---|---|---|
| `CONFIRMED` | 14 | **23** |
| `PENDING-OWNER-REVIEW` | 71 | **66** |

| Verifiable in code | count |
|---|---|
| `IMPLEMENTED` | 64 |
| `CONTRADICTED` | 10 |
| `NOT-IMPLEMENTED` | 8 |
| `UNVERIFIABLE` | 7 |

**Recounted on 2026-08-30, by parsing the rows rather than by adding a delta to the numbers
above.** The three tables above are the 2026-08-12 snapshot and are left as history.

| | 2026-08-12 | 2026-08-30 |
|---|---|---|
| **Decisions total** | 89 | **107** |
| `OWNER-STATED` | 23 | **37** |
| `AGENT-PROPOSED` | 35 | **39** |
| `UNATTRIBUTED` | 31 | **31** |
| `CONFIRMED` | 23 | **37** |
| `PENDING-OWNER-REVIEW` | 66 | **67** |
| `IMPLEMENTED` | 64 | **81** |
| `CONTRADICTED` (verifiable-in-code column) | 10 | **11** |
| `NOT-IMPLEMENTED` | 8 | **7** |
| `UNVERIFIABLE` | 7 | **7** |

The 2026-08-30 wave added D-91 to D-107: fourteen `OWNER-STATED` / `CONFIRMED` rows carrying
the owner's own instruction and its date, and three `AGENT-PROPOSED` /
`PENDING-OWNER-REVIEW` rows for judgements the register did not settle. Four earlier rows
were amended in place with their history kept — D-22 narrowed, D-27 restated (and its stale
evidence corrected), D-30 marked `UNMECHANISED` as well as `UNVERIFIABLE`, D-52's surviving
constant re-explained.

**Recounted again on 2026-09-01, and the two recounts disagree, which is reported rather
than reconciled away.** The 2026-08-30 table above stops at D-107. Eleven rows have been
added since: D-108 to D-114 on 2026-08-31, and D-115 to D-118 in the registry-migration
wave of 2026-09-01. The ledger holds **118** rows.

| | 2026-08-30 (D-01..D-107) | 2026-09-01 (all 118) |
|---|---|---|
| **Decisions total** | 107 | **118** |
| `OWNER-STATED` | 37 | **51** |
| `AGENT-PROPOSED` | 39 | **36** |
| `UNATTRIBUTED` | 31 | **31** |
| `CONFIRMED` | 37 | **48** |
| `PENDING-OWNER-REVIEW` | 67 | **68** |
| `IMPLEMENTED` | 81 | **90** |
| `CONTRADICTED` (verifiable-in-code column) | 11 | **10** |
| `NOT-IMPLEMENTED` | 7 | **9** |
| `UNVERIFIABLE` | 7 | **7** |

**Recounted again on 2026-09-02, by a parser over the rows, and it does not reproduce the
2026-09-01 column.** Two rows moved and one was added in this wave: **D-111** goes
`NOT-IMPLEMENTED` to `IMPLEMENTED` (the drift checker now walks `packages/`), and **D-119** is
new (`AGENT-PROPOSED`, `IMPLEMENTED`, `PENDING-OWNER-REVIEW`). The ledger holds **119** rows.

| | 2026-09-01 (as recorded) | 2026-09-02 (parsed) |
|---|---|---|
| **Decisions total** | 118 | **119** |
| `OWNER-STATED` | 51 | **50** |
| `AGENT-PROPOSED` | 36 | **38** |
| `UNATTRIBUTED` | 31 | **31** |
| `CONFIRMED` | 48 | **47** |
| `PENDING-OWNER-REVIEW` | 68 | **69** |
| `IMPLEMENTED` | 90 | **93** |
| `CONTRADICTED` (verifiable-in-code column) | 10 | **10** |
| `NOT-IMPLEMENTED` | 9 | **7** |
| `UNVERIFIABLE` | 7 | **7** |

**Recounted again on 2026-09-05, by the same kind of parser over the rows, after two waves
that added eighteen.** D-120 to D-130 landed on 2026-09-04 without a recount and D-131 to
D-137 land here. The ledger holds **137** rows.

| | 2026-09-02 (parsed) | 2026-09-05 (parsed) |
|---|---|---|
| **Decisions total** | 119 | **137** |
| `OWNER-STATED` | 50 | **62** |
| `REPORTED OWNER-STATED` (D-115, D-116; `UNATTRIBUTED` under this document's own bar) | — | **2** |
| `AGENT-PROPOSED` | 38 | **42** |
| `UNATTRIBUTED` | 31 | **31** |
| `CONFIRMED` | 47 | **57** |
| `PENDING-OWNER-REVIEW` | 69 | **77** |
| `IMPLEMENTED` | 93 | **106** |
| `CONTRADICTED` (verifiable-in-code column) | 10 | **10** |
| `NOT-IMPLEMENTED` | 7 | **11** |
| `UNVERIFIABLE` | 7 | **7** |

**Recounted a fifth time on 2026-09-05, after the §11.0 walk of the same day added six.**
D-138 to D-143 land here, all six `OWNER-STATED` / `CONFIRMED` / `IMPLEMENTED`, and three
earlier rows moved from `PENDING-OWNER-REVIEW` to `CONFIRMED` because the question each was
holding open was put to the owner and answered: **D-120** (what becomes of the ballot
machinery, answered in part by D-141), **D-121** (what happens to `ScoringModel`, answered by
D-140) and **D-135** (the `app/globals.css` divergence, fixed by D-142). **D-114 was narrowed
in place rather than moved** — the read half is untouched and the write half now means *a key
minted with `scope: "write"`* — and **D-129's priced cost was discharged**, by deletion rather
than by the rewrite it was waiting for. The ledger holds **143** rows.

| | 2026-09-05 (as recorded above) | 2026-09-05 (parsed, after the §11.0 walk) |
|---|---|---|
| **Decisions total** | 137 | **143** |
| `OWNER-STATED` | 62 | **68** |
| `REPORTED OWNER-STATED` (D-115, D-116) | 2 | **2** |
| `AGENT-PROPOSED` | 42 | **42** |
| `UNATTRIBUTED` | 31 | **31** |
| `CONFIRMED` | 57 | **65** |
| `PENDING-OWNER-REVIEW` | 77 | **75** |
| `IMPLEMENTED` | 106 | **112** |
| `CONTRADICTED` (verifiable-in-code column) | 10 | **10** |
| `NOT-IMPLEMENTED` | 11 | **11** |
| `UNVERIFIABLE` | 7 | **7** |

**The arithmetic disagrees with itself by one in the status column, and the row responsible is
named rather than absorbed.** 65 + 75 + 3 unbucketed = 143, so this parse accounts for every
row. The 2026-09-05 column does not: 57 + 77 = 134, and the paragraph two below says two rows
open `CONTRADICTED` "where the other 135 open `CONFIRMED` or `PENDING-OWNER-REVIEW`" —
134 + 2 = 136 against a stated total of 137. The missing row is **D-52**, whose status cell
opens `REVERSED (OWNER-STATED, 2026-08-25 …)` and belongs to neither bucket, exactly as its
verifiable-in-code cell does. So the three unbucketed status cells are D-52, D-81 and D-83,
not two.

**The same paragraph names the wrong two rows for the verifiable-in-code column, and this is a
correction rather than a second reading.** It says D-88's cell opens `NOT A CODE CHANGE` and
"D-18's neighbour" opens `WAS IMPLEMENTED`. Parsed against the tree: **D-88 and D-18 both open
`IMPLEMENTED`**; the cell opening `NOT A CODE CHANGE` is **D-114**'s and the one opening
`WAS IMPLEMENTED` is **D-56**'s. D-52's `REVERSED` was right. Three unbucketed cells either
way, and two of the three ids were wrong — which is what a hand-carried citation does over
five recounts.

**Three rows fall in no bucket and are named rather than rounded into one**: D-52's
verifiable-in-code cell opens `REVERSED, 2026-08-25`, D-88's opens `NOT A CODE CHANGE`, and
D-18's neighbour opens `WAS IMPLEMENTED`. The status column has two rows opening
`CONTRADICTED` where the other 135 open `CONFIRMED` or `PENDING-OWNER-REVIEW`. A parser that
forced them into the four names would report four numbers that add up and describe nothing.

**Recounted a SEVENTH time on 2026-09-06, later the same day, by the same parser, after the
two owner rulings of that morning.** D-151 (amber is the card register) and D-152 (`ScoreRadar`
is deleted) land here, both `OWNER-STATED` / `CONFIRMED` / `IMPLEMENTED`, and both are
`CONFIRMED` rather than `PENDING-OWNER-REVIEW` because the owner stated each in the
conversation that wrote them, in their own words, which is the one condition `CLAUDE.md`
allows. **Three earlier rows were amended in place with their history kept, and one of them
moved a column.** **D-15** — amber's "no third thing may take it" — is superseded in that
clause by D-151 and its verifiable-in-code cell goes `IMPLEMENTED` to `WAS IMPLEMENTED`,
which is the honest cell for a rule whose implementation is what the supersession overturns;
its status stays `PENDING-OWNER-REVIEW` as to the original, since the supersession is a new
row rather than a ratification of the old one. **D-75** takes the amendment in its sentence
and its provenance cell names the owner's 2026-09-06 instruction alongside the original
`UNATTRIBUTED`, which the first-keyword rule still reads as `UNATTRIBUTED`. **D-90** is
corrected for the second time in two days, and this correction is a change of kind rather
than of count: its witness is not merely unmounted now, it is deleted, and the row states the
three facts that replace it (a producer-side enforcement site, no renderer, no test) rather
than a single sentence that would be wrong in one direction or the other. The ledger holds
**152** rows and every id from D-01 to D-152 is present exactly once, checked by the parser
rather than asserted.

| | 2026-09-06 (parsed, sixth) | 2026-09-06 (parsed, seventh) |
|---|---|---|
| **Decisions total** | 150 | **152** |
| `OWNER-STATED` | 71 | **73** |
| `REPORTED OWNER-STATED` (D-115, D-116) | 2 | **2** |
| `AGENT-PROPOSED` | 46 | **46** |
| `UNATTRIBUTED` | 31 | **31** |
| `CONFIRMED` | 68 | **70** |
| `PENDING-OWNER-REVIEW` | 79 | **79** |
| `IMPLEMENTED` | 120 | **121** |
| `CONTRADICTED` (verifiable-in-code column) | 10 | **10** |
| `NOT-IMPLEMENTED` | 10 | **10** |
| `UNVERIFIABLE` | 7 | **7** |

**Both columns add up and the unbucketed cells are now FOUR rather than three, because one of
them was created by this pass and is named rather than rounded away.** Status: 70 `CONFIRMED`
+ 79 `PENDING-OWNER-REVIEW` + 2 `CONTRADICTED` + 1 `REVERSED` = 152. Verifiable-in-code: 121
`IMPLEMENTED` + 10 `CONTRADICTED` + 10 `NOT-IMPLEMENTED` + 7 `UNVERIFIABLE` + **2**
`WAS IMPLEMENTED` (D-56 and now D-15) + 1 `NOT A CODE CHANGE` (D-114) + 1 `REVERSED` (D-52) =
152. The `AGENT-PROPOSED` figure still counts three cells that continue the word into a
sentence and is 43 if only the bare spelling is counted, unchanged from the sixth pass.
**Neither column should be read as exact**, for the reasons the earlier recounts give at
length; what is exact is the movement this pass claims, which is +2 rows and one cell moving
from `IMPLEMENTED` to `WAS IMPLEMENTED`.


**Recounted an EIGHTH time on 2026-09-06, after a fourth owner ruling of the same day.**
D-153 lands here — the vocabulary's two routes fold into one — `OWNER-STATED` / `CONFIRMED` /
`IMPLEMENTED`, stated in the conversation in the owner's own words. **Three earlier rows were
amended in place with their history kept, and one of them moved a column in the direction
almost nothing ever moves.** **D-39** goes `CONTRADICTED` back to `IMPLEMENTED`: its sentence
— `/spec/ontology` canonical, `/ontology` a compatibility URL — was true, then false, and is
true again, and it is the only row in this ledger to have been restored rather than
superseded. Its provenance stays `AGENT-PROPOSED` and its status stays
`PENDING-OWNER-REVIEW`, because what the owner confirmed is the outcome and not the plan that
first proposed it. **D-40** keeps its naming rule and loses its two-route premise; the open
question it leaves is carried as a `TBD:` in the glossary rather than answered here.
**D-135** loses a page reference that pointed into a deleted file, and its subject is renamed
from a route to the components the rule is actually enforced over. The ledger holds **153**
rows and every id from D-01 to D-153 is present exactly once.

**THIS RECOUNT IS THREE ROWS BEHIND THE FILE AS OF 2026-09-06, AND THE GAP IS NAMED RATHER
THAN PATCHED.** D-154, D-155 and D-156 all landed on 2026-09-06 after the eighth parse ran,
so the table below is a reading of a file that no longer exists and the row total is
**156**. It is left standing because every figure in it was calibrated against the seventh
recount before it was believed, and a table with three rows added by hand is no longer that
calibrated reading. **What the three add, stated so a ninth parse has something to check
itself against**: three `OWNER-STATED`, three `CONFIRMED`, three `IMPLEMENTED`, and no
movement in `CONTRADICTED` except D-39, which goes back to it. So the prediction is 156 /
77 `OWNER-STATED` / 74 `CONFIRMED` / 126 `IMPLEMENTED` / 10 `CONTRADICTED`. **D-39 is the
figure to watch and the reason to re-parse rather than to trust this paragraph**: its cell
opens `IMPLEMENTED` and closes `CONTRADICTED AGAIN`, which is a history and not a state, and
a parser reading either end alone gets 9 or 10. The state is contradicted — `/spec/ontology`
is not the canonical route for anything, because it is not a route. D-144's single-clause
contradiction is still counted nowhere, for the reason stated near the end of this section,
and D-156 supersedes that clause a second time without changing how it is counted.

| | 2026-09-06 (parsed, seventh) | 2026-09-06 (parsed, eighth) |
|---|---|---|
| **Decisions total** | 152 | **153** |
| `OWNER-STATED` | 73 | **74** |
| `REPORTED OWNER-STATED` (D-115, D-116) | 2 | **2** |
| `AGENT-PROPOSED` | 46 | **46** |
| `UNATTRIBUTED` | 31 | **31** |
| `CONFIRMED` | 70 | **71** |
| `PENDING-OWNER-REVIEW` | 79 | **79** |
| `IMPLEMENTED` | 121 | **123** |
| `CONTRADICTED` (verifiable-in-code column) | 10 | **9** |
| `NOT-IMPLEMENTED` | 10 | **10** |
| `UNVERIFIABLE` | 7 | **7** |

Status: 71 `CONFIRMED` + 79 `PENDING-OWNER-REVIEW` + 2 `CONTRADICTED` + 1 `REVERSED` = 153.
Verifiable-in-code: 123 `IMPLEMENTED` + 9 `CONTRADICTED` + 10 `NOT-IMPLEMENTED` + 7
`UNVERIFIABLE` + 2 `WAS IMPLEMENTED` + 1 `NOT A CODE CHANGE` + 1 `REVERSED` = 153. **The
movement this pass claims is +1 row, +2 `IMPLEMENTED` and −1 `CONTRADICTED`** — two, because
D-153 arrives implemented and D-39 returns to it. The caveats every earlier recount states
about reading these columns as exact are unchanged.

**The parser was CALIBRATED against the seventh recount before it was believed, and that is
what makes this table a reading rather than an assertion.** Run over the file as it stood
before this pass, it answers 152 rows, 73/46/31/2 on provenance, 121/10/10/7/2/1/1 on
verifiable-in-code and 79/70/2/1 on status — every figure the seventh recount records,
reproduced cell for cell. Only then was it run over the amended file.

**It caught something on the first run, and the finding is about writing rather than about
counting.** Both rows this pass amended had opened their verifiable-in-code cell with a word
that is not a bucket: D-39 with `RESTORED` and D-40 with `HALF IMPLEMENTED`. A first-keyword
parse buckets neither, so the honest table would have read 121 `IMPLEMENTED` and two new
unbucketed names, and the count would have said the restoration never happened. **The
sentences were reworded to lead with `IMPLEMENTED` and keep the qualifier after it**, which
is the same rule the earlier recounts arrived at from the other direction when they refused
to force `WAS IMPLEMENTED` and `NOT A CODE CHANGE` into the four names. A cell's first word
is load-bearing here; a cell that starts with the interesting word instead of the true one
deletes itself from the census.


**Recounted a sixth time on 2026-09-06, by a parser over the rows, after the wave that
deleted `/build`, shipped `/spec/attractor` and rebuilt the blueprint page.** D-144 to D-150
land here: three `OWNER-STATED` / `CONFIRMED` (the ontology governance band, the `/build`
deletion, the blueprint page's four removals) and four `AGENT-PROPOSED` /
`PENDING-OWNER-REVIEW` (the three card-fork rulings the implementing session made, and the
destination it chose for two orphaned 308s). **One earlier row moved and it moved on the
verifiable-in-code column alone**: `D-126` goes `NOT-IMPLEMENTED` to `IMPLEMENTED`, because
the fourth `/spec` route it recorded as owed now exists. Its status was already `CONFIRMED`
and did not move, which is the distinction that column is for. **Two rows were corrected in
place with their history kept**: D-137, whose own counter-example (`ScorePanel.tsx:291`) was
deleted with `/build`, so the premise it falsified is true again of everything except
`/upload` and its open boundary NARROWS to one route; and D-90, whose surviving witness
`ScoreRadar.tsx` is still the only one and is now UNMOUNTED, which is a different state from
the one that clause describes. The ledger holds **150** rows.

| | 2026-09-05 (parsed) | 2026-09-06 (parsed) |
|---|---|---|
| **Decisions total** | 143 | **150** |
| `OWNER-STATED` | 68 | **71** |
| `REPORTED OWNER-STATED` (D-115, D-116) | 2 | **2** |
| `AGENT-PROPOSED` | 42 | **46** |
| `UNATTRIBUTED` | 31 | **31** |
| `CONFIRMED` | 65 | **68** |
| `PENDING-OWNER-REVIEW` | 75 | **79** |
| `IMPLEMENTED` | 112 | **120** |
| `CONTRADICTED` (verifiable-in-code column) | 10 | **10** |
| `NOT-IMPLEMENTED` | 11 | **10** |
| `UNVERIFIABLE` | 7 | **7** |

**Both columns of this parse add up, and the three unbucketed cells are the same three the
2026-09-05 pass named rather than a new set.** Status: 68 + 79 + 2 `CONTRADICTED` + 1
`REVERSED` = 150. Verifiable-in-code: 120 + 10 + 10 + 7 + D-114's `NOT A CODE CHANGE` +
D-52's `REVERSED` + D-56's `WAS IMPLEMENTED` = 150. The `AGENT-PROPOSED` figure counts three
cells that continue the word into a sentence (`AGENT-PROPOSED as a decision`,
`AGENT-PROPOSED. Nobody ruled this…` twice) and is 43 if only the bare spelling is counted —
named here because a parser keyed on an exact cell value would report 43 and describe the
same rows.

**One historical paragraph below is now stale and is left as history.** The list of eleven
`CONTRADICTED` rows names **D-105**, which was `CONTRADICTED` before its 2026-08-30 ruling
and reads `IMPLEMENTED` / `CONFIRMED` today; D-131 supersedes it outright, by deleting the
concept it priced.

**The disagreement is not the delta and is reported rather than reconciled away**, the same
way the 2026-08-30 and 2026-09-01 recounts were left disagreeing. Running the same parser over
`HEAD`'s copy of this file, before this wave touched it, gives 50 / 37 / 31 provenance,
47 `CONFIRMED` and 91 `IMPLEMENTED` against the 51 / 36 / 31, 48 and 90 recorded above, so the
gap predates this edit by one row in each of four buckets. Five cells do not begin with any of
the vocabulary the columns use and are counted in none of them: **D-52**'s verifiable and status
cells both open `REVERSED`, **D-114**'s opens `NOT A CODE CHANGE`, and **D-81** and **D-83**
carry `CONTRADICTED` in the *status* column, which is not one of the two values that column
takes. Whoever wants one number has to rule on those five first, and this row does not.

**The method, stated because it is what the two runs disagree about.** The 2026-09-01
column classifies each row by the FIRST bar keyword its provenance cell contains and the
first status keyword its status cell contains. Run over the identical D-01..D-107 scope,
that rule returns `OWNER-STATED` 42, `AGENT-PROPOSED` 34, `UNATTRIBUTED` 31, `CONFIRMED` 41,
`PENDING-OWNER-REVIEW` 64, and it does **not** reproduce the 2026-08-30 figures. Two causes
are visible and neither is fully separable from the other: several cells name two provenance
values in sequence (D-27 opens `AGENT-PROPOSED` and closes `OWNER-STATED as restated`), so a
first-keyword rule and a whole-cell reading will always differ on them; and rows have been
amended in place since, D-105 among them. Four rows carry no bare keyword at all in one
column and fall out of one tally each: D-52 and D-114 in the verifiable column, D-81 and
D-83 in the status column. **Neither column should be read as exact.** What is exact, and is
the only movement this wave claims: **all four new rows are `PENDING-OWNER-REVIEW`**,
because `CLAUDE.md` forbids stamping a row `CONFIRMED` on a ruling the conversation writing
it did not hear, and D-115 and D-116 record rulings that reached this session through a
relay with no quotation reproduced.

**Recounted again on 2026-09-04, by the same parser, over the eleven rows this wave adds.**
D-120 to D-130. The ledger holds **130** rows and every id from D-01 to D-130 is present
exactly once.

| | 2026-09-02 (parsed) | 2026-09-04 (parsed) |
|---|---|---|
| **Decisions total** | 119 | **130** |
| `OWNER-STATED` | 50 | **59** |
| `AGENT-PROPOSED` | 38 | **40** |
| `UNATTRIBUTED` | 31 | **31** |
| `CONFIRMED` | 47 | **51** |
| `PENDING-OWNER-REVIEW` | 69 | **77** |
| `IMPLEMENTED` | 93 | **100** |
| `CONTRADICTED` (verifiable-in-code column) | 10 | **10** |
| `NOT-IMPLEMENTED` | 7 | **11** |
| `UNVERIFIABLE` | 7 | **7** |

**What this wave adds, stated exactly, because the columns above are not exact and the
paragraphs before this one say why.** Eight rows are `OWNER-STATED` on the owner's
instructions of 2026-09-04: D-120 (the scoring reading leaves the blueprint page), D-121
(the grading page leaves the site behind a 308), D-122 (condition guards in the archive),
D-123 (`tool` to box/codergen plus a `shell-tool` term), D-124 (`max_retries` as the
archive's cap), D-125 (the unexpressed-attribute disclosure in `README.md`), D-126 (the
crosswalk as a fourth `/spec` route) and D-127 (browser-side Attractor import on
`/upload`). Three are `AGENT-PROPOSED` and `PENDING-OWNER-REVIEW` for judgements the
instructions did not settle: D-128 (refusing §10.7's FUTURE operators), D-129 (grading
`condition-syntax` a warning against Attractor's own ERROR) and D-130 (following §10.2's
grammar over §10.5's evaluator).

**Three of the eight owner-stated rows are `NOT-IMPLEMENTED` and are stamped so rather than
softened.** D-125, D-126 and D-127 are instructions given and not carried out in this wave.
Their Status is `CONFIRMED` because the owner did state them; their verifiable-in-code cell
is what records that nothing was built, and `docs/ARCHITECTURE.md` §11.0 Q20 carries them as
owed work. An instruction described anywhere as done because it was agreed is the failure
this ledger's own provenance bar exists to prevent, one level up.

**Five rows carry a `PENDING-OWNER-REVIEW` qualified by clause rather than whole**, which is
new in this wave and is a shape the parser reads as `PENDING-OWNER-REVIEW`. D-120 to D-124
each record an owner instruction that was carried out and a judgement inside it that was
not put to him — which keys each fork routes on, whether `params` is the right carrier for
`tool_command`, whether `runbook-executor`'s corrected cap is 0. The instruction half of
those five is `OWNER-STATED` and the row says which half is which. Nobody may cite the
qualified half as settled.

**A paragraph two below this one is now stale, and D-117 is why that matters.** The
paragraph beginning "The `CONTRADICTED` rows were ten and are eleven" lists **D-105** among
them. D-105 was afterwards amended to
`CONFIRMED` on the owner's ruling of 2026-08-30, and its verifiable-in-code cell reads
`IMPLEMENTED`, so the list is ten rows now and D-105 is not one of them. That is a
bookkeeping correction. The substantive one is D-117: the ruling that moved D-105 to
`CONFIRMED` rests on a premise ("no score has ever been published under any vocabulary")
that a measurement of `darkprint.io` on 2026-09-01 falsifies. The row's `CONFIRMED` stamp is
left standing, because the owner did rule it; what is owed is a fresh ruling taken with the
deployment in front of him.

D-85 to D-89 are numbered out of order because they were added after the ledger was first
numbered; the IDs are stable and each sits in the section it belongs to. Numbering passed
100 in the 2026-08-30 wave: a bare `D-101` is this ledger's, a three-segment `D-100-04` is a
backend task's ruling in `backend.md`.

The `CONTRADICTED` rows were ten and are **eleven** as of 2026-08-30: D-09, D-11, D-32,
D-35, D-39, D-42, D-43, D-63, D-81, D-83 and **D-105** — **and a twelfth entry landed on
2026-09-06 that this dated sentence deliberately does not absorb: D-144 is CONTRADICTED in ONE
CLAUSE ONLY, by D-154, with the rest of it standing. Counting it whole would misreport it, and
leaving it out entirely would lose it, so it is named here and counted nowhere** — the newcomer is contradicted by the
repository's own rule rather than by an owner decision, which is a different and worse case
than the six below, and is why it carries an explicit request for a ruling. Of the original ten, six of
them are `CONTRADICTED` because a later owner decision superseded the earlier one, which is
the healthy case: D-32 (`feaa9bc`), D-35 and D-43 (D-88), D-42 (D-89), and D-81 and D-83
(the owner's twenty backend answers of 2026-08-13, recorded as `backend.md` B-02 and B-17).
Both of the last two were `AGENT-PROPOSED` and unowned, so what superseded them is the
first owner statement either question ever received rather than a reversal of one. **No question in
this audit is left open.**

One thing the answers settled by direction rather than by fact, and it is recorded rather
than resolved: whether `/towards-a-dark-factory/the-climb` was deleted at the owner's
instruction is still unknown. Two files say it was and neither quotes him; he answered that
the current page is fine and the children are not of interest. So D-89 endorses the state
going forward, and the historical attribution is closed as not pursued — not as confirmed.

**One consequence of D-88, stated once and not argued.** Keeping `Publish` in the header
means the chrome names a capability the site does not have; the page it opens says so
(`components/upload/UploadFlow.tsx:1185`), and the rule that would have forbidden it, D-12,
is `AGENT-PROPOSED` and unowned, so nothing with authority stands against it.

The largest movement is not in the counts. It is that 22 rows moved from `UNATTRIBUTED` to
`AGENT-PROPOSED` — meaning the audit no longer says *we cannot tell who decided this*, it
says *nobody with authority decided this*.
